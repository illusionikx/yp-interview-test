/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
# TEST-01/Decision #7 (14-04): carries the same APP_KEY + DB credentials as
# .env — never committed to this public repo. See README's "Running the
# browser tests" section for how a fresh clone recreates it.
.env.dusk.local
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

/.claude/worktrees
