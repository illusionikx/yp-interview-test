---
phase: 03-exam-assignment-class-scoped-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Lecturer/ExamAssignmentTest.php
  - tests/Feature/Student/ExamIndexTest.php
  - tests/Feature/Student/ExamAccessTest.php
autonomous: true
requirements: [ASN-01, ASN-02, RBAC-05]

must_haves:
  truths:
    - "The ASN-01 assignment contract (sync, re-sync detach, validation, draft-assignable, student-forbidden) is expressed as failing feature tests (RED)"
    - "The ASN-02 index-visibility contract (published+assigned shows, unpublished excluded, other-class excluded, null-classroom empty) is expressed as failing feature tests"
    - "The RBAC-05 IDOR matrix (assigned+published 200 / different-class 403 / unpublished 403 / null-classroom 403 / lecturer-on-student-routes 403) is expressed as failing feature tests"
  artifacts:
    - path: "tests/Feature/Lecturer/ExamAssignmentTest.php"
      provides: "ASN-01 assignment behaviour (5 cases)"
      contains: "test_a_lecturer_can_assign_an_exam_to_classrooms"
      min_lines: 60
    - path: "tests/Feature/Student/ExamIndexTest.php"
      provides: "ASN-02 index-visibility behaviour (4 cases)"
      contains: "test_a_student_with_no_classroom_sees_an_empty_index"
      min_lines: 55
    - path: "tests/Feature/Student/ExamAccessTest.php"
      provides: "RBAC-05 IDOR matrix (5 cases)"
      contains: "test_a_student_in_a_different_class_is_forbidden"
      min_lines: 70
  key_links:
    - from: "tests/Feature/Lecturer/ExamAssignmentTest.php"
      to: "routes/lecturer.php"
      via: "route('lecturer.exams.assignment.update', $exam) — the endpoint plan 03-02 registers"
      pattern: "lecturer\\.exams\\.assignment\\.update"
    - from: "tests/Feature/Student/ExamAccessTest.php"
      to: "routes/student.php"
      via: "route('student.exams.show', $exam) — the endpoint plan 03-03 registers"
      pattern: "student\\.exams\\.show"
---

<objective>
Wave 0 test infrastructure for Phase 3. Author the three feature test files that encode the full acceptance contract for ASN-01, ASN-02, and RBAC-05 — including the complete RBAC-05 IDOR matrix (200/403/403/403/403) — as RED (failing) tests, before any implementation exists.

These tests are the executable specification the two implementation slices (03-02 assignment, 03-03 student access) are built to satisfy. Writing them first prevents the divergence/omission failure modes this phase exists to prevent and gives each downstream executor a precise `--filter` gate.

Purpose: Establish the Nyquist verification harness (`wave_0_complete`) so implementation is measured against a fixed, pre-committed contract rather than tests written to match whatever got built.
Output: Three failing feature test files under `tests/Feature/Lecturer/` and `tests/Feature/Student/`, exactly matching the file names and case names in 03-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-exam-assignment-class-scoped-access/03-VALIDATION.md
@.planning/phases/03-exam-assignment-class-scoped-access/03-RESEARCH.md

# Analog test (structure to mirror: RefreshDatabase, actingAs, route(), assertDatabaseHas/assertForbidden)
@tests/Feature/Lecturer/ExamControllerTest.php

# Fixtures the tests use (no new factories needed)
@database/factories/ExamFactory.php
@database/factories/UserFactory.php
@app/Models/Exam.php
@app/Models/User.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExamAssignmentTest — the ASN-01 assignment contract (RED)</name>
  <files>tests/Feature/Lecturer/ExamAssignmentTest.php</files>
  <read_first>
    - `tests/Feature/Lecturer/ExamControllerTest.php` — mirror its exact shape: `namespace Tests\Feature\Lecturer;`, `use RefreshDatabase;`, `actingAs()`, `route('lecturer...')`, `assertDatabaseHas`/`assertForbidden`.
    - `database/factories/ExamFactory.php` — `Exam::factory()->create()` (draft) and `->published()` states exist; `created_by` defaults to a lecturer.
    - `database/factories/UserFactory.php` — `User::factory()->lecturer()->create()` and `->student()->create()`.
    - `app/Models/Exam.php` — `classrooms()` belongsToMany over the `exam_classroom` pivot (assert against pivot via `$exam->classrooms` / `assertDatabaseHas('exam_classroom', ...)`).
    - 03-VALIDATION.md §Per-Task Verification Map — the five ASN-01 rows and their exact `--filter` case names.
  </read_first>
  <action>
    Create `tests/Feature/Lecturer/ExamAssignmentTest.php` (namespace `Tests\Feature\Lecturer`, `use RefreshDatabase`) with exactly these five test methods, named verbatim so their `--filter` names match 03-VALIDATION.md:
    (1) `test_a_lecturer_can_assign_an_exam_to_classrooms` — create a published exam and two Classrooms; acting as a lecturer, PUT `route('lecturer.exams.assignment.update', $exam)` with `classroom_ids => [both ids]`; assert redirect back to `lecturer.exams.show` and that `exam_classroom` has one row per classroom for this exam (assertDatabaseHas on the `exam_classroom` pivot for each id).
    (2) `test_resyncing_removes_unselected_classrooms` — pre-attach two classrooms via `$exam->classrooms()->sync([a,b])`; PUT with `classroom_ids => [a]` only; assert `exam_classroom` still has the (exam,a) row and no longer has the (exam,b) row (assertDatabaseMissing) — proves `sync()` detaches, not just attaches.
    (3) `test_assignment_rejects_a_nonexistent_classroom_id` — PUT with `classroom_ids => [999999]`; assert `assertSessionHasErrors('classroom_ids.0')` (the `.*` `exists` rule keys per-item) and that no `exam_classroom` row was written for this exam.
    (4) `test_a_draft_exam_can_be_assigned_before_publishing` — create a DRAFT exam (`Exam::factory()->create()`), assign a classroom, assert the pivot row exists — proves assignment is editable regardless of publish state (D-01).
    (5) `test_a_student_is_forbidden_from_assigning_an_exam` — acting as a `student()`, PUT the same assignment route; `assertForbidden()` (the `role:lecturer` route-group middleware denies before the controller runs) and assert no pivot row was written.
    Use `route()` name `lecturer.exams.assignment.update` throughout (the endpoint 03-02 will register). Do NOT create the route/controller here — this file is expected to FAIL/ERROR until 03-02 lands (RED).
  </action>
  <verify>
    <automated>php -l tests/Feature/Lecturer/ExamAssignmentTest.php prints "No syntax errors"; `php artisan test --filter=ExamAssignmentTest` discovers 5 cases that FAIL or ERROR (RED — route/controller absent), not "No tests executed"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` on the file reports no syntax errors.
    - `php artisan test --filter=ExamAssignmentTest` collects exactly the 5 named methods above.
    - All 5 are RED (fail/error) because `lecturer.exams.assignment.update` does not exist yet — this is the expected Wave 0 state, NOT a task failure.
  </acceptance_criteria>
  <done>File exists with the 5 verbatim-named ASN-01 cases; PHPUnit discovers and runs them; all fail/error for the right reason (missing implementation).</done>
</task>

<task type="auto">
  <name>Task 2: ExamIndexTest — the ASN-02 student-list contract (RED)</name>
  <files>tests/Feature/Student/ExamIndexTest.php</files>
  <read_first>
    - `tests/Feature/Lecturer/ExamControllerTest.php` — same test conventions (create the `tests/Feature/Student/` directory if absent).
    - `app/Models/User.php` — `classroom_id` is fillable, so `User::factory()->student()->create(['classroom_id' => $classroom->id])` works today.
    - `app/Models/Exam.php` — `classrooms()` for attaching an exam to a classroom in the arrange step.
    - `database/factories/ExamFactory.php` — `->published()` vs default draft.
    - 03-VALIDATION.md §Per-Task Verification Map — the four ASN-02 rows and exact case names.
  </read_first>
  <action>
    Create `tests/Feature/Student/ExamIndexTest.php` (namespace `Tests\Feature\Student`, `use RefreshDatabase`) with exactly these four methods:
    (1) `test_a_student_sees_a_published_exam_assigned_to_their_classroom` — arrange a Classroom, a `->published()` exam attached to it, and a student with `classroom_id` = that classroom; GET `route('student.exams.index')`; `assertOk()` and `assertSee($exam->title)`.
    (2) `test_index_excludes_an_unpublished_but_assigned_exam` — same arrange but the exam is DRAFT (not published) yet attached to the student's classroom; GET the index; `assertOk()` and `assertDontSee($exam->title)` — proves `is_published` is ANDed in (Pitfall 3).
    (3) `test_index_excludes_a_published_exam_for_a_different_classroom` — published exam attached to classroom A; student in classroom B; GET index; `assertDontSee($exam->title)`.
    (4) `test_a_student_with_no_classroom_sees_an_empty_index` — a published exam attached to some classroom; student created with `classroom_id => null`; GET index; `assertOk()` and `assertDontSee($exam->title)` — the null-classroom guard (Pitfall 2).
    Use `route('student.exams.index')` (registered by 03-03). Assert on rendered content (`assertSee`/`assertDontSee` on the exam title); do not assert on internal view data. RED until 03-03 lands.
  </action>
  <verify>
    <automated>php -l tests/Feature/Student/ExamIndexTest.php prints "No syntax errors"; `php artisan test --filter=ExamIndexTest` discovers 4 cases that FAIL or ERROR (RED — student.exams.index absent)</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` reports no syntax errors.
    - `php artisan test --filter=ExamIndexTest` collects exactly the 4 named methods.
    - All 4 are RED because `student.exams.index` does not exist yet (expected Wave 0 state).
    - The null-classroom case exists and is named `test_a_student_with_no_classroom_sees_an_empty_index` (Pitfall 2 coverage).
  </acceptance_criteria>
  <done>File exists with the 4 verbatim-named ASN-02 cases including the explicit null-classroom case; PHPUnit discovers and runs them; all RED for the right reason.</done>
</task>

<task type="auto">
  <name>Task 3: ExamAccessTest — the RBAC-05 IDOR matrix (RED)</name>
  <files>tests/Feature/Student/ExamAccessTest.php</files>
  <read_first>
    - `tests/Feature/Lecturer/ExamControllerTest.php` — `assertForbidden()` (403) pattern for the denial cases.
    - `app/Models/User.php` — `classroom_id` fillable and `isStudent()`/`isLecturer()`; needed to build the student/lecturer actors.
    - `app/Models/Exam.php` — `classrooms()` for attaching in the arrange step; `->published()` factory state for the 200 case.
    - 03-VALIDATION.md §Per-Task Verification Map — the five RBAC-05 rows; the IDOR matrix is THE phase gate, enumerate all five.
    - 03-RESEARCH.md §Pitfall 5 — the lecturer-on-student-routes direction must be tested for the NEW routes, not assumed from Phase 1 middleware.
  </read_first>
  <action>
    Create `tests/Feature/Student/ExamAccessTest.php` (namespace `Tests\Feature\Student`, `use RefreshDatabase`) with exactly these five methods — the full IDOR matrix hitting `route('student.exams.show', $exam)` directly (bypassing the list):
    (1) `test_a_student_in_the_assigned_class_can_view_the_published_exam` — published exam attached to the student's classroom; GET show; `assertOk()` (the single 200 case) — this also proves the landing page renders without a `RouteNotFoundException` from the disabled "Start" seam (Pitfall 4).
    (2) `test_a_student_in_a_different_class_is_forbidden` — published exam attached to classroom A; student in classroom B; GET show; `assertForbidden()` (403, NOT a redirect, NOT 404-hidden — the resource is denied, not merely omitted).
    (3) `test_an_unpublished_but_assigned_exam_is_forbidden` — DRAFT exam attached to the student's own classroom; GET show; `assertForbidden()` — `is_published` gate holds even for own-class (Pitfall 3).
    (4) `test_a_student_with_no_classroom_is_forbidden_direct_access` — published exam attached to some classroom; student with `classroom_id => null`; GET show; `assertForbidden()` — null-classroom denial on the direct path (complements the empty-index case).
    (5) `test_a_lecturer_is_forbidden_from_the_student_exam_routes` — acting as a `lecturer()`, GET both `route('student.exams.index')` and `route('student.exams.show', $exam)`; `assertForbidden()` on each — the `role:student` middleware covers these specific new routes (Pitfall 5).
    Use `route('student.exams.show', ...)` / `route('student.exams.index')` (registered by 03-03). RED until 03-03 lands.
  </action>
  <verify>
    <automated>php -l tests/Feature/Student/ExamAccessTest.php prints "No syntax errors"; `php artisan test --filter=ExamAccessTest` discovers 5 cases that FAIL or ERROR (RED — student routes/policy absent)</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` reports no syntax errors.
    - `php artisan test --filter=ExamAccessTest` collects exactly the 5 named methods forming the 200/403/403/403/403 matrix.
    - The lecturer-on-student-routes case (Pitfall 5) and the null-classroom direct-access case (Pitfall 2) are both present.
    - All 5 are RED because the student routes + `ExamPolicy` do not exist yet (expected Wave 0 state).
  </acceptance_criteria>
  <done>File exists with the 5 verbatim-named RBAC-05 cases enumerating the full IDOR matrix; PHPUnit discovers and runs them; all RED for the right reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No runtime code, routes, or inputs introduced. Its role is to make the phase's threat mitigations executable and provable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure / Elevation | student exam show route (IDOR) | verify-here | `ExamAccessTest` cases 2–4 assert 403 on direct access to unassigned/unpublished/null-classroom exams — the executable proof of the mitigation built in 03-03 |
| T-3-03 | Elevation of Privilege | lecturer assignment endpoint reached by a student | verify-here | `ExamAssignmentTest` case 5 asserts a student is 403 on the assignment route (role middleware) |
| T-3-05 | Tampering | forged `classroom_ids` payload | verify-here | `ExamAssignmentTest` case 3 asserts a non-existent classroom id is rejected by validation before any pivot write |
| T-3-06 | Information Disclosure | draft exam pre-assigned, leaking before publish | verify-here | `ExamIndexTest` case 2 + `ExamAccessTest` case 3 assert an unpublished-but-assigned exam is invisible/403 |
| T-3-SC | Tampering | npm/composer installs | accept | No new packages this phase (D-06); `php -l` + PHPUnit are already present. Nothing to install. |
</threat_model>

<verification>
- `php artisan test --filter=ExamAssignmentTest` — 5 cases discovered, all RED (route/controller absent).
- `php artisan test --filter=ExamIndexTest` — 4 cases discovered, all RED (student index absent).
- `php artisan test --filter=ExamAccessTest` — 5 cases discovered, all RED (student routes/policy absent).
- `php artisan test` — the pre-existing Phase 1/2 suite stays green; only the three new files are RED (they are the pending contract, not a regression).
</verification>

<success_criteria>
- Three feature test files exist with the exact file names and verbatim case names from 03-VALIDATION.md (5 + 4 + 5 = 14 cases).
- The RBAC-05 IDOR matrix is fully enumerated (200/403/403/403/403), including the lecturer-on-student-routes and null-classroom cases.
- Every new test is RED for the correct reason (absent implementation), never a syntax/collection error.
- No production code was written in this plan.
</success_criteria>

<output>
Create `.planning/phases/03-exam-assignment-class-scoped-access/03-01-SUMMARY.md` when done.
</output>
