---
phase: 08-v2-0-features-enrollment-exam-availability-user-manuals
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/Feature/Student/SubjectBrowseControllerTest.php
  - tests/Feature/Student/EnrollmentControllerTest.php
  - tests/Feature/Student/AttemptAvailabilityTest.php
  - tests/Feature/Student/ExamShowTest.php
  - tests/Feature/Lecturer/RejectEnrollmentControllerTest.php
  - tests/Feature/Lecturer/ExamAvailabilityTest.php
autonomous: true
requirements: [ENR-01, ENR-02, ENR-03, ENR-04, ENR-05, ENR-06, ENR-07, AVL-01, AVL-02, AVL-03]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "Every Wave 0 gap listed in 08-VALIDATION.md has an executable test file"
    - "Every window-related test asserts the exact-boundary case, not only comfortably-inside/outside cases"
    - "Each test fails for the intended reason (missing route/controller/behavior), not on a fatal error or a typo"
    - "The rejection test suite includes the SEC-03 negative case: a lecturer NOT assigned to the subject is refused"
  artifacts:
    - path: "tests/Feature/Student/SubjectBrowseControllerTest.php"
      provides: "ENR-01 live capacity + ENR-06 out-of-window-listed-not-appliable"
    - path: "tests/Feature/Student/EnrollmentControllerTest.php"
      provides: "ENR-02 capacity refusal, ENR-03 withdraw, ENR-04 one-active, ENR-05 re-apply-updates"
    - path: "tests/Feature/Lecturer/RejectEnrollmentControllerTest.php"
      provides: "ENR-07 reject with fixed reason + SEC-03 non-assigned-lecturer 403"
    - path: "tests/Feature/Lecturer/ExamAvailabilityTest.php"
      provides: "AVL-01 window save incl. the blank-datetime-local null coercion assumption (A1)"
    - path: "tests/Feature/Student/AttemptAvailabilityTest.php"
      provides: "AVL-03 start refused outside window, incl. exact boundary"
    - path: "tests/Feature/Student/ExamShowTest.php"
      provides: "AVL-02 pre-start page content + actions"
  key_links:
    - from: "tests/Feature/Student/AttemptAvailabilityTest.php"
      to: "database/factories/ExamFactory.php"
      via: "window fixtures use 08-01's available()/opening()/closed() states"
      pattern: "->(available|opening|closed)\\(\\)"
  prohibitions:
    - "No production code is written in this plan — test files only (the Phase 3 / Phase 7 Wave-0 precedent)"
    - "No test may claim to prove true concurrency for ENR-02 — the runner is single-threaded"
    - "No test asserts a rejection-reason string outside the locked 5-value set"
---

<objective>
Write the executable acceptance contract for Phase 8's enrollment and availability
surfaces — every Wave 0 gap named in 08-VALIDATION.md — as failing (RED) tests, before
any controller exists.

Purpose: the RED tests are the fixed contract that 08-04 through 08-07 must satisfy.
Pinning them up front prevents test/implementation drift, which is exactly what 03-01's
SUMMARY recorded as the reason for copying test method names verbatim from the validation
strategy. It also front-loads discovery of 08-RESEARCH.md's one flagged unknown (Assumption
A1, blank datetime-local -> null) instead of letting it surface during manual QA.

Output: six test files, all failing for the right reason.

Deliberately excluded: `tests/Feature/Student/AttemptPolicyTest.php`. That file belongs to
08-03, which runs in this same wave — keeping it out of this plan is what makes the two
plans parallel-safe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-VALIDATION.md
@.planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-UI-SPEC.md
@.planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-RESEARCH.md
@.planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-01-SUMMARY.md
</context>

<route_name_contract>
These routes do not exist yet. This plan's `route()` calls FIX their names — 08-04 through
08-07 must implement exactly these. (Route naming is explicitly at Claude's discretion per
08-CONTEXT.md; this is where the discretion is exercised and locked.)

| Name | Verb + URI | Implemented by |
|------|-----------|----------------|
| `student.subjects.index` | GET /student/subjects | 08-04 |
| `student.subjects.show` | GET /student/subjects/{subject} | 08-04 |
| `student.sections.enroll` | POST /student/sections/{section}/enroll | 08-04 |
| `student.sections.withdraw` | DELETE /student/sections/{section}/enroll | 08-04 |
| `lecturer.sections.show` | GET /lecturer/sections/{section} | 08-05 |
| `lecturer.sections.enrollments.reject` | PATCH /lecturer/sections/{section}/enrollments/{student}/reject | 08-05 |

`lecturer.sections.show` is deliberately TOP-LEVEL (a sibling of the existing
`lecturer.sections.index`), not nested under `subjects/{subject}` — this matches
08-UI-SPEC.md and 08-PATTERNS.md, and keeps the roster reachable from the flat sections
list. Existing nested names (`lecturer.subjects.sections.edit` etc.) are untouched.
</route_name_contract>

<tasks>

<task type="auto">
  <name>Task 1: Student enrollment RED contract (ENR-01..ENR-06)</name>
  <files>tests/Feature/Student/SubjectBrowseControllerTest.php, tests/Feature/Student/EnrollmentControllerTest.php</files>
  <read_first>
    - .planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-VALIDATION.md (the Wave 0 Requirements list and the Stated Coverage Limitations — both are binding on this task)
    - .planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-UI-SPEC.md (the "Error / refusal states" copy table — assert these strings verbatim)
    - tests/Feature/Lecturer/SectionControllerTest.php (the RefreshDatabase + actingAs + factory fixture idiom for this codebase)
    - tests/Feature/Student/ExamVisibilityRegressionTest.php (note 07-02's recorded choice to use raw status string literals in data providers so RED failures land in the test body)
    - database/factories/SectionFactory.php (default capacity 30, opens_at now()->subDay(), closes_at now()->addDays(14))
    - app/Models/Section.php (the enrollments() BelongsToMany with ->using(Enrollment::class) — this is how fixtures attach enrollment rows)
  </read_first>
  <action>
    Write both files as failing feature tests using RefreshDatabase and Tests\TestCase, matching
    SectionControllerTest's fixture style. Use the route names from the contract table above.

    SubjectBrowseControllerTest (ENR-01, ENR-06):
    - a student sees a subject's sections listed with live capacity rendered as "{enrolled}/{capacity}"
      (build 2 enrolled students into a capacity-30 section, assert the response sees "2/30")
    - a section at capacity shows the FULL pill and offers no Apply action
    - a section whose window has not opened is still LISTED (ENR-06/Decision #5 — never hidden)
      but offers no Apply action
    - a section whose window has closed is still listed and offers no Apply action
    - an open, non-full section offers an Apply action
    - the subject index lists subjects that have sections
    For "offers no Apply action", assert against the section's own POST url
    (`route('student.sections.enroll', $section)`) rather than the word "Apply", so a second
    eligible section's button on the same page cannot mask the assertion.

    EnrollmentControllerTest (ENR-02, ENR-03, ENR-04, ENR-05):
    - applying to an open, non-full section enrolls immediately with no approval step, and
      flashes the exact success copy "You're enrolled in {section name}."
    - applying to a section already at capacity is refused with the exact copy
      "This section just reached capacity — choose another section." and the enrolled count
      does not increase
    - ENR-02 sequential simulation: fill a capacity-N section to N-1, then two DIFFERENT students
      apply back-to-back; assert exactly one succeeds, one is refused, and the final enrolled
      count equals N — never N+1. Name the method so its limitation is legible, and put a
      doc comment on it stating plainly: this is a SEQUENTIAL simulation, PHPUnit's runner is
      single-threaded and cannot fire two simultaneous requests; it proves the count check and
      that the lock introduces no deadlock or logic error, but the actual concurrency-safety
      argument rests on the structural presence of lockForUpdate() inside the transaction
      (code-review-verifiable, 08-VALIDATION.md "Stated Coverage Limitations"). Do not write a
      test name or comment implying true concurrency coverage exists.
    - applying outside the window is refused SERVER-SIDE (not merely hidden) with the exact copy
      "Enrollment for this section hasn't opened yet." before open, and
      "Enrollment for this section is closed." after close
    - EXACT-BOUNDARY (REQUIREMENTS.md #6): with now() pinned exactly to opens_at, applying
      SUCCEEDS; with now() pinned exactly to closes_at, applying is REFUSED. Use travelTo().
    - withdrawing before closes_at succeeds, flashes "You've withdrawn from {section name}.",
      and leaves the row at status withdrawn
    - withdrawing at/after closes_at is refused with
      "You can no longer withdraw — this section's enrollment window has closed."
      (assert the exact-boundary instant too)
    - ENR-04: a student holding an active enrollment in one section cannot apply to a DIFFERENT
      section of the same subject with the same year+semester; refused with
      "You already have an active enrollment in this subject for this semester. Withdraw first
      if you want to switch sections."
    - ENR-04 negative control: the same student CAN apply to a section of the same subject in a
      DIFFERENT semester, and CAN apply to a section of a different subject
    - ENR-05: after withdrawing, re-applying while the window is open succeeds AND UPDATES the
      existing row — assert `enrollments` has exactly ONE row for (section_id, user_id) after
      the re-apply, not two. This is the assertion that catches an accidental create() against
      the unique(section_id,user_id) index.
    - ENR-05: after being rejected, re-applying while the window is open succeeds, sets status
      back to enrolled, and CLEARS rejection_reason to null (a stale reason must never linger
      on a fresh enrolled row)
    - security (T-08-02-MA): applying with a forged `status` and `rejection_reason` in the POST
      body still produces status=enrolled and rejection_reason=null — request input is never
      forwarded into the write
    - security (T-08-02-IDOR): applying with a forged `user_id` in the POST body enrolls the
      AUTHENTICATED student, never the forged one
    - a lecturer hitting the student enroll route is refused by the role middleware
  </action>
  <verify>
    <automated>php artisan test --filter=SubjectBrowseControllerTest</automated>
    <automated>php artisan test --filter=EnrollmentControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, are syntactically valid, and RUN (each test reaches its assertions or fails on a missing route — no fatal "class not found"/parse error).
    - Every test currently FAILS (RED). A test that passes before 08-04 exists is asserting nothing — fix it.
    - Both window-bearing behaviors (apply, withdraw) have an explicit exact-boundary test using travelTo().
    - The ENR-02 test's name and doc comment state the sequential-simulation limitation explicitly.
    - The ENR-05 test asserts a row COUNT of 1 for (section_id, user_id), not merely that status changed.
    - Refusal assertions use the verbatim strings from 08-UI-SPEC.md's Copywriting Contract.
    - No file outside tests/ is touched.
  </acceptance_criteria>
  <done>The enrollment acceptance contract is executable and failing, with the capacity/window/one-active/re-apply rules pinned to exact copy and exact boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Lecturer reject + exam availability RED contract (ENR-07, AVL-01)</name>
  <files>tests/Feature/Lecturer/RejectEnrollmentControllerTest.php, tests/Feature/Lecturer/ExamAvailabilityTest.php</files>
  <read_first>
    - tests/Feature/Lecturer/SubjectLecturerTest.php (the subject_user assignment fixture idiom — how a lecturer is attached to a subject)
    - tests/Feature/Lecturer/SectionControllerTest.php (the SEC-03 non-assigned-lecturer 403 assertion precedent — mirror it)
    - tests/Feature/Lecturer/ExamControllerTest.php (the exam store/update request-payload shape)
    - tests/Feature/Lecturer/ExamPublishedEditGateTest.php (the existing D-06 draft-only gate coverage — the availability window inherits it)
    - app/Enums/RejectionReason.php (created in 08-01 — the locked 5 values)
    - app/Http/Requests/Lecturer/UpdateExamRequest.php (authorize() returns ! is_published — the D-06 gate that stands)
  </read_first>
  <action>
    RejectEnrollmentControllerTest (ENR-07):
    - a lecturer assigned to the section's subject can reject an enrolled student with a reason
      from the fixed enum; the enrollment row becomes status=rejected with that
      rejection_reason persisted as the enum's BACKING VALUE
    - the flash is the exact copy "{student name} has been rejected from this section."
    - the rejected student then SEES the reason on the subject's sections page, rendered as the
      human LABEL, e.g. "Rejected: Prerequisite not met" (this is the ENR-07 "student can see
      the reason" half — assert against the student-facing page, not just the DB)
    - ENR-07/SEC-03 (the important negative): a lecturer NOT assigned to that subject gets 403.
      Mirror SectionControllerTest's existing non-assigned-lecturer assertion exactly. This is
      the per-subject ownership boundary — never `return true;`.
    - a SECOND lecturer, also assigned to the same subject but not the section's creator, CAN
      reject ("any lecturer of the subject", ENR-07)
    - a reason outside the fixed 5-value enum is rejected with a 422 validation error and the
      enrollment is unchanged
    - a missing reason is a 422 validation error
    - a student hitting the reject route is refused by the role middleware
    Iterate the enum's cases to prove all 5 are accepted rather than hardcoding one.

    ExamAvailabilityTest (AVL-01):
    - a lecturer creating a DRAFT exam can set both available_from and available_until; both persist
    - a lecturer can set only available_from (available_until stays null — unbounded on that side)
    - a lecturer can set only available_until (available_from stays null)
    - submitting BOTH fields BLANK persists null on both and does NOT produce a validation error.
      This is 08-RESEARCH.md's flagged Assumption A1 — an unfilled datetime-local input submits
      as an empty string, and the claim that Laravel's default ConvertEmptyStringsToNull
      middleware coerces it to null before validation was NOT verified against this project.
      Submit the fields as literal empty strings (not omitted keys) so the test actually
      exercises the coercion path. If it fails, the fix belongs in 08-06 (a
      prepareForValidation() normalisation in the Form Requests) — do not paper over it here by
      omitting the keys.
    - available_until earlier than available_from is a 422 validation error
    - a lecturer can UPDATE a draft exam's window
    - D-06 still stands (binding decision, no exception carved): attempting to set the window on
      a PUBLISHED exam is refused by UpdateExamRequest::authorize() with a 403, and the exam's
      window is unchanged. Assert this explicitly — it is a deliberate product constraint
      ("set the window before publishing"), not a bug, and 08-09's lecturer manual documents it.
    - the create and edit forms both render the available_from and available_until inputs
  </action>
  <verify>
    <automated>php artisan test --filter=RejectEnrollmentControllerTest</automated>
    <automated>php artisan test --filter=ExamAvailabilityTest</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, run, and every test FAILS for a missing-implementation reason.
    - RejectEnrollmentControllerTest includes the non-assigned-lecturer 403 case and the second-assigned-lecturer allow case.
    - RejectEnrollmentControllerTest asserts the student-visible reason LABEL, not only the persisted value.
    - Reason validation is asserted across all 5 enum cases by iteration, plus an out-of-enum 422.
    - ExamAvailabilityTest submits blank fields as empty STRINGS (Assumption A1 is genuinely exercised).
    - ExamAvailabilityTest asserts the published-exam 403 — the D-06 gate is covered, not carved out.
    - No file outside tests/ is touched.
  </acceptance_criteria>
  <done>The lecturer reject and exam-availability contracts are executable and failing, with SEC-03 ownership and the D-06 draft-only gate both pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Attempt availability gate + pre-start page RED contract (AVL-02, AVL-03)</name>
  <files>tests/Feature/Student/AttemptAvailabilityTest.php, tests/Feature/Student/ExamShowTest.php</files>
  <read_first>
    - tests/Feature/Student/AttemptStartTest.php (the existing start-attempt fixture + assertions this must not contradict)
    - tests/Feature/Student/ExamAccessTest.php (07-08 split the denial cases into withdrawn / rejected / no-enrollment — reuse that fixture shape)
    - app/Http/Controllers/Student/AttemptController.php (store()'s current authorize('takeable') + firstOrCreate/1062 flow — the new gate slots in between)
    - app/Http/Controllers/Student/ExamController.php (show()'s authorize('takeable') + load('subject')->loadCount('questions'))
    - resources/views/student/exams/show.blade.php (the current pre-start card, the Start/Resume button, the "Back to my exams" link)
    - .planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-UI-SPEC.md (Phase Notes section 3 — the pre-start page contents and the Proceed/Back copy)
    - database/factories/ExamFactory.php (08-01's available()/opening()/closed() states)
  </read_first>
  <action>
    Note for the executor: 08-VALIDATION.md marks ExamShowTest.php as "extend existing", but that
    file does NOT exist — tests/Feature/Student/ holds AttemptShowTest, AttemptStartTest,
    ExamAccessTest, ExamIndexTest and others, but no ExamShowTest. Create it new. Record this
    correction in the SUMMARY so 08-VALIDATION.md can be reconciled.

    AttemptAvailabilityTest (AVL-03):
    - an enrolled student CAN start an attempt on a published exam inside its availability window
    - starting BEFORE available_from is refused with the exact copy
      "This exam is not available yet. It opens {date}." and NO attempt row is created
    - starting AFTER available_until is refused with the exact copy
      "This exam is no longer available. It closed {date}." and NO attempt row is created
    - an exam with both bounds null is startable (unbounded — the default for every pre-Phase-8 exam)
    - an exam with only available_from set, now past it, is startable
    - an exam with only available_until set, now before it, is startable
    - EXACT-BOUNDARY (REQUIREMENTS.md #6): now() pinned exactly to available_from -> start
      SUCCEEDS; now() pinned exactly to available_until -> start is REFUSED. Use travelTo().
    - THE CRITICAL COMPOSITION CASE (AVL-04 boundary, protects against the gate being written in
      the wrong place): a student with an ALREADY-STARTED in-progress attempt can still POST the
      start route after available_until has passed and be routed to their attempt, NOT refused.
      The availability check applies to the NEW-attempt branch only; an existing attempt is
      always resumable. Assert they land on student.attempts.show, not a refusal redirect.
    - refusal takes precedence correctly over enrollment: a NON-enrolled student outside the
      window still gets the existing 403 from authorize('takeable'), not the availability flash
      (the enrollment gate stays first — availability never widens access)
    Build every window fixture from 08-01's ExamFactory states rather than hand-rolled datetimes.

    ExamShowTest (AVL-02) — new file:
    - the pre-start page is reachable by an enrolled student for an exam that is NOT yet available
      and for one that has CLOSED (it must always be reachable to display the "not yet open" /
      "closed" messaging — availability never gates this page, only the start action)
    - the page shows the exam's duration
    - the page shows the exam's description/instructions when present
    - the page shows the availability window
    - the page shows the availability state pill: Available / Opens {date} / Closed
    - the page shows the student's own enrolled section name for this exam's subject
    - the page offers a "Proceed" primary action and a "Back" action returning to student.exams.index
    - a non-enrolled student still gets 403 (the existing takeable gate is unchanged)
    - the page renders the red refusal flash when session('error') is set (the new flash key)
  </action>
  <verify>
    <automated>php artisan test --filter=AttemptAvailabilityTest</automated>
    <automated>php artisan test --filter=ExamShowTest</automated>
    <automated>php artisan test --filter=AttemptStartTest</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, run, and fail for missing-implementation reasons.
    - AttemptAvailabilityTest asserts BOTH exact boundaries with travelTo().
    - AttemptAvailabilityTest includes the already-started-attempt-after-window-close resumable case — the single most important guard against the gate being placed on the wrong branch.
    - AttemptAvailabilityTest asserts no Attempt row is created on a refused start.
    - Refusal copy matches 08-UI-SPEC.md verbatim.
    - ExamShowTest covers all six AVL-02 page elements plus reachability while out-of-window.
    - The existing AttemptStartTest still passes unchanged (this plan must not alter Phase 4's contract).
    - No file outside tests/ is touched.
  </acceptance_criteria>
  <done>The availability gate and pre-start page contracts are executable and failing, with the new-attempt-branch-only rule pinned by test.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Kind | Symbol | Status |
|------|--------|--------|
| Test class | `Tests\Feature\Student\SubjectBrowseControllerTest` | NEW |
| Test class | `Tests\Feature\Student\EnrollmentControllerTest` | NEW |
| Test class | `Tests\Feature\Student\AttemptAvailabilityTest` | NEW |
| Test class | `Tests\Feature\Student\ExamShowTest` | NEW (08-VALIDATION.md said "extend existing" — the file does not exist) |
| Test class | `Tests\Feature\Lecturer\RejectEnrollmentControllerTest` | NEW |
| Test class | `Tests\Feature\Lecturer\ExamAvailabilityTest` | NEW |
| Route-name contract | `student.subjects.index/show`, `student.sections.enroll/withdraw`, `lecturer.sections.show`, `lecturer.sections.enrollments.reject` | FIXED HERE (see `<route_name_contract>`) |

Consumed downstream by: 08-04, 08-05, 08-06, 08-07 — each must satisfy these tests and match the route names byte-for-byte.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> production behavior | A test that passes vacuously (asserts nothing, or is green before the feature exists) is a false safety signal — the primary risk this plan carries. |
| documented coverage -> actual coverage | Claiming concurrency or browser-JS coverage that PHPUnit cannot deliver would mislead the phase gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-VAC | Repudiation | All six test files | mitigate | Acceptance criteria require every test to be RED before implementation lands. A green test here is a defect, not a pass. |
| T-08-02-MA | Tampering | The enroll POST body (forged `status` / `rejection_reason` / `user_id`) | mitigate | EnrollmentControllerTest asserts forged fields are ignored and the authenticated user is the one enrolled. Enrollment extends Pivot (`$guarded = []`), so this is a live risk, not theoretical — the test is what holds 08-04 to literal write arrays. |
| T-08-02-IDOR | Elevation of Privilege | `lecturer.sections.enrollments.reject` | mitigate | RejectEnrollmentControllerTest's non-assigned-lecturer 403 case pins the SEC-03 per-subject boundary before the controller exists, so 08-05 cannot ship with `return true;`. |
| T-08-02-BND | Elevation of Privilege | Half-open window boundaries on apply / withdraw / attempt start | mitigate | Every window-bearing behavior has a dedicated exact-boundary test using travelTo(); an off-by-one is an access-control defect and is treated as one. |
| T-08-02-OVR | Information Disclosure | Overstated coverage in the phase gate | accept (documented) | ENR-02 concurrency and AVL-05 beforeunload are not automatable here (single-threaded runner; no browser JS; Dusk barred by CLAUDE.md's no-new-packages constraint). The ENR-02 test carries an explicit doc comment stating the limitation; AVL-05 is a human-verify checkpoint in 08-08. |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs; no Dusk, no test-helper package. Nothing to audit. |
</threat_model>

<verification>
- All six files exist and execute (no parse errors, no "class not found" fatals).
- Every new test is RED — `php artisan test --filter=Enrollment` and `--filter=Availability` report failures, not errors and not passes.
- `php artisan test` shows the pre-existing 187 tests still green — this plan adds failures only in the new files.
- `git diff --name-only` for this plan touches only `tests/`.
</verification>

<success_criteria>
Every Wave 0 gap in 08-VALIDATION.md has a failing, honest, executable test. Window behaviors
are pinned at their exact boundaries. SEC-03 ownership, mass-assignment resistance, and the
new-attempt-branch-only availability rule are all asserted before any controller exists. Nothing
claims coverage it does not have.
</success_criteria>

<output>
Create `.planning/phases/08-v2-0-features-enrollment-exam-availability-user-manuals/08-02-SUMMARY.md` when done.
Record: the final route-name contract as implemented in the route() calls (08-04..08-07 must
match it byte-for-byte), the outcome of the Assumption A1 blank-datetime-local probe if it can
already be determined, and the ExamShowTest "extend existing -> actually new" correction for
08-VALIDATION.md.
</output>
