---
phase: 06-demo-seeder-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/DatabaseSeederTest.php
  - tests/Feature/ReadmeTest.php
  - tests/Feature/TestAccountSeederTest.php
autonomous: true
requirements: [DEL-01, DEL-02]

must_haves:
  truths:
    - "test_seeder_builds_full_demo_graph asserts the full demo graph by natural key and FAILS (RED) against the current minimal Phase-1 seeder"
    - "test_seeder_is_idempotent_on_repeat_runs asserts re-seeding does not grow row counts"
    - "ReadmeTest asserts README.md exists and contains the setup command, the demo credential email, and the project name — and FAILS (RED) against the current Laravel-default README"
  artifacts:
    - path: "tests/Feature/DatabaseSeederTest.php"
      provides: "Executable RED contract for the expanded seeder graph + idempotency"
      contains: "test_seeder_builds_full_demo_graph"
    - path: "tests/Feature/ReadmeTest.php"
      provides: "Executable RED contract for the project README content"
      contains: "test_readme_documents_setup_and_credentials"
  key_links:
    - from: "tests/Feature/DatabaseSeederTest.php"
      to: "database/seeders/DatabaseSeeder.php"
      via: "$this->seed(DatabaseSeeder::class) then assert-by-natural-key"
      pattern: "seed\\(DatabaseSeeder::class\\)"
    - from: "tests/Feature/ReadmeTest.php"
      to: "README.md"
      via: "file_get_contents(base_path('README.md')) + assertStringContainsString"
      pattern: "base_path\\('README.md'\\)"
---

## Phase Goal

**As a** reviewer with an empty MySQL database and a fresh git clone, **I want to** run one seed command and follow the README, **so that** I can log in as both a Lecturer and a Student and exercise every feature without manually creating any data.

> Story derived faithfully from the ROADMAP Phase 6 goal ("a stranger with an empty MySQL database and a git clone can stand up a working, populated demo end-to-end") and Success Criterion 2 ("the documented demo credentials successfully log in as both a Lecturer and a Student"). Not invented.

## Acceptance Criteria (phase)
- [ ] `php artisan migrate:fresh --seed` on an empty DB produces the full demo graph (DEL-01)
- [ ] Re-running the seeder is idempotent (no duplicate rows) (DEL-01 / D-05)
- [ ] README documents clean-clone setup, DB creation, seed, demo credentials, and how to run/test (DEL-02)

<objective>
Wave 0 (test-first). Author the two RED feature tests that pin this phase's executable contract BEFORE any seeder/README implementation exists, then delete the now-superseded minimal-seeder test.

This is the first MVP slice: the failing end-to-end test for the happy path. Wave 2 (06-02 seeder, 06-03 README) exists solely to turn these RED tests GREEN.

Purpose: Lock the demo-graph shape and README content as a fixed contract so implementation cannot drift.
Output: `tests/Feature/DatabaseSeederTest.php` (RED), `tests/Feature/ReadmeTest.php` (RED); `tests/Feature/TestAccountSeederTest.php` removed (its two assertions become a strict subset of the new graph test — consolidated per 06-RESEARCH.md Wave 0 Gaps).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-seeder-delivery/06-CONTEXT.md
@.planning/phases/06-demo-seeder-delivery/06-RESEARCH.md
@.planning/phases/06-demo-seeder-delivery/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatabaseSeederTest.php — full-graph + idempotency RED contract</name>
  <files>tests/Feature/DatabaseSeederTest.php, tests/Feature/TestAccountSeederTest.php</files>
  <read_first>
    - tests/Feature/TestAccountSeederTest.php — the analog to fold in: its `test_seeder_creates_verified_lecturer_and_classroom_assigned_student` + `test_seeder_is_idempotent_on_repeat_runs` are the seed of this file's assertions; both use RefreshDatabase and assert by natural key already.
    - database/seeders/DatabaseSeeder.php — current minimal Phase-1 seed (lecturer@ + student@ only); the new graph test must FAIL against it.
    - app/Models/{User,Classroom,Subject,Exam,Question,Option,Attempt,Answer}.php — relationship names + fillable for the assertions (Exam::classrooms() uses the exam_classroom pivot; Classroom::subjects() uses classroom_subject).
    - app/Enums/{Role,QuestionType}.php — Role::Lecturer/Student, QuestionType::Mcq/Open (enum values 'mcq'/'open').
    - app/Services/AttemptGrader.php — confirms the demo attempt stays 'submitted' when an open-text answer has null score (syncStatus early-returns while any open-text score is null).
    - .planning/phases/06-demo-seeder-delivery/06-VALIDATION.md — the Per-Task Verification Map (test names are pinned there verbatim).
  </read_first>
  <action>
    Create Tests\Feature\DatabaseSeederTest using the RefreshDatabase trait, with exactly two public methods named verbatim from 06-VALIDATION.md: test_seeder_builds_full_demo_graph and test_seeder_is_idempotent_on_repeat_runs.

    In test_seeder_builds_full_demo_graph: call $this->seed(DatabaseSeeder::class), then assert the full demo graph strictly by natural/business key (email, classroom name, subject code, exam title) and NEVER by a numeric id (MySQL auto-increment does not reset between runs — 06-RESEARCH.md Pitfall 3):
    - lecturer@example.com exists, role === Role::Lecturer, email_verified_at is not null.
    - The three students student@example.com, student2@example.com, student3@example.com each exist, role === Role::Student, email_verified_at not null (assert per-user, not just the first — Pitfall 5).
    - student@example.com and student2@example.com share the classroom_id of the "Demo Classroom" classroom; student3@example.com has the classroom_id of the "Advanced Classroom" classroom.
    - Two subjects exist looked up by code: MATH101 (Mathematics) and SCI101 (Science). "Demo Classroom" is linked to both subjects via classroom_subject; "Advanced Classroom" is linked to MATH101.
    - An exam titled "Mathematics Midterm" under the Mathematics subject exists with is_published true and duration_minutes 30; it has exactly one Mcq question and exactly one Open question; the Mcq question has at least two options with exactly one is_correct true.
    - The exam is assigned to "Demo Classroom" via exam_classroom and is NOT assigned to "Advanced Classroom" (class-scoping demo).
    - A demo attempt for student2@example.com on that exam exists with status 'submitted'; its answer to the Mcq question has is_correct true and score equal to that question's points; its answer to the Open question has a null score (still pending).

    In test_seeder_is_idempotent_on_repeat_runs: call $this->seed(DatabaseSeeder::class) twice and assert the row count of each of User, Classroom, Subject, Exam, Question, Option, Attempt, Answer is identical after the second run as after the first (assert counts, not ids).

    Then delete tests/Feature/TestAccountSeederTest.php — its two assertions are a strict subset of the above and keeping it would make a `--filter=test_seeder_is_idempotent_on_repeat_runs` run match two classes and duplicate maintenance (06-RESEARCH.md Wave 0 Gaps: consolidate).

    Do not write any production code in this wave — only the test file. These tests MUST fail now (test_seeder_builds_full_demo_graph in particular), which is the point: they are the fixed RED contract Wave 2 satisfies.
  </action>
  <verify>
    <automated>php artisan test --filter=DatabaseSeederTest</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/DatabaseSeederTest.php exists with both named methods.
    - Running the filter FAILS today on test_seeder_builds_full_demo_graph (proves a real RED contract against the minimal seeder, not a vacuous pass).
    - tests/Feature/TestAccountSeederTest.php no longer exists.
  </acceptance_criteria>
  <done>DatabaseSeederTest.php exists with both pinned methods asserting by natural key; the graph test is RED against the minimal seeder; TestAccountSeederTest.php is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: ReadmeTest.php — README content RED contract</name>
  <files>tests/Feature/ReadmeTest.php</files>
  <read_first>
    - README.md — the current Laravel-default README (contains "Laravel is a web application framework..."); the assertions below must FAIL against it.
    - .planning/phases/06-demo-seeder-delivery/06-VALIDATION.md — pins the test name test_readme_documents_setup_and_credentials and the assertStringContainsString targets.
    - .planning/phases/06-demo-seeder-delivery/06-RESEARCH.md — README structure (setup command block, demo-credentials table) the README (06-03) will satisfy.
    - .planning/PROJECT.md — the exact project title "Online Examination Portal".
  </read_first>
  <action>
    Create Tests\Feature\ReadmeTest (no database, no RefreshDatabase) with one public method named verbatim test_readme_documents_setup_and_credentials.

    In it: assertFileExists(base_path('README.md')); read the contents once via file_get_contents(base_path('README.md')); then assertStringContainsString for each of these exact substrings — 'Online Examination Portal', 'composer install', 'npm install', 'yp-student-exam', 'migrate:fresh --seed', 'lecturer@example.com', 'student@example.com'. These prove the README is the project README (title + setup commands + DB name + one-command seed + both demo login emails), not the Laravel default.

    This MUST fail now (RED) because the current README is the Laravel default and contains none of these strings. Do not touch README.md in this task — only create the test.
  </action>
  <verify>
    <automated>php artisan test --filter=ReadmeTest</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/ReadmeTest.php exists with the named method.
    - Running the filter FAILS today because the current Laravel-default README lacks every asserted substring.
  </acceptance_criteria>
  <done>ReadmeTest.php exists with test_readme_documents_setup_and_credentials; it is RED against the current Laravel-default README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → live MySQL `yp-student-exam` | RefreshDatabase migrates/rolls back the configured DB; tests run against real MySQL (phpunit.xml overrides commented out), so a test run wipes any seeded demo data (documented, not a defect). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Documented weak demo credentials asserted by ReadmeTest | accept | Passwords are the deliberate, disclosed demo convention (`password`); the README (06-03) frames them as demo/evaluation-only. Test only asserts their presence, introduces no new secret. |
| T-06-SC | Tampering | npm/composer installs | accept | No packages installed this phase — tests use already-vetted PHPUnit 11 only (06-RESEARCH.md Package Legitimacy Audit: no external packages). No `[ASSUMED]`/`[SUS]` packages, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `php artisan test --filter=DatabaseSeederTest` — both methods present; graph test RED against the minimal seeder.
- `php artisan test --filter=ReadmeTest` — RED against the Laravel-default README.
- These RED results are expected and required in Wave 0; the full suite is not green until Wave 2 implements the seeder (06-02) and README (06-03).
</verification>

<success_criteria>
- Both test files exist with the pinned method names from 06-VALIDATION.md.
- Each new test currently FAILS for the right reason (missing graph / default README), establishing the executable contract.
- TestAccountSeederTest.php removed (consolidated).
</success_criteria>

## Source Coverage Audit (plan set 06-01..06-04)

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL (ROADMAP §Phase 6) | "clean clone stands up a working, populated demo" | 06-02 (seeder) + 06-03 (README) + 06-04 (clean-clone gate) |
| GOAL (Success Criterion 1) | `migrate:fresh --seed` produces lecturer/students/classrooms/subjects/exam(both types) assigned to a classroom | 06-02, verified 06-01 + 06-04 |
| GOAL (Success Criterion 2) | README steps + demo credentials log in as both roles | 06-03 + 06-04 |
| REQ DEL-01 | Seeder creates demo data via `migrate:fresh --seed` | 06-01 (test), 06-02 (impl), 06-04 (gate) |
| REQ DEL-02 | README documents setup/DB/migrate/seed/credentials/run+test | 06-01 (test), 06-03 (impl), 06-04 (gate) |
| RESEARCH | idempotent firstOrCreate; sync pivots; wasRecentlyCreated child guard; pre-graded attempt via AttemptGrader (open-text left null → stays submitted); email_verified_at on every user; don't touch defaultStringLength(191); firstOrCreate not updateOrCreate | 06-02 |
| RESEARCH | README structure + Pitfall 8 (tests wipe demo DB) + Security Domain | 06-03 |
| RESEARCH | test map (DatabaseSeederTest + ReadmeTest) | 06-01 |
| CONTEXT D-01 | full idempotent demo graph | 06-02 |
| CONTEXT D-02 | one pre-graded submitted demo attempt; first student un-attempted | 06-02 |
| CONTEXT D-03 | fixed documented credentials | 06-02 (pins) + 06-03 (documents) |
| CONTEXT D-04 | replace Laravel-default README | 06-03 |
| CONTEXT D-05 | clean-clone `migrate:fresh --seed` acceptance gate | 06-04 |
| CONTEXT D-06 | do NOT automate git push; README documents it only | 06-03 (note, no push command) + honored across all plans |

**Exclusions (not gaps):** the actual `git push` to a public repo (D-06 → user-gated, deferred), v2 features (partial-credit/analytics), CI/CD/Docker — all Deferred Ideas in 06-CONTEXT.md. No unplanned source items remain.

<output>
Create `.planning/phases/06-demo-seeder-delivery/06-01-SUMMARY.md` when done.
</output>
