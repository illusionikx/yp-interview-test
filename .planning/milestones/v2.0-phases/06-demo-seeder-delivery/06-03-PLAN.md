---
phase: 06-demo-seeder-delivery
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - README.md
autonomous: true
requirements: [DEL-02]

must_haves:
  truths:
    - "README.md is the project README (title 'Online Examination Portal' + two-role overview), not the Laravel default"
    - "README documents the full clean-clone path: clone, composer install, npm install, copy .env, key:generate, create the yp-student-exam DB + set DB_*, migrate:fresh --seed, npm run build, php artisan serve"
    - "README lists the four seeded demo credentials exactly matching the seeder, warns that php artisan test wipes the demo DB, and documents publishing to GitHub as a manual user step (no automated push)"
  artifacts:
    - path: "README.md"
      provides: "Project README covering setup, DB creation, seeding, demo credentials, per-role walkthrough, tests, and a GitHub-publishing note (D-04, D-06)"
      contains: "migrate:fresh --seed"
      min_lines: 60
  key_links:
    - from: "README.md"
      to: "database/seeders/DatabaseSeeder.php"
      via: "demo-credentials table lists the exact seeded emails/password"
      pattern: "lecturer@example.com"
    - from: "README.md"
      to: ".env.example"
      via: "documents copying .env.example and setting the DB_* keys it defines"
      pattern: "\\.env"
---

## Phase Goal

**As a** reviewer with an empty MySQL database and a fresh git clone, **I want to** run one seed command and follow the README, **so that** I can log in as both a Lecturer and a Student and exercise every feature without manually creating any data.

## Acceptance Criteria (this plan)
- [ ] `ReadmeTest::test_readme_documents_setup_and_credentials` (06-01) passes (DEL-02)
- [ ] README documents DB creation, migrate, seed, credentials, run, and how to test — and the GitHub-publish step as manual (D-04, D-06)

<objective>
The documentation slice: replace the Laravel-default README.md with a project README so a stranger can go from a fresh clone to a logged-in session as both roles by following the doc alone. After this plan, the "follow the README" half of the phase goal is a real capability.

Purpose: Deliver DEL-02 and satisfy the Wave 0 ReadmeTest contract.
Output: Replaced `README.md`.

Credentials documented here MUST match 06-02's seeder exactly (fixed per D-03): password `password` for every account; lecturer@example.com, student@example.com (un-attempted), student2@example.com (submitted attempt awaiting open-text grading), student3@example.com (in a classroom without the exam — class-scoping demo).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-demo-seeder-delivery/06-CONTEXT.md
@.planning/phases/06-demo-seeder-delivery/06-RESEARCH.md
@routes/lecturer.php
@routes/student.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Laravel-default README with the project README</name>
  <files>README.md</files>
  <read_first>
    - README.md — the current Laravel-default README being replaced wholesale.
    - tests/Feature/ReadmeTest.php — the exact substrings this README must contain (from 06-01): 'Online Examination Portal', 'composer install', 'npm install', 'yp-student-exam', 'migrate:fresh --seed', 'lecturer@example.com', 'student@example.com'.
    - .env.example — run `cat .env.example` (or Read it) FIRST to confirm the exact DB connection variable names (DB_CONNECTION / DB_DATABASE / DB_USERNAME / DB_PASSWORD) before documenting the .env edits (06-RESEARCH.md Open Question 1 — the exact keys must be quoted from the real file, not assumed).
    - composer.json + package.json — the real setup/build script names to reference (composer install, npm install, npm run build / npm run dev).
    - routes/lecturer.php + routes/student.php — the real feature surface for the per-role walkthrough (lecturer: subjects/classrooms/exams CRUD, publish, assignment, results + attempts.answers.grade; student: exams.index/show, attempts start/answer/submit, attempts.result).
    - .planning/PROJECT.md — the one-paragraph app overview + two roles + stack + the "public GitHub repo" delivery constraint.
    - .planning/phases/06-demo-seeder-delivery/06-RESEARCH.md — README structure, the setup command block, the demo-credentials table shape, Pitfall 8 (tests wipe the demo DB), and the Security Domain notes.
  </read_first>
  <action>
    Replace README.md entirely (D-04) with a project README, in this section order:

    1. Title "Online Examination Portal" and a one-paragraph overview naming the two roles — a Lecturer authors subject exams (MCQ + open-text), publishes and assigns them to classrooms, and grades open-text answers; a Student logs in, sees only the exams assigned to their classroom, takes them under a server-enforced timer, and views their graded result.

    2. Tech stack: Laravel 11 + Breeze, MySQL, Blade + Tailwind + Alpine (no SPA).

    3. Setup from a clean clone (a copyable command sequence): git clone the repo; composer install; npm install; cp .env.example .env; php artisan key:generate; create the MySQL database named yp-student-exam (show an example CREATE DATABASE command and note the exact invocation depends on the reader's local MySQL client/credentials) and set DB_DATABASE (and DB_USERNAME/DB_PASSWORD) in .env to match — using the exact variable names confirmed from .env.example; php artisan migrate:fresh --seed; npm run build (note npm run dev as the local-iteration alternative); php artisan serve.

    4. Seeded demo credentials — a table listing all four accounts with password `password`: lecturer@example.com (Lecturer), student@example.com (Student — un-attempted, take the exam fresh), student2@example.com (Student — has a submitted attempt awaiting open-text grading), student3@example.com (Student — in a classroom the exam is not assigned to, demonstrating class-scoped access). Explicitly frame these as demo/evaluation-only credentials for a scoped grading deliverable, NOT a production security posture (threat T-06-01).

    5. Per-role walkthrough. Lecturer: manage classrooms and subjects, author + publish + assign the Mathematics Midterm exam, grade student2's pending open-text answer, then view results per exam/student. Student: sign in, see the assigned exam, start it under the live countdown, submit, and view the graded result once grading completes.

    6. Running the tests: php artisan test — WITH the caveat (Pitfall 8) that the suite uses RefreshDatabase against the configured MySQL yp-student-exam database and will wipe the seeded demo data; re-run php artisan migrate:fresh --seed afterward to restore the demo dataset for manual review.

    7. Publishing to GitHub — a short note that the intended delivery is a public repository; that the committer must set a correct git author identity before pushing; and that only .env.example (placeholders) is ever committed — never a filled .env with a real APP_KEY or DB credentials (threat T-06-03). This section documents the intent only; it MUST NOT contain any git push / git remote add / repo-creation command, because the push is a user-gated step performed outside this phase (D-06).

    Do not instruct setting APP_DEBUG=true in any step (Security Domain). Keep every credential and command string byte-identical to what the 06-02 seeder produces and what ReadmeTest asserts.
  </action>
  <verify>
    <automated>php artisan test --filter=ReadmeTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=ReadmeTest` passes.
    - README contains the project title, composer install, npm install, yp-student-exam, migrate:fresh --seed, and both lecturer@example.com and student@example.com (all four demo emails present).
    - README documents the tests-wipe-demo-data caveat and the manual GitHub-publishing note, and contains NO git push / git remote command.
  </acceptance_criteria>
  <done>README.md is the project README (overview, stack, clean-clone setup, DB creation, seeding, demo credentials matching the seeder, per-role walkthrough, tests-with-wipe-caveat, manual GitHub-publish note); ReadmeTest passes; no automated push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README + repo → public internet | The README and code are the graded, publicly-shipped deliverable; anything written here becomes public. |
| documented `.env` setup → committed files | The README tells readers how to build `.env`; only `.env.example` (placeholders) may ever be committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Publicly-documented weak demo credentials | mitigate | README explicitly frames the credentials as demo/evaluation-only, not a production security posture — so a reader does not mistake the weak password for the app's real security bar. |
| T-06-03 | Information Disclosure | Accidental commit of a real `.env` (APP_KEY / DB creds) | mitigate | README's "Publishing to GitHub" note states only `.env.example` is committed and `.env` stays gitignored (standard Laravel `.gitignore`, unchanged by this phase). |
| T-06-04 | Information Disclosure | README instructing an insecure prod flag (APP_DEBUG=true) | mitigate | No documented step sets APP_DEBUG=true (06-RESEARCH.md Security Domain). |
| T-06-06 | Elevation of Privilege | Automated push to an unrotated/leaked-token repo (build history carries a leaked GitLab token — D-06) | mitigate | README documents publishing as a manual user step ONLY; this plan writes no push/remote command, so nothing is published from here (D-06). |
| T-06-SC | Tampering | composer/npm installs | accept | No packages installed this phase (06-RESEARCH.md Package Legitimacy Audit: none). No legitimacy checkpoint applies. |
</threat_model>

<verification>
- `php artisan test --filter=ReadmeTest` — green.
- Manual read-through: the setup sequence is copy-paste runnable and the credential table matches the seeder.
- grep the README for `git push` / `git remote add` — must return nothing (D-06).
</verification>

<success_criteria>
- README.md is the project README covering overview, stack, clean-clone setup, DB creation, seeding, demo credentials, per-role walkthrough, tests (with the wipe caveat), and a manual GitHub-publishing note (DEL-02, D-04).
- Credentials/commands match the 06-02 seeder exactly; no automated push (D-06).
</success_criteria>

<output>
Create `.planning/phases/06-demo-seeder-delivery/06-03-SUMMARY.md` when done.
</output>
