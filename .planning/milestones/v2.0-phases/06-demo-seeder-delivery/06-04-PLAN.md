---
phase: 06-demo-seeder-delivery
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: true
requirements: [DEL-01, DEL-02]

must_haves:
  truths:
    - "php artisan migrate:fresh --seed succeeds on the empty yp-student-exam schema (all migrations + the full seed, no 'Specified key was too long')"
    - "The full test suite (Phases 1-5 + DatabaseSeederTest + ReadmeTest) is green"
    - "Following only the README, a human logs in as both a Lecturer and a Student and the author/assign/take/grade/results + class-scoping walkthrough behaves as documented"
  artifacts: []
  key_links:
    - from: "clean-clone gate"
      to: "database/seeders/DatabaseSeeder.php + README.md"
      via: "migrate:fresh --seed then README-driven browser walkthrough as both roles"
      pattern: "migrate:fresh --seed"
---

## Phase Goal

**As a** reviewer with an empty MySQL database and a fresh git clone, **I want to** run one seed command and follow the README, **so that** I can log in as both a Lecturer and a Student and exercise every feature without manually creating any data.

## Acceptance Criteria (this plan)
- [ ] `php artisan migrate:fresh --seed` exits 0 on the MySQL DB (DEL-01 / D-05)
- [ ] Full `php artisan test` suite green (DEL-01, DEL-02)
- [ ] (end-of-phase human check) Both roles log in via the README steps and the walkthrough behaves as documented (DEL-02 / D-05)

<objective>
The end-to-end acceptance gate (D-05): prove the whole clean-clone path works after the seeder (06-02) and README (06-03) are in place. This is the phase's gate — the single command the seeder must survive, plus the human confirmation that the README alone gets a stranger logged in as both roles.

Purpose: Close DEL-01 + DEL-02 against the real MySQL database, not just the transactional test harness.
Output: No source files — a verification record (SUMMARY) capturing the migrate:fresh --seed result, the full-suite result, and the README-driven walkthrough outcome.

Note: `workflow.human_verify_mode` is `end-of-phase`, so the browser walkthrough is expressed as a `<human-check>` (collected at phase verification), not a blocking checkpoint — this plan stays autonomous.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-demo-seeder-delivery/06-CONTEXT.md
@.planning/phases/06-demo-seeder-delivery/06-VALIDATION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean-clone acceptance gate — migrate:fresh --seed + full suite + README walkthrough</name>
  <files></files>
  <read_first>
    - .planning/phases/06-demo-seeder-delivery/06-VALIDATION.md — the Manual-Only Verifications table (clean-clone end-to-end instructions) and the Phase gate definition.
    - README.md — the documented setup + per-role walkthrough steps to exercise verbatim (drive the app using ONLY what the README says, no insider knowledge).
    - .planning/phases/06-demo-seeder-delivery/06-RESEARCH.md — the clean-clone delivery flow diagram and Pitfall 6 (defaultStringLength guard makes migrate:fresh survive an empty utf8mb4 schema).
  </read_first>
  <action>
    Run the phase acceptance gate (D-05).

    Automated: execute php artisan migrate:fresh --seed against the configured yp-student-exam MySQL database and confirm it exits 0 — all 13 migrations plus the full seed complete with no "Specified key was too long" error (that error would mean Schema::defaultStringLength(191) was disturbed — Pitfall 6). Then run the full suite php artisan test and confirm it is green: the Phases 1-5 tests plus the new DatabaseSeederTest and ReadmeTest. Because php artisan test runs RefreshDatabase against the same MySQL DB and wipes the demo data (Pitfall 8), re-run php artisan migrate:fresh --seed after the suite so the demo dataset is restored for the human walkthrough.

    Then a human confirms the browser end-to-end using ONLY the README's documented steps (see human-check). Do NOT run git push, git remote add, or create any remote — publishing is a user-gated step outside this phase (D-06).
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed && php artisan test</automated>
    <human-check>
      Following ONLY the README (no insider knowledge), after `php artisan migrate:fresh --seed` and `php artisan serve`:
      1. Log in as lecturer@example.com / password — the Mathematics Midterm shows a pending open-text answer for student2's submitted attempt; grade it; view results per exam/student.
      2. Log out; log in as student@example.com / password — the assigned Mathematics Midterm is visible; start it under the live countdown timer and submit.
      3. Log in as student3@example.com / password — the Mathematics Midterm is NOT visible (class-scoped access: student3 is in "Advanced Classroom", which the exam is not assigned to).
      Expected: both roles log in successfully and every step behaves as the README describes.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh --seed` exits 0 on the empty/live yp-student-exam MySQL schema (migrations + full seed, no key-length error).
    - `php artisan test` is fully green (Phases 1-5 + DatabaseSeederTest + ReadmeTest).
    - (end-of-phase human-check) Both roles log in via the README steps; lecturer grading + student take + student3 class-scoped denial behave as documented.
    - No git push / remote was performed (D-06).
  </acceptance_criteria>
  <done>migrate:fresh --seed exits 0 on the live MySQL DB; the full suite is green; (end-of-phase human-check) both roles log in via the README and the walkthrough + class-scoping behave as documented; nothing pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI (migrate:fresh --seed / php artisan test) → MySQL `yp-student-exam` | Both commands rebuild/wipe the live dev DB; the gate must restore the seed after the suite. |
| README-only walkthrough → browser session | A stranger drives the app using only the doc; anything the README omits surfaces here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | migrate:fresh on a genuinely empty utf8mb4 schema | mitigate | The gate specifically exercises the empty-schema path; a "Specified key was too long" failure would flag a disturbed Schema::defaultStringLength(191) guard before ship (Pitfall 6). |
| T-06-06 | Elevation of Privilege | Accidental publish/push during verification (leaked-token history — D-06) | mitigate | The gate runs only migrate:fresh --seed + php artisan test; it performs no git push / remote add. Publishing stays a user-gated step. |
| T-06-SC | Tampering | composer/npm installs | accept | No packages installed this phase (06-RESEARCH.md Package Legitimacy Audit: none). No legitimacy checkpoint applies. |
</threat_model>

<verification>
- `php artisan migrate:fresh --seed` — exit 0, full graph seeded on the live MySQL DB.
- `php artisan test` — full suite green; then re-seed to restore demo data for the walkthrough.
- Human-check (end-of-phase): README-driven login + walkthrough as both roles, plus student3 class-scoped denial.
</verification>

<success_criteria>
- The clean-clone one-command path (`migrate:fresh --seed`) succeeds on MySQL and the full suite is green (DEL-01).
- A stranger, using only the README, logs in as both a Lecturer and a Student and exercises the documented walkthrough (DEL-02, D-05).
- Nothing was pushed/published (D-06).
</success_criteria>

<output>
Create `.planning/phases/06-demo-seeder-delivery/06-04-SUMMARY.md` when done.
</output>
