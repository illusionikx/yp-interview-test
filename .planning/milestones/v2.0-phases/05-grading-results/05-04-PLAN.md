---
phase: 05-grading-results
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - app/Http/Controllers/Lecturer/ResultController.php
  - resources/views/lecturer/results/index.blade.php
  - routes/lecturer.php
  - resources/views/lecturer/exams/show.blade.php
autonomous: true
requirements: [GRD-05]

must_haves:
  truths:
    - "A lecturer can open a per-exam results index listing every attempt with its status and total score (D-06/GRD-05)"
    - "While an attempt is still pending, its score column shows a dash — never a partial number (Pitfall 3 applied to the list)"
    - "Each row links to the per-attempt drill-in (Grade while submitted-with-pending, View once graded)"
    - "The lecturer exam page has a 'View Results' entry point routing to that index (D-06)"
  artifacts:
    - path: "app/Http/Controllers/Lecturer/ResultController.php"
      provides: "index($exam) listing the exam's attempts"
      contains: "index"
    - path: "resources/views/lecturer/results/index.blade.php"
      provides: "per-exam attempt table (Student / Status / Score / Action) + empty state"
      contains: "No submissions yet"
    - path: "resources/views/lecturer/exams/show.blade.php"
      provides: "'View Results' link in the exam action row"
      contains: "View Results"
  key_links:
    - from: "resources/views/lecturer/exams/show.blade.php"
      to: "app/Http/Controllers/Lecturer/ResultController.php"
      via: "'View Results' link routes to lecturer.exams.results.index"
      pattern: "exams.results.index"
    - from: "resources/views/lecturer/results/index.blade.php"
      to: "app/Http/Controllers/Lecturer/ResultController.php"
      via: "each row's Grade/View link routes to lecturer.results.show"
      pattern: "results.show"
---

<objective>
The lecturer browse slice that closes GRD-05: a per-exam results index listing every attempt with status + total score, an entry point from the exam page, and per-row links into the drill-in built in 05-03. This completes the phase — every submitted attempt is now reachable, gradeable, and reviewable per exam and per student.

**After this plan, a real lecturer can:** click "View Results" on an exam, see all its attempts and scores at a glance, and drill into any one.

Purpose: Deliver GRD-05 (lecturer views results per exam and per student). Implements D-06.
Output: Lecturer\ResultController@index + results index view + results route + "View Results" link on the exam page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-grading-results/05-CONTEXT.md
@.planning/phases/05-grading-results/05-RESEARCH.md
@.planning/phases/05-grading-results/05-UI-SPEC.md
@app/Models/Exam.php
@app/Models/Attempt.php
@app/Http/Controllers/Lecturer/ResultController.php
@routes/lecturer.php
@resources/views/lecturer/exams/index.blade.php
@resources/views/lecturer/exams/show.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lecturer results index — ResultController@index, route, and index view</name>
  <files>app/Http/Controllers/Lecturer/ResultController.php, resources/views/lecturer/results/index.blade.php, routes/lecturer.php</files>
  <read_first>
    - app/Http/Controllers/Lecturer/ResultController.php (extend the existing controller from 05-03 by adding index($exam); keep show() untouched)
    - resources/views/lecturer/exams/index.blade.php (analog: max-w-7xl, the min-w-full divide-y table pattern, th/td classes, the colspan empty-row pattern — reuse verbatim)
    - app/Models/Exam.php (attempts() relation, questions()->sum('points') for the total possible)
    - .planning/phases/05-grading-results/05-UI-SPEC.md (Screen 2: columns Student / Status badge / Score / Action; "—" for score while pending; Grade vs View action copy; "No submissions yet" empty state)
    - routes/lecturer.php (add the index route inside the role:lecturer group)
  </read_first>
  <action>
    Add index(Exam $exam) to App\Http\Controllers\Lecturer\ResultController (role:lecturer group is the sole gate, D-06 — no per-lecturer ownership). Load the exam's attempts with their user and eager-load what the table needs; compute the total possible once (exam.questions()->sum('points')). Pass attempts + totals to the view.
    Create resources/views/lecturer/results/index.blade.php per UI-SPEC Screen 2: max-w-7xl container; exam title in the x-slot header; a table (reusing the exams/index.blade.php pattern) with columns Student (name), Status (amber Submitted / green Graded badge), Score ("{score} / {total}" once graded, a plain "—" while submitted-and-pending — never a partial number, Pitfall 3 applied to the list), and Action (a "Grade" link to lecturer.results.show while status is submitted with pending open-text, a "View" link to the same route once graded). Render a single full-width colspan row with the "No submissions yet" copy when the exam has no attempts.
    Register the route in routes/lecturer.php inside the role:lecturer group: GET exams/{exam}/results → ResultController@index, name exams.results.index.
  </action>
  <verify>
    <automated>php artisan test --filter=test_index_lists_attempts_per_exam</automated>
  </verify>
  <done>The per-exam results index lists each attempt with status + score ("—" while pending) and links into the drill-in; test_index_lists_attempts_per_exam is green.</done>
  <acceptance_criteria>
    - `php artisan test --filter=test_index_lists_attempts_per_exam` → GREEN (Tests\Feature\Lecturer\ResultTest::test_index_lists_attempts_per_exam).
    - Each attempt row shows its status + score; a pending attempt shows "—", never a partial number.
    - Rows link into lecturer.results.show; the empty exam shows "No submissions yet".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: "View Results" entry point on the lecturer exam page + full-suite gate</name>
  <files>resources/views/lecturer/exams/show.blade.php</files>
  <read_first>
    - resources/views/lecturer/exams/show.blade.php (the existing action row holding Publish/Unpublish/Edit/Delete — add the "View Results" link alongside, routing to the new index; match the existing text-indigo-600 link style)
    - .planning/phases/05-grading-results/05-UI-SPEC.md (Copywriting: "View Results" link added to the exam show action row, D-06 entry point)
  </read_first>
  <action>
    Add a "View Results" link to the action row in resources/views/lecturer/exams/show.blade.php (next to the existing Publish/Unpublish/Edit/Delete controls), routing to route('lecturer.exams.results.index', $exam). Use the established indigo text-link style (text-indigo-600 hover:text-indigo-900 text-sm). This is the sole entry point into the results area (D-06); it should be visible regardless of publish state (a lecturer reviews results after students have submitted, which may be after unpublish).
    This task closes the phase — after wiring the link, run the full suite as the phase regression gate.
  </action>
  <verify>
    <automated>php artisan test</automated>
  </verify>
  <done>The exam page links to "View Results"; both Lecturer\ResultTest methods are green and the entire Phase 1-5 suite passes.</done>
  <acceptance_criteria>
    - The lecturer exam show page renders a "View Results" link to lecturer.exams.results.index.
    - `php artisan test --filter=ResultTest` → both Tests\Feature\Lecturer\ResultTest methods GREEN (index + show).
    - `php artisan test` → the ENTIRE suite is green (Phase 1-4 + all Phase 5 tests), no regression.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lecturer browser → GET /lecturer/exams/{exam}/results | A lecturer identity + route-bound exam id cross into the results listing; access is gated by role:lecturer only (any lecturer may view any exam's results, per precedent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure (premature score in the list) | lecturer/results/index.blade.php | mitigate | Score column renders "—" while an attempt is still pending; a numeric total appears only once status===graded (Pitfall 3 applied to the list view). |
| T-05-10 | Elevation of Privilege | results index route | mitigate | role:lecturer route-group middleware is the sole, sufficient gate; no per-lecturer ownership is added (deliberate — matches the Phase 2/3 "any lecturer" convention, 05-RESEARCH.md Anti-Patterns / Assumption A2). |
| T-05-SC | Tampering | Composer dependency surface | accept | No new packages (Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `php artisan test --filter=test_index_lists_attempts_per_exam` → GREEN.
- `php artisan test --filter=ResultTest` → 2 Lecturer methods GREEN (plus the 5 Student methods still GREEN from 05-02).
- `php artisan test` → full suite green — the phase gate.
- Manual (UI-SPEC, human-check): from an exam page, click "View Results", confirm the attempt list shows statuses/scores with "—" for pending, and each row drills into the breakdown.
</verification>

<success_criteria>
- A lecturer can browse per-exam results and drill into any student's attempt.
- The full test suite (Phases 1-5) is green — the phase is functionally complete.
</success_criteria>

<output>
Create `.planning/phases/05-grading-results/05-04-SUMMARY.md` when done.
</output>
