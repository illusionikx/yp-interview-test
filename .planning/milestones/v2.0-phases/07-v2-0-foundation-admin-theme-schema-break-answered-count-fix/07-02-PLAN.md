---
phase: 07-v2-0-foundation-admin-theme-schema-break-answered-count-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/DomainSchemaTest.php
  - tests/Feature/DatabaseSeederTest.php
  - tests/Feature/Lecturer/SectionControllerTest.php
  - tests/Feature/Lecturer/SubjectLecturerTest.php
  - tests/Feature/Student/ExamVisibilityRegressionTest.php
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, ENR-08, DEL-03]
must_haves:
  truths:
    - "The ENR-08 cross-consumer regression test exists and asserts, for all four enrollment states (enrolled/withdrawn/rejected/never-applied), that the exam LIST (Exam::visibleTo) and the direct-access GATE (ExamPolicy::takeable) return the exact same visibility — this is the phase's hard acceptance gate"
    - "A section CRUD test asserts a lecturer assigned to a subject can create/edit/delete a section with capacity + enrollment window, and the section name resolves to year-semester-sequence (SEC-01/SEC-02)"
    - "A subject-lecturer test asserts a subject can be assigned to multiple lecturers, any assigned lecturer can manage its sections, and a lecturer NOT assigned to the subject is denied (SEC-03 per-subject ownership)"
    - "The schema test asserts the new table set (sections, subject_user, enrollments, exam_section) exists with the enrollments unique(section_id,user_id) index, and that classroom_id is gone from users (DEL-03)"
    - "Every new/rewritten test in this plan is RED (fails or errors on missing Section/Enrollment/EnrollmentStatus symbols and the old schema) until the schema slice lands in 07-03/07-04"
  artifacts:
    - path: "tests/Feature/Student/ExamVisibilityRegressionTest.php"
      provides: "ENR-08 list-vs-gate agreement across 4 enrollment states (hard gate)"
      min_lines: 40
    - path: "tests/Feature/Lecturer/SectionControllerTest.php"
      provides: "SEC-01/SEC-02 section CRUD + computed name coverage"
      min_lines: 40
    - path: "tests/Feature/Lecturer/SubjectLecturerTest.php"
      provides: "SEC-03 subject↔lecturer assignment + per-subject ownership coverage"
      min_lines: 30
    - path: "tests/Feature/DomainSchemaTest.php"
      provides: "New table set + enrollments unique index + role-without-classroom_id assertions"
      contains: "enrollments"
    - path: "tests/Feature/DatabaseSeederTest.php"
      provides: "DEL-03 reseed assertions against the section/enrollment demo graph"
      contains: "sections"
  key_links:
    - from: "tests/Feature/Student/ExamVisibilityRegressionTest.php"
      to: "Exam::visibleTo + ExamPolicy::takeable"
      via: "assertSame(listVisible, gateVisible) across a DataProvider of enrollment states"
      pattern: "assertSame\\("
---

<objective>
Author the Wave 0 RED test contract for the atomic schema slice (SEC-01/02/03, ENR-08, DEL-03): three new feature tests and two rewritten ones. These tests are the executable acceptance contract the schema/backend waves (07-03, 07-04, 07-07) must satisfy. They are expected to be RED on completion of this plan (they reference `Section`/`Enrollment`/`EnrollmentStatus` and the new table set, none of which exist yet).

Purpose: Lock the ENR-08 list-vs-gate agreement as a hard, table-driven regression gate — per STATE.md's blocker and 07-VALIDATION — before any implementation, and pin the section/subject-lecturer CRUD behaviour and the reseeded-schema shape so downstream waves cannot silently diverge.

Output: Five RED test files that turn green as the schema slice lands. No production code is written in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-CONTEXT.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-RESEARCH.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-PATTERNS.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-VALIDATION.md
@tests/Feature/DomainSchemaTest.php
@tests/Feature/Lecturer/ClassroomControllerTest.php
@tests/Feature/Student/ExamAccessTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENR-08 cross-consumer regression test (hard gate) + section CRUD test</name>
  <read_first>
    - 07-RESEARCH.md "Pattern 1: Exam::scopeVisibleTo() Rewrite" including the "Regression test contract (hard acceptance gate)" DataProvider example — copy it near-verbatim
    - 07-PATTERNS.md "tests/Feature/Student/ExamVisibilityRegressionTest.php" and "tests/Feature/Lecturer/SectionControllerTest.php"
    - tests/Feature/Lecturer/ClassroomControllerTest.php (the CRUD test structure to mirror 1:1, renaming Classroom→Section, nesting section writes under a subject)
    - app/Policies/ExamPolicy.php (takeable signature) and app/Models/Exam.php (visibleTo scope call form)
  </read_first>
  <files>tests/Feature/Student/ExamVisibilityRegressionTest.php, tests/Feature/Lecturer/SectionControllerTest.php</files>
  <action>
    Create tests/Feature/Student/ExamVisibilityRegressionTest.php using `Tests\TestCase` + `RefreshDatabase` and a PHPUnit `#[DataProvider('enrollmentStates')]`. Copy the hard-gate contract from 07-RESEARCH Pattern 1: for each of the four states (enrolled→true, withdrawn→false, rejected→false, never_applied→false) build a `Section::factory()->create()`, a `Exam::factory()->published()->create()` synced to that section via `$exam->sections()->sync([...])`, and a `User::factory()->student()->create()`; attach the student to the section's enrollments with the given status (skip attach for never_applied). Assert `Exam::visibleTo($student)->whereKey($exam->id)->exists()` equals the expected visibility AND `assertSame($listVisible, $gateVisible)` where the gate is `app(ExamPolicy::class)->takeable($student, $exam)` — "List and gate must always agree." This is the phase's hard acceptance gate.
    Create tests/Feature/Lecturer/SectionControllerTest.php mirroring ClassroomControllerTest 1:1 but for the nested-under-subject section routes (07-04 owns the routes): a lecturer assigned to a subject can POST a section (capacity + opens_at + closes_at) to `lecturer.subjects.sections.store`, PUT/update it, DELETE it; a blank/invalid capacity fails validation; `closes_at` before `opens_at` fails validation; and assert a created section's computed `name` accessor equals `year-semester-sequence` (e.g. `2026-2-1`) and that `sequence` auto-increments per (subject, year, semester). Use `Section::factory()` and `Subject::factory()` and attach the acting lecturer to the subject via `$subject->lecturers()->attach($lecturer)`.
    Do NOT write any production code. These tests are RED until 07-03/07-04.
  </action>
  <verify>
    <automated>php artisan test --filter=ExamVisibilityRegressionTest || true</automated>
  </verify>
  <acceptance_criteria>
    - ExamVisibilityRegressionTest exists with a 4-state DataProvider and an assertSame(list, gate) agreement assertion
    - SectionControllerTest covers create/edit/delete + capacity/window validation + computed name + per-(subject,year,semester) sequence
    - Both files parse and are collected by PHPUnit (RED is expected — they fail/error on missing Section/Enrollment symbols)
  </acceptance_criteria>
  <done>The ENR-08 hard gate and section CRUD contract are pinned as RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-03 subject↔lecturer authorization test + schema/seeder test rewrites</name>
  <read_first>
    - 07-RESEARCH.md "Pattern 2: subject_user" and Assumption A4 (per-subject ownership in authorize(), a divergence from the D-09 return-true convention)
    - 07-PATTERNS.md "tests/Feature/DomainSchemaTest.php (rewrite)" and "tests/Feature/DatabaseSeederTest.php (rewrite)"
    - tests/Feature/DomainSchemaTest.php in full (current table list + composite-unique-index assertion idiom to mirror for enrollments)
    - tests/Feature/DatabaseSeederTest.php in full (current classroom-shaped assertions to replace)
  </read_first>
  <files>tests/Feature/Lecturer/SubjectLecturerTest.php, tests/Feature/DomainSchemaTest.php, tests/Feature/DatabaseSeederTest.php</files>
  <action>
    Create tests/Feature/Lecturer/SubjectLecturerTest.php (`Tests\TestCase` + `RefreshDatabase`): a lecturer can be assigned to a subject (subject_user), a subject can hold multiple lecturers, any assigned lecturer can manage that subject's sections (POST a section succeeds), and a lecturer NOT assigned to the subject is forbidden (assert 403) from creating/editing a section under it and from assigning/unassigning lecturers — the SEC-03 per-subject ownership contract. Assert unassign detaches the subject_user pivot row.
    Rewrite tests/Feature/DomainSchemaTest.php: change the table list to `['sections','subjects','subject_user','enrollments','exams','questions','options','exam_section','attempts','answers']`; change `test_users_table_has_role_and_classroom_id_columns` to assert `users` has `role` and does NOT have `classroom_id` (schema break); add an assertion (mirroring the existing `Schema::getIndexes(...)` composite-unique idiom) that `enrollments` has a unique index on (section_id, user_id).
    Rewrite tests/Feature/DatabaseSeederTest.php's classroom-shaped assertions to the section/enrollment demo graph shape (per 07-PATTERNS): after `db:seed`, assert the seeded demo has at least one `sections` row named in the `year-semester-count` form, at least one `subject_user` row, and at least one `enrollments` row with status enrolled; preserve the existing idempotency assertion (re-seed does not error / does not duplicate). Keep assertions the seeder can actually satisfy once rewritten in 07-07.
    No production code. RED is expected.
  </action>
  <verify>
    <automated>php artisan test --filter=SubjectLecturerTest || true</automated>
  </verify>
  <acceptance_criteria>
    - SubjectLecturerTest asserts multi-lecturer assignment, assigned-lecturer-can-manage, and non-assigned-lecturer-denied (403)
    - DomainSchemaTest asserts the new table set, enrollments unique(section_id,user_id), and role-present/classroom_id-absent on users
    - DatabaseSeederTest asserts the section/subject_user/enrollment demo graph + idempotency
  </acceptance_criteria>
  <done>The SEC-03 ownership contract and the reseeded-schema shape are pinned as RED tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → implementation contract | These RED tests define the acceptance boundary the schema/backend waves must satisfy; a weak test lets an insecure implementation pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Elevation of Privilege (test-coverage gap) | ENR-08 list-vs-gate agreement | mitigate | The regression test asserts `assertSame(listVisible, gateVisible)` across all four enrollment states, so any future divergence between the exam list and the direct-access gate fails the build (the exact IDOR/hidden-but-reachable class this phase exists to prevent) |
| T-07-04 | Elevation of Privilege (test-coverage gap) | SEC-03 per-subject ownership | mitigate | SubjectLecturerTest asserts a non-assigned lecturer receives 403 on section management, pinning the ownership boundary before implementation |
| T-07-05 | Tampering (test-coverage gap) | schema shape after break | mitigate | DomainSchemaTest asserts classroom_id is gone and the enrollments unique(section_id,user_id) constraint exists, so a partial/incorrect schema break fails the build |
</threat_model>

<verification>
- All five files are collected by PHPUnit and fail RED (missing Section/Enrollment/EnrollmentStatus symbols and old schema) — expected pre-implementation.
- `php artisan test --filter=ExamVisibilityRegressionTest` and `--filter=SubjectLecturerTest` run without fatal parse errors (assertion/symbol failures are the expected RED state).
</verification>

<success_criteria>
- ENR-08 hard-gate regression test authored with a 4-state DataProvider asserting list==gate.
- SEC-01/02 section CRUD + computed-name + sequence coverage authored.
- SEC-03 per-subject ownership (403 for non-assigned lecturer) coverage authored.
- DomainSchemaTest + DatabaseSeederTest rewritten to the section/enrollment shape.
- No production code written; all tests RED.
</success_criteria>

## Artifacts this plan produces

- `tests/Feature/Student/ExamVisibilityRegressionTest.php` — new (ENR-08 hard gate, DataProvider `enrollmentStates`).
- `tests/Feature/Lecturer/SectionControllerTest.php` — new (SEC-01/SEC-02).
- `tests/Feature/Lecturer/SubjectLecturerTest.php` — new (SEC-03).
- `tests/Feature/DomainSchemaTest.php` — rewritten table set + enrollments unique-index assertion.
- `tests/Feature/DatabaseSeederTest.php` — rewritten to the section/enrollment demo graph.

<output>
Create `.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-02-SUMMARY.md` when done.
</output>
