---
phase: 07-v2-0-foundation-admin-theme-schema-break-answered-count-fix
plan: 07
type: execute
wave: 5
depends_on: ["07-03", "07-04", "07-05", "07-06"]
files_modified:
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Grading/AttemptGraderTest.php
  - tests/Feature/Lecturer/GradeAnswerTest.php
  - tests/Feature/Lecturer/Phase5ReviewFixesTest.php
  - tests/Feature/Lecturer/ResultTest.php
  - tests/Feature/Lecturer/ExamAssignmentTest.php
  - tests/Feature/Lecturer/ClassroomControllerTest.php
  - tests/Feature/Lecturer/ClassroomRosterTest.php
  - tests/Feature/Lecturer/ClassroomSubjectLinkageTest.php
autonomous: true
requirements: [DEL-03, ENR-08]
must_haves:
  truths:
    - "DatabaseSeeder is rewritten for the section/enrollment model: it seeds subject↔lecturer assignments (subject_user), sections named year-semester-count with open windows, sample enrollments, and exam→section assignment, and stays idempotent (firstOrCreate on natural keys) so re-seeding never errors (DEL-03)"
    - "The seeded demo lets a reviewer log in as an enrolled student and see/take the exam, while a non-enrolled student cannot — the live class-scoping demo, now enrollment-driven (ENR-08)"
    - "Every lecturer/root test that referenced the v1 classroom shape is either swept to the section/enrollment shape or deleted (for tests of dropped features), so the lecturer/grading/assignment suites are green against the v2.0 schema"
    - "DatabaseSeederTest (rewritten in 07-02) passes against the rewritten seeder"
  artifacts:
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Section/enrollment/subject_user demo graph + pre-graded demo attempt"
      contains: "enrollments"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "sections/enrollments/subject_user/exam_section"
      via: "Section::firstOrCreate + enrollments()->syncWithoutDetaching(status) + lecturers()->syncWithoutDetaching + exam->sections()->sync"
      pattern: "enrollments\\(\\)->syncWithoutDetaching"
---

<objective>
Rewrite the demo seeder for the section/enrollment model and sweep the lecturer/root test suite off the dropped v1 classroom shape — deleting tests of removed features and converting the rest to the section+enrollment setup idiom. This is part of the completeness half of the atomic schema slice: `migrate:fresh --seed` must stand up a working v2.0 demo, and the lecturer/grading/assignment tests must pass against the new schema.

Purpose: Deliver DEL-03's reseed and keep the ENR-08 class-scoping demo alive (now enrollment-driven), and remove the last of the classroom references from the lecturer-side tests so the full-suite phase gate (07-08) can pass.

Output: A rewritten idempotent seeder (DatabaseSeederTest green) and a green lecturer/root test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-RESEARCH.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-PATTERNS.md
@database/seeders/DatabaseSeeder.php
@tests/Feature/Lecturer/ClassroomControllerTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DatabaseSeeder for the section/enrollment demo graph</name>
  <read_first>
    - 07-PATTERNS.md "database/seeders/DatabaseSeeder.php (full rewrite)" (the exact firstOrCreate→section/enrollment/subject_user transformation, wasRecentlyCreated guard to preserve, and "seed at least one subject_user row")
    - 07-RESEARCH.md "Seeder & Factory Rewrite" and "State of the Art" (enrollment replaces classroom_id; lecturer no longer directly assigns students)
    - database/seeders/DatabaseSeeder.php in full (current classroom-shaped graph + the seedDemoAttempt idempotency + wasRecentlyCreated pattern to keep)
    - app/Enums/EnrollmentStatus.php (from 07-03)
  </read_first>
  <files>database/seeders/DatabaseSeeder.php</files>
  <action>
    Rewrite DatabaseSeeder.run() to the section/enrollment model while preserving every idempotency idiom (firstOrCreate on natural keys, never updateOrCreate; the `wasRecentlyCreated` guard for question/option seeding stays untouched):
    - Keep the fixed-credential demo accounts: 1 lecturer + 3 students (student/student2/student3, all email_verified_at set) via firstOrCreate on email.
    - Assign the lecturer to a subject: after seeding Mathematics/Science subjects, `$mathematics->lecturers()->syncWithoutDetaching([$lecturer->id])` (subject_user) so SEC-03/SubjectLecturerTest has a live demo row and the section-management ownership check passes for the demo lecturer.
    - Replace the two Classroom rows + classroom_subject sync + classroom_id updates with Sections under Mathematics: create at least two sections via `Section::firstOrCreate(['subject_id'=>$mathematics->id,'year'=>2026,'semester'=>2,'sequence'=>1], ['capacity'=>30,'opens_at'=>now()->subDay(),'closes_at'=>now()->addDays(14)])` (open window) and a second section (sequence 2 or a different term). Name resolves to `2026-2-1` via the accessor.
    - Enrollments: enroll `student` and `student2` into the first Mathematics section via `$section->enrollments()->syncWithoutDetaching([$id => ['status' => EnrollmentStatus::Enrolled]])`; leave `student3` NOT enrolled in that section (the live class-scoping/ENR-08 denial demo). Optionally seed a withdrawn/rejected example for realism.
    - Exam: keep the published Mathematics Midterm (MCQ + open-text) but assign it to the section via `$exam->sections()->sync([$section->id])` (exam_section) instead of `$exam->classrooms()->sync(...)`. Update the seedExam signature/params from Classroom to Section.
    - Keep seedDemoAttempt for `student2` (must be enrolled in the exam's section so it is a valid attempt) — the pre-graded, still-'submitted' demo attempt via AttemptGrader stays as-is except for any classroom coupling.
    - `student` (Demo Student) stays enrolled but un-attempted so a reviewer can take the exam fresh.
    Run `php artisan migrate:fresh --seed` to confirm the demo boots.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed && php artisan test --filter=DatabaseSeederTest</automated>
  </verify>
  <acceptance_criteria>
    - `migrate:fresh --seed` completes clean; re-running the seeder does not error (idempotent)
    - Seeded graph has ≥1 subject_user row, ≥2 sections (year-semester-count names), sample enrollments (enrolled), and the exam assigned to a section
    - student2 is enrolled in the exam's section and has the pre-graded submitted demo attempt; student3 is not enrolled (denial demo)
    - `php artisan test --filter=DatabaseSeederTest` GREEN
  </acceptance_criteria>
  <done>The v2.0 demo seeds cleanly and idempotently with enrollment-driven visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep lecturer/root tests to the section/enrollment shape; delete dropped-feature tests</name>
  <read_first>
    - 07-RESEARCH.md Runtime State Inventory (the enumerated rename-sweep test list) and Validation Architecture (full suite green is the completeness proof)
    - 07-PATTERNS.md test analog notes; app/Enums/EnrollmentStatus.php
    - Each target test file's current classroom-based setup before editing
  </read_first>
  <files>tests/Feature/Grading/AttemptGraderTest.php, tests/Feature/Lecturer/GradeAnswerTest.php, tests/Feature/Lecturer/Phase5ReviewFixesTest.php, tests/Feature/Lecturer/ResultTest.php, tests/Feature/Lecturer/ExamAssignmentTest.php, tests/Feature/Lecturer/ClassroomControllerTest.php, tests/Feature/Lecturer/ClassroomRosterTest.php, tests/Feature/Lecturer/ClassroomSubjectLinkageTest.php</files>
  <action>
    Delete (`git rm`) the tests of removed features, superseded by 07-02's new tests: ClassroomControllerTest.php (→ SectionControllerTest), ClassroomRosterTest.php (roster mechanism deleted, Pitfall 4), ClassroomSubjectLinkageTest.php (classroom_subject pivot dropped — sections now carry a direct subject_id FK; subject_user is covered by SubjectLecturerTest).
    Sweep the remaining lecturer/grading/assignment tests to the section/enrollment setup idiom, applying this canonical transformation wherever a test set up class-scoped visibility:
    - OLD: `Classroom::factory()->create()` + `$exam->classrooms()->sync([$classroom->id])` + `User::factory()->student()->create(['classroom_id'=>$classroom->id])`.
    - NEW: `Section::factory()->create()` + `$exam->sections()->sync([$section->id])` + `User::factory()->student()->create()` then `$section->enrollments()->attach($student->id, ['status'=>EnrollmentStatus::Enrolled->value])`.
    For ExamAssignmentTest: change the assignment payload key `classroom_ids`→`section_ids`, the route/pivot to exam_section, and any `assertDatabaseHas('exam_classroom', ...)` to `exam_section`. For grading/result tests: the student must be actively enrolled in the section the exam is assigned to for visibility-dependent paths; ownership-only result paths (AttemptPolicy::viewResult) are unaffected. Replace `Classroom` imports with `Section` and add `EnrollmentStatus` imports where used.
    Run the swept suites to confirm green.
  </action>
  <verify>
    <automated>php artisan test --filter=Grading --filter=Lecturer</automated>
  </verify>
  <acceptance_criteria>
    - ClassroomControllerTest, ClassroomRosterTest, ClassroomSubjectLinkageTest deleted
    - AttemptGraderTest, GradeAnswerTest, Phase5ReviewFixesTest, Lecturer/ResultTest, ExamAssignmentTest swept to section/enrollment setup with no Classroom/classroom_id/exam_classroom references
    - `php artisan test --filter=Grading --filter=Lecturer` GREEN
  </acceptance_criteria>
  <done>The lecturer/root test suite is green against the v2.0 schema with no classroom vestiges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeder → clean-clone boot | A seeder referencing dropped columns/relations makes `migrate:fresh --seed` fail — the non-booting-clone risk this phase's atomicity exists to prevent |
| test suite → completeness of the rename | Any un-swept test referencing the old schema fails, which is the intended completeness signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Availability / Tampering | DatabaseSeeder against v2.0 schema | mitigate | Full rewrite to section/enrollment/subject_user; verified by `migrate:fresh --seed` + DatabaseSeederTest; idempotent firstOrCreate so re-seed never errors |
| T-07-21 | Elevation of Privilege (demo integrity) | seeded enrollment-driven visibility | mitigate | Seeds student3 un-enrolled so the class-scoping denial is demonstrably live (enrollment-driven ENR-08), and student2 enrolled+attempted so grading has content |
| T-07-22 | Tampering (test-coverage gap) | un-swept lecturer tests | mitigate | Dropped-feature tests deleted; remaining tests swept to the enrollment idiom; green suites confirm no stale classroom coupling |
</threat_model>

<verification>
- `php artisan migrate:fresh --seed` clean; `php artisan test --filter=DatabaseSeederTest` GREEN.
- `php artisan test --filter=Grading --filter=Lecturer` GREEN.
- (Student-side sweep + full-suite phase gate is 07-08.)
</verification>

<success_criteria>
- DatabaseSeeder rewritten to the section/enrollment demo graph, idempotent, boots via migrate:fresh --seed (DEL-03).
- Enrollment-driven class-scoping demo preserved (ENR-08).
- Lecturer/root tests swept or deleted; those suites green.
</success_criteria>

## Artifacts this plan produces

- `database/seeders/DatabaseSeeder.php` — section/enrollment/subject_user demo graph + preserved pre-graded demo attempt.
- Swept tests: `Grading/AttemptGraderTest`, `Lecturer/GradeAnswerTest`, `Lecturer/Phase5ReviewFixesTest`, `Lecturer/ResultTest`, `Lecturer/ExamAssignmentTest`.
- Removed: `Lecturer/ClassroomControllerTest`, `Lecturer/ClassroomRosterTest`, `Lecturer/ClassroomSubjectLinkageTest`.

<output>
Create `.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-07-SUMMARY.md` when done.
</output>
