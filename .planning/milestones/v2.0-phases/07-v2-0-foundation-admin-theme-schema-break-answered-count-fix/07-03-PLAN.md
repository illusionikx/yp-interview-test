---
phase: 07-v2-0-foundation-admin-theme-schema-break-answered-count-fix
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - database/migrations/2026_07_15_100001_create_subjects_table.php
  - database/migrations/2026_07_15_100002_create_sections_table.php
  - database/migrations/2026_07_15_100003_add_role_to_users_table.php
  - database/migrations/2026_07_15_100004_create_subject_user_table.php
  - database/migrations/2026_07_15_100008_create_exam_section_table.php
  - database/migrations/2026_07_15_100011_create_enrollments_table.php
  - app/Enums/EnrollmentStatus.php
  - app/Models/Enrollment.php
  - app/Models/Section.php
  - app/Models/Classroom.php
  - app/Models/Subject.php
  - app/Models/User.php
  - app/Models/Exam.php
  - app/Policies/ExamPolicy.php
  - app/Policies/AttemptPolicy.php
  - database/factories/SectionFactory.php
  - database/factories/ClassroomFactory.php
autonomous: true
requirements: [SEC-01, SEC-03, ENR-08, DEL-03]
must_haves:
  truths:
    - "migrate:fresh runs clean: subjects is created before sections so the sections.subject_id foreign key resolves (the Pitfall-1 ordering fix)"
    - "The users table no longer has classroom_id; sections/subject_user/enrollments/exam_section tables exist with correct FKs and the enrollments unique(section_id,user_id) constraint"
    - "Exam::scopeVisibleTo() derives visibility from active enrollment (whereHas sections.enrollments status=Enrolled) and is the ONE predicate both the list and ExamPolicy/AttemptPolicy consume — no visibility logic re-derived anywhere else"
    - "The ENR-08 cross-consumer regression test (list==gate across all four enrollment states) passes — the phase's hard acceptance gate for this wave"
    - "Section.name is a computed accessor (year-semester-sequence), not a stored column; enrollments.status is enum-cast via a custom Enrollment Pivot using ->using()"
  artifacts:
    - path: "database/migrations/2026_07_15_100002_create_sections_table.php"
      provides: "sections table with subject_id FK, year/semester/sequence, capacity, opens_at/closes_at, unique(subject_id,year,semester,sequence)"
      contains: "subject_id"
    - path: "database/migrations/2026_07_15_100011_create_enrollments_table.php"
      provides: "enrollments pivot: section_id, user_id, status, rejection_reason, unique(section_id,user_id)"
      contains: "enrollments"
    - path: "app/Enums/EnrollmentStatus.php"
      provides: "backed enum Enrolled/Withdrawn/Rejected"
      contains: "enum EnrollmentStatus"
    - path: "app/Models/Enrollment.php"
      provides: "custom Pivot with status enum cast"
      contains: "extends Pivot"
    - path: "app/Models/Section.php"
      provides: "Section model (subject belongsTo, enrollments belongsToMany using Enrollment, exams belongsToMany exam_section, computed name)"
      contains: "class Section"
    - path: "app/Models/Exam.php"
      provides: "rewritten scopeVisibleTo (enrollment-driven) + sections() relation"
      contains: "sections.enrollments"
  key_links:
    - from: "app/Models/Exam.php scopeVisibleTo"
      to: "enrollments.status"
      via: "whereHas('sections.enrollments', user_id=X AND status=Enrolled)"
      pattern: "sections\\.enrollments"
    - from: "app/Policies/ExamPolicy.php + app/Policies/AttemptPolicy.php"
      to: "Exam::visibleTo()"
      via: "both delegate to the single shared scope; no classroom_id logic remains"
      pattern: "visibleTo\\("
    - from: "app/Models/Section.php enrollments()"
      to: "app/Models/Enrollment.php"
      via: "->using(Enrollment::class)->withPivot(status, rejection_reason)"
      pattern: "using\\(Enrollment::class\\)"
---

<objective>
Land the database + model half of the atomic schema break: reorder and rewrite the in-place migrations (subjects before sections, sections with a subject_id FK, drop users.classroom_id, repurpose the pivots, add enrollments), introduce the EnrollmentStatus enum and the custom Enrollment Pivot, rename Classroom→Section and rewire Subject/User/Exam, rewrite Exam::scopeVisibleTo() to be enrollment-driven, and rename ClassroomFactory→SectionFactory. The hard acceptance gate for this wave is the ENR-08 cross-consumer regression test going green.

Purpose: This is the single highest-blast-radius change of the phase and the core of the "must land together" atomic slice. It converts the v1 single-classroom model into subject-scoped, enrollment-gated sections while preserving the one-predicate-two-consumers visibility invariant.

Output: A booting schema + model layer where DomainSchemaTest and the ENR-08 regression test pass. Controllers/routes/views/seeder are swept in later waves (07-04, 07-05, 07-06, 07-07); the app is not required to fully boot every page until those land, but `migrate:fresh` and the model-level tests must be green here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-RESEARCH.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-PATTERNS.md
@app/Models/Classroom.php
@app/Models/Subject.php
@app/Models/User.php
@app/Models/Exam.php
@app/Policies/ExamPolicy.php
@app/Policies/AttemptPolicy.php
@app/Enums/Role.php
@database/migrations/2026_07_15_100001_create_classrooms_table.php
@database/migrations/2026_07_15_100002_create_subjects_table.php
@database/migrations/2026_07_15_100003_add_role_and_classroom_id_to_users_table.php
@database/migrations/2026_07_15_100004_create_classroom_subject_table.php
@database/migrations/2026_07_15_100008_create_exam_classroom_table.php
@database/factories/ClassroomFactory.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder and rewrite the schema-break migrations</name>
  <read_first>
    - 07-RESEARCH.md "Pitfall 1: Migration ordering" (the exact file-rename map — subjects must precede sections) and the "Schema Break" file list
    - 07-PATTERNS.md "Migration structure" shared pattern (Schema::create + FK + composite unique)
    - All five current migrations in the context block (their exact up()/down() bodies)
  </read_first>
  <files>database/migrations/2026_07_15_100001_create_subjects_table.php, database/migrations/2026_07_15_100002_create_sections_table.php, database/migrations/2026_07_15_100003_add_role_to_users_table.php, database/migrations/2026_07_15_100004_create_subject_user_table.php, database/migrations/2026_07_15_100008_create_exam_section_table.php, database/migrations/2026_07_15_100011_create_enrollments_table.php</files>
  <action>
    Perform the file renames with `git mv` first (preserving history), then edit the contents:
    (1) `git mv` 2026_07_15_100002_create_subjects_table.php → 2026_07_15_100001_create_subjects_table.php (content unchanged — subjects must be created first so sections.subject_id resolves, Pitfall 1).
    (2) `git mv` 2026_07_15_100001_create_classrooms_table.php → 2026_07_15_100002_create_sections_table.php, then rewrite it to `Schema::create('sections', ...)` with: id; `foreignId('subject_id')->constrained()->cascadeOnDelete()`; `unsignedSmallInteger('year')`; `unsignedTinyInteger('semester')`; `unsignedInteger('sequence')`; `unsignedInteger('capacity')`; `dateTime('opens_at')`; `dateTime('closes_at')`; timestamps; `unique(['subject_id','year','semester','sequence'])`. down() drops `sections`.
    (3) `git mv` 2026_07_15_100003_add_role_and_classroom_id_to_users_table.php → 2026_07_15_100003_add_role_to_users_table.php, then edit up() to add ONLY `role` (drop the classroom_id column + its FK entirely); edit down() to drop only `role`.
    (4) `git mv` 2026_07_15_100004_create_classroom_subject_table.php → 2026_07_15_100004_create_subject_user_table.php, then rewrite to `Schema::create('subject_user', ...)`: id; `foreignId('subject_id')->constrained()->cascadeOnDelete()`; `foreignId('user_id')->constrained()->cascadeOnDelete()`; timestamps; `unique(['subject_id','user_id'])`. (Cascade per REQUIREMENTS resolved-decision #8.)
    (5) `git mv` 2026_07_15_100008_create_exam_classroom_table.php → 2026_07_15_100008_create_exam_section_table.php, then rewrite to `Schema::create('exam_section', ...)`: id; `foreignId('exam_id')->constrained()->cascadeOnDelete()`; `foreignId('section_id')->constrained()->cascadeOnDelete()`; timestamps; `unique(['exam_id','section_id'])`.
    (6) Create NEW 2026_07_15_100011_create_enrollments_table.php: `Schema::create('enrollments', ...)`: id; `foreignId('section_id')->constrained()->cascadeOnDelete()`; `foreignId('user_id')->constrained()->cascadeOnDelete()`; `string('status')->default('enrolled')`; `string('rejection_reason')->nullable()`; timestamps; `unique(['section_id','user_id'])`.
    Leave 100005/100006/100007 (exams/questions/options) and 100009/100010 (attempts/answers) untouched.
    After edits, run `php artisan migrate:fresh` (no seed yet — the seeder is rewritten in 07-07) to confirm the ordering resolves and all FKs build.
  </action>
  <verify>
    <automated>php artisan migrate:fresh</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh` completes with no FK-ordering error
    - subjects migration timestamp precedes sections migration timestamp
    - users migration adds role and NOT classroom_id
    - enrollments table has unique(section_id,user_id); exam_section and subject_user exist with cascade FKs
  </acceptance_criteria>
  <done>The reordered/rewritten migration set builds the v2.0 schema cleanly on a fresh database.</done>
</task>

<task type="auto">
  <name>Task 2: EnrollmentStatus enum, Enrollment pivot, Section model + SectionFactory</name>
  <read_first>
    - 07-RESEARCH.md "Code Examples: Enrollment Custom Pivot Model" and "Sequence Assignment for New Sections"
    - 07-PATTERNS.md "app/Models/Section.php" (target shape) and "Enum-cast Pivot model" shared pattern
    - app/Enums/Role.php (backed-enum style to mirror) and app/Models/Classroom.php (rename source)
    - database/factories/ClassroomFactory.php (rename source)
  </read_first>
  <files>app/Enums/EnrollmentStatus.php, app/Models/Enrollment.php, app/Models/Section.php, app/Models/Classroom.php, database/factories/SectionFactory.php, database/factories/ClassroomFactory.php</files>
  <action>
    Create app/Enums/EnrollmentStatus.php: backed string enum mirroring Role — cases `Enrolled = 'enrolled'`, `Withdrawn = 'withdrawn'`, `Rejected = 'rejected'`.
    Create app/Models/Enrollment.php: `class Enrollment extends Illuminate\Database\Eloquent\Relations\Pivot` with `protected $table = 'enrollments';` and `casts(): array` returning `['status' => EnrollmentStatus::class]` (Laravel 11 casts() method style, not the $casts property).
    `git mv` app/Models/Classroom.php → app/Models/Section.php and rewrite as `class Section`: `use HasFactory`; `$fillable = ['subject_id','year','semester','sequence','capacity','opens_at','closes_at']`; `casts()` returning `['opens_at' => 'datetime','closes_at' => 'datetime']`; `subject(): BelongsTo` → Subject; `enrollments(): BelongsToMany` → User via 'enrollments' `->using(Enrollment::class)->withPivot(['status','rejection_reason'])->withTimestamps()`; `exams(): BelongsToMany` → Exam via 'exam_section'; and a computed `name(): Attribute` accessor returning `"{$this->year}-{$this->semester}-{$this->sequence}"`. Remove the old `users(): HasMany` and `subjects(): BelongsToMany` relations (no longer valid). Delete app/Models/Classroom.php via the git mv (no Classroom class remains).
    `git mv` database/factories/ClassroomFactory.php → database/factories/SectionFactory.php and rewrite: `@extends Factory<Section>`, default state producing `subject_id => Subject::factory()`, a `year`, `semester` (1 or 2), `sequence` (default 1), `capacity` (e.g. 30), `opens_at => now()->subDay()`, `closes_at => now()->addDays(14)` (an open window by default so ENR/section tests can enroll immediately). Update the docblock generic to Section.
  </action>
  <verify>
    <automated>php artisan test --filter=SectionModelTest || php artisan test --filter=SectionControllerTest || true</automated>
  </verify>
  <acceptance_criteria>
    - EnrollmentStatus enum has the three cases; Enrollment extends Pivot and casts status to the enum
    - Section model has subject/enrollments(using Enrollment)/exams relations and a computed name accessor; no Classroom class remains
    - SectionFactory produces a valid, open-window section bound to a subject
  </acceptance_criteria>
  <done>The section/enrollment model layer exists with enum-cast pivot state and a computed section name.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire Subject/User/Exam, rewrite scopeVisibleTo, sweep policy comments</name>
  <read_first>
    - 07-RESEARCH.md "Pattern 1" (the exact BEFORE/AFTER scopeVisibleTo) and "Pitfall 3" (rename exam_classroom→exam_section in the model, not just the migration)
    - 07-PATTERNS.md "app/Models/Exam.php — scopeVisibleTo() rewrite" and "Pattern 2: subject_user"
    - app/Models/Subject.php, app/Models/User.php, app/Models/Exam.php, app/Policies/ExamPolicy.php, app/Policies/AttemptPolicy.php (all current, in context)
  </read_first>
  <files>app/Models/Subject.php, app/Models/User.php, app/Models/Exam.php, app/Policies/ExamPolicy.php, app/Policies/AttemptPolicy.php</files>
  <action>
    Subject.php: remove `classrooms(): BelongsToMany`; add `sections(): HasMany` → Section; add `lecturers(): BelongsToMany` → User via 'subject_user'. Keep `exams(): HasMany`.
    User.php: remove `classroom_id` from `$fillable`; remove the `classroom(): BelongsTo` relation. Add `sections(): BelongsToMany` → Section via 'enrollments' `->using(Enrollment::class)->withPivot(['status','rejection_reason'])->withTimestamps()` (the student's enrolled sections) and `subjects(): BelongsToMany` → Subject via 'subject_user' (the lecturer's assigned subjects). Update the mass-assignment docblock to drop the classroom_id note; keep `role` fillable as the server-controlled carve-out.
    Exam.php: rename `classrooms(): BelongsToMany` → `sections(): BelongsToMany` targeting 'exam_section'. Rewrite `scopeVisibleTo(Builder $query, User $user)` to: `->where('is_published', true)->whereHas('sections.enrollments', fn ($q) => $q->where('user_id', $user->id)->where('status', EnrollmentStatus::Enrolled))`. Update the scope's doc comment to describe the enrollment-driven predicate and to state it is the single predicate consumed by BOTH Student\ExamController@index and ExamPolicy/AttemptPolicy (drop the classroom_id/->when null-guard prose — a student with zero enrollments naturally matches zero rows).
    ExamPolicy.php + AttemptPolicy.php: NO logic change — both still delegate to `Exam::visibleTo()`. Sweep only their doc comments so no stale classroom_id wording remains (replace with enrollment wording).
    Run the ENR-08 hard-gate regression test — it must pass now.
  </action>
  <verify>
    <automated>php artisan test --filter=ExamVisibilityRegressionTest</automated>
  </verify>
  <acceptance_criteria>
    - Exam::scopeVisibleTo uses `sections.enrollments` + status=Enrolled and no classroom_id/->when guard remains
    - Subject has sections()+lecturers(); User has sections()+subjects() and no classroom()/classroom_id fillable
    - ExamPolicy/AttemptPolicy unchanged in logic, comments swept
    - `php artisan test --filter=ExamVisibilityRegressionTest` is GREEN across all four enrollment states (hard gate)
    - `php artisan test --filter=DomainSchemaTest` is GREEN
  </acceptance_criteria>
  <done>Visibility is enrollment-driven through one shared predicate, verified by the ENR-08 hard gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student request → exam list / direct-access gate | Exam visibility is decided by one predicate; divergence between list and gate is the IDOR/hidden-but-reachable risk (ENR-08) |
| request-sourced input → User mass assignment | Dropping classroom_id and adding pivots must not open a mass-assignment path on role/enrollment |
| enrollment status string → authorization decision | status must be a validated enum, not an arbitrary string, so only Enrolled grants visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Elevation of Privilege / Information Disclosure | Exam::scopeVisibleTo divergence | mitigate | Single shared scope consumed identically by the list and ExamPolicy/AttemptPolicy; ENR-08 regression test asserts list==gate across all four states as a hard gate before this wave completes |
| T-07-07 | Elevation of Privilege | enrollments.status semantics | mitigate | status is enum-cast (EnrollmentStatus) via the custom Enrollment Pivot; only `Enrolled` satisfies the visibility predicate, so withdrawn/rejected/absent enrollments resolve to zero rows |
| T-07-08 | Tampering (mass assignment) | User $fillable | mitigate | classroom_id removed from fillable; role stays a server-controlled carve-out (registration hardcodes it, per Phase 1 decision); no request path builds enrollment/subject_user rows in this wave |
| T-07-09 | Information Disclosure | Section.name drift | accept | name is a computed accessor over year/semester/sequence — no stored redundant column to drift or be tampered independently |
</threat_model>

<verification>
- `php artisan migrate:fresh` builds the full v2.0 schema with correct ordering and FKs.
- `php artisan test --filter=DomainSchemaTest` GREEN (new table set + enrollments unique index + classroom_id absent).
- `php artisan test --filter=ExamVisibilityRegressionTest` GREEN across all four enrollment states (ENR-08 hard gate).
- Note: controller/route/view/seeder tests remain RED until 07-04/07-05/07-06/07-07 — expected within the atomic slice.
</verification>

<success_criteria>
- Migrations reordered/rewritten; migrate:fresh clean; classroom_id dropped.
- EnrollmentStatus enum + Enrollment pivot + Section model + SectionFactory in place.
- Subject/User/Exam rewired; scopeVisibleTo enrollment-driven through one predicate.
- ENR-08 hard-gate regression test GREEN.
</success_criteria>

## Artifacts this plan produces

- Migrations: `..._create_subjects_table` (renumbered 100001), `..._create_sections_table` (100002), `..._add_role_to_users_table` (100003), `..._create_subject_user_table` (100004), `..._create_exam_section_table` (100008), `..._create_enrollments_table` (100011, new).
- `app/Enums/EnrollmentStatus.php` — enum (Enrolled/Withdrawn/Rejected).
- `app/Models/Enrollment.php` — custom Pivot (status enum cast).
- `app/Models/Section.php` — renamed from Classroom; subject/enrollments/exams relations + computed `name`.
- `app/Models/Subject.php` — `sections()`, `lecturers()`.
- `app/Models/User.php` — `sections()`, `subjects()`; classroom_id/classroom() removed.
- `app/Models/Exam.php` — `sections()`; rewritten `scopeVisibleTo()`.
- `database/factories/SectionFactory.php` — renamed from ClassroomFactory.
- Removed: `app/Models/Classroom.php`, `database/factories/ClassroomFactory.php`, `classroom_subject`/`exam_classroom`/`classrooms` tables, `users.classroom_id`.

<output>
Create `.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-03-SUMMARY.md` when done.
</output>
