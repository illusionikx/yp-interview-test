---
phase: 07-v2-0-foundation-admin-theme-schema-break-answered-count-fix
plan: 08
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - tests/Feature/Student/AttemptAnswerTest.php
  - tests/Feature/Student/AttemptPolicyTest.php
  - tests/Feature/Student/AttemptShowTest.php
  - tests/Feature/Student/AttemptStartTest.php
  - tests/Feature/Student/AttemptSubmitTest.php
  - tests/Feature/Student/ExamAccessTest.php
  - tests/Feature/Student/ExamIndexTest.php
  - tests/Feature/Student/Phase4ReviewFixesTest.php
  - tests/Feature/Student/ResultTest.php
autonomous: true
requirements: [ENR-08, DEL-03]
must_haves:
  truths:
    - "Every student-side test that referenced the v1 classroom shape is swept to the section/enrollment setup idiom, so the student attempt/access/result suites are green against the v2.0 schema"
    - "The full automated test suite passes end to end (php artisan test, 0 failures) — the completeness proof that all ~26 rename-swept files were updated correctly"
    - "A clean `php artisan migrate:fresh --seed` from an empty database succeeds and the ENR-08 cross-consumer regression test is green — the phase's hard atomic acceptance gate"
  artifacts:
    - path: "tests/Feature/Student/ExamAccessTest.php"
      provides: "Swept IDOR/access matrix on the enrollment-driven visibility"
      contains: "enrollments"
  key_links:
    - from: "tests/Feature/Student/*"
      to: "Section + enrollments(status=Enrolled) + exam_section"
      via: "canonical setup transformation (enroll student in the exam's section)"
      pattern: "enrollments\\(\\)->attach"
---

<objective>
Sweep the student-side test suite off the v1 classroom shape to the section/enrollment idiom, then run the phase's hard atomic acceptance gate: the entire automated suite green plus a clean `migrate:fresh --seed` from an empty database, with the ENR-08 list-vs-gate regression confirmed. This closes the atomic schema slice — proving every rename-swept file across migrations, models, controllers, routes, views, seeder, and tests landed correctly and the app boots from a clean clone.

Purpose: Deliver the completeness guarantee STATE.md's blocker and 07-VALIDATION demand — the full suite is the actual proof of the 26-file sweep, and the reseed is the actual proof the schema break is coherent.

Output: A green full suite, a booting seeded demo, and a verified ENR-08 gate — the phase is ready for verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-RESEARCH.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-VALIDATION.md
@.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-PATTERNS.md
@tests/Feature/Student/ExamAccessTest.php
@tests/Feature/Student/ExamIndexTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep student-side tests to the section/enrollment shape</name>
  <read_first>
    - 07-RESEARCH.md Runtime State Inventory (the student test files in the sweep list) and "Pattern 1" regression contract (the enrollment states)
    - app/Enums/EnrollmentStatus.php, app/Models/Section.php (from 07-03)
    - Each target student test's current classroom-based setup before editing
  </read_first>
  <files>tests/Feature/Student/AttemptAnswerTest.php, tests/Feature/Student/AttemptPolicyTest.php, tests/Feature/Student/AttemptShowTest.php, tests/Feature/Student/AttemptStartTest.php, tests/Feature/Student/AttemptSubmitTest.php, tests/Feature/Student/ExamAccessTest.php, tests/Feature/Student/ExamIndexTest.php, tests/Feature/Student/Phase4ReviewFixesTest.php, tests/Feature/Student/ResultTest.php</files>
  <action>
    Apply the canonical setup transformation to every student test that established class-scoped visibility:
    - OLD: `Classroom::factory()->create()` + `$exam->classrooms()->sync([$classroom->id])` + `User::factory()->student()->create(['classroom_id'=>$classroom->id])`.
    - NEW: `Section::factory()->create()` + `$exam->sections()->sync([$section->id])` + `User::factory()->student()->create()` then `$section->enrollments()->attach($student->id, ['status'=>EnrollmentStatus::Enrolled->value])`.
    Replace `use App\Models\Classroom;` with `use App\Models\Section;` and add `use App\Enums\EnrollmentStatus;` where needed. For ExamAccessTest (the RBAC-05/IDOR matrix): the "not visible" cases now mean "not enrolled / withdrawn / rejected" rather than "wrong classroom" — set the student up with no enrollment (or a withdrawn/rejected enrollment) for the denial cases, keeping the assertion that a direct URL to a non-visible exam is denied server-side. For ExamIndexTest: an enrolled student sees the assigned exam; a non-enrolled student sees nothing. AttemptShowTest already received a FIX-01 assertion in 07-01 — sweep its (and every method's) classroom setup to enrollment here.
    Do not change production code. After sweeping, run the student suite.
  </action>
  <verify>
    <automated>php artisan test --filter=Student</automated>
  </verify>
  <acceptance_criteria>
    - No student test references Classroom, classroom_id, ->classrooms(), or exam_classroom
    - Denial cases in ExamAccessTest/ExamIndexTest use non-enrolled/withdrawn/rejected enrollment states and still assert server-side denial
    - `php artisan test --filter=Student` GREEN
  </acceptance_criteria>
  <done>The student test suite is green against the enrollment-driven schema.</done>
</task>

<task type="auto">
  <name>Task 2: Phase acceptance gate — full suite + clean reseed + ENR-08 confirmation</name>
  <read_first>
    - 07-VALIDATION.md "Sampling Rate" (per-wave full suite; phase gate = full suite green + manual migrate:fresh --seed) and "Wave 0 Requirements"
    - 07-RESEARCH.md "Validation Architecture" (full suite is the completeness proof of the 26-file sweep; use artisan, not the mysql CLI)
  </read_first>
  <files>tests/Feature/Student/ExamAccessTest.php</files>
  <action>
    Run the atomic phase acceptance gate (no code changes unless a gate surfaces a defect — if it does, fix the offending file and re-run):
    (1) `php artisan migrate:fresh --seed` against the yp-student-exam database — must complete clean (proves the schema break + seeder are coherent for a clean clone).
    (2) `php artisan test` (the FULL suite) — must be green with 0 failures (the actual proof that every one of the ~26 rename-swept files across migrations/models/controllers/routes/views/seeder/tests was updated correctly).
    (3) Confirm the ENR-08 hard gate specifically: `php artisan test --filter=ExamVisibilityRegressionTest` green across all four enrollment states (list == gate).
    If the full suite reveals any residual classroom reference or render error, treat it as an in-scope completeness defect: fix the specific file (view/test/controller) and re-run until green — this is the atomic-slice closure task, so the phase does not complete with a red suite or a failing reseed.
    Record in the SUMMARY the final `php artisan test` tally and confirmation that `migrate:fresh --seed` booted cleanly. Note: the `flowbite` npm dependency and Vite build (`npm run build`) were validated in 07-01; a final `npm run build` is an optional belt-and-suspenders check, not a blocker for the PHP gate.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh --seed` completes clean from an empty database
    - `php artisan test` reports 0 failures / 0 errors across the whole suite
    - `php artisan test --filter=ExamVisibilityRegressionTest` GREEN (ENR-08 list==gate, all four states)
  </acceptance_criteria>
  <done>The atomic schema slice is proven complete: full suite green, clean reseed, ENR-08 gate confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full suite + reseed → phase completion | The gate is the atomic checkpoint that the schema break shipped coherently; passing it is what makes the clean-clone boot and the list-vs-gate agreement trustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Elevation of Privilege / Information Disclosure | ENR-08 list-vs-gate agreement | mitigate | The phase gate re-runs ExamVisibilityRegressionTest (all four enrollment states) so the single-predicate invariant is confirmed at phase close, not just when it was written |
| T-07-24 | Availability / Tampering | clean-clone boot after schema break | mitigate | `migrate:fresh --seed` from empty + full suite green is a hard completion gate; a residual classroom reference or a non-booting seeder fails the gate and blocks phase close |
| T-07-25 | Tampering (incomplete sweep) | student-side tests | mitigate | Student suite swept to enrollment idiom and run green; combined with 07-07's lecturer sweep, the full-suite green proves the whole 26-file sweep is complete |
</threat_model>

<verification>
- `php artisan test --filter=Student` GREEN.
- `php artisan migrate:fresh --seed` clean from empty; `php artisan test` full suite 0 failures.
- `php artisan test --filter=ExamVisibilityRegressionTest` GREEN (ENR-08 hard gate).
</verification>

<success_criteria>
- Student test suite swept and green (no classroom vestiges).
- Full automated suite green end to end.
- Clean `migrate:fresh --seed` from an empty database.
- ENR-08 list-vs-gate regression confirmed at phase close.
</success_criteria>

## Artifacts this plan produces

- Swept tests: `Student/AttemptAnswerTest`, `Student/AttemptPolicyTest`, `Student/AttemptShowTest`, `Student/AttemptStartTest`, `Student/AttemptSubmitTest`, `Student/ExamAccessTest`, `Student/ExamIndexTest`, `Student/Phase4ReviewFixesTest`, `Student/ResultTest`.
- Phase acceptance evidence: full-suite pass tally + clean `migrate:fresh --seed` (recorded in the SUMMARY).

<output>
Create `.planning/phases/07-v2-0-foundation-admin-theme-schema-break-answered-count-fix/07-08-SUMMARY.md` when done.
</output>
