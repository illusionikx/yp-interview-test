---
phase: 12-lecturer-workspace-class-management-exam-editor-grading
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/Lecturer/ResultController.php
  - resources/views/lecturer/results/index.blade.php
  - tests/Feature/Lecturer/GradingPageTest.php
autonomous: true
requirements: [GRD-06]

must_haves:
  truths:
    - "The grading page shows the exam's details (name, subject, duration, status)"
    - "The grading page shows grading progress — how many attempts are graded out of those needing grading"
    - "The grading page lists every student who has an attempt on the exam, with status, score, and a grade/view action"
  artifacts:
    - path: "app/Http/Controllers/Lecturer/ResultController.php"
      provides: "index() feeds exam details + a bounded grading-progress aggregate + the student attempt list"
      contains: "graded"
    - path: "resources/views/lecturer/results/index.blade.php"
      provides: "Grading page with an exam-details + progress header above the student list"
  key_links:
    - from: "resources/views/lecturer/results/index.blade.php"
      to: "route lecturer.results.show"
      via: "per-student Grade/View action into the per-attempt grading screen"
      pattern: "results\\.show"
---

<objective>
Turn the lecturer per-exam results index into the GRD-06 grading page: above the existing student list, add a header block showing the exam's details (name, subject, duration, draft/active status) and a grading-progress indicator (graded vs. attempts needing grading), computed as bounded aggregates — never a per-attempt loop. The per-student list already exists (sorted by name, with Grade/View actions into `results.show`); this plan enriches the surrounding context so the page answers "what is this exam, how far is grading, and who still needs it" at a glance.

Purpose: Make the grading entry point self-describing (exam + progress + everyone) as GRD-06 requires, reusing the shipped ResultController/AnswerGradeController grading backend unchanged.
Output: `ResultController::index` gains a bounded progress aggregate; `results/index.blade.php` gains an exam-details + progress header; a new GradingPageTest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-CONTEXT.md

@app/Http/Controllers/Lecturer/ResultController.php
@resources/views/lecturer/results/index.blade.php
@app/Services/AttemptVoider.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exam-details + grading-progress header to the grading page</name>
  <files>app/Http/Controllers/Lecturer/ResultController.php, resources/views/lecturer/results/index.blade.php</files>
  <read_first>
    - app/Http/Controllers/Lecturer/ResultController.php (index() already loads $attempts sorted by user name + $totalPossible; add the progress aggregate here)
    - resources/views/lecturer/results/index.blade.php (existing student table + "Back to exam" link to retarget)
    - app/Services/AttemptVoider.php (summarize() returns inProgress/submittedUngraded/graded/notYetGraded/total from ONE grouped query — reuse it as the bounded aggregate rather than looping attempts)
  </read_first>
  <action>
    In `ResultController::index(Exam $exam)`: keep the existing `$attempts` (name-sorted) and `$totalPossible`. Add a grading-progress aggregate WITHOUT looping attempts — reuse `app(AttemptVoider::class)->summarize($exam)` (one grouped COUNT query) and derive: `gradedCount = summary['graded']`, `needingGrading = summary['submittedUngraded']` (attempts submitted but not yet graded), and a denominator `gradableTotal = summary['graded'] + summary['submittedUngraded']`. Pass `exam` (with its `subject` loaded), `attempts`, `totalPossible`, and a `progress` array (`['graded' => ..., 'needingGrading' => ..., 'gradableTotal' => ...]`) to the view. Do not change `show()` or AnswerGradeController — per-attempt grading is unchanged.

    In `results/index.blade.php`: above the existing attempts table, add a details/progress header card showing the exam's name (`$exam->title`), subject (`$exam->subject->name`), duration, and a draft/active `<x-status-pill>` (mirror the pill already used on exams/index.blade.php); plus a grading-progress line/bar reading "{{ $progress['graded'] }} / {{ $progress['gradableTotal'] }} graded" with a simple Tailwind progress bar (guard divide-by-zero when `gradableTotal` is 0 → show "No submissions to grade yet"). Keep the existing student table exactly (Student / Status / Score / Grade|View action into `results.show`) — it already lists every student with an attempt. Retarget the bottom "Back to exam" link from `route('lecturer.exams.show', $exam)` to the same editor route (still valid) OR to the hub exams tab `route('lecturer.subjects.manage', ['subject' => $exam->subject_id]) . '?tab=exams'` — prefer the hub so the back affordance names where it leads (UX-04). Use `<x-back-button>`.
  </action>
  <acceptance_criteria>
    - `grep -c "summarize" app/Http/Controllers/Lecturer/ResultController.php` >= 1 (bounded aggregate reused; no attempt loop for progress).
    - `grep -c "progress" resources/views/lecturer/results/index.blade.php` >= 1 (progress header present).
    - `grep -c "\$exam->subject" resources/views/lecturer/results/index.blade.php` >= 1 (exam/subject details shown).
    - The grading page renders exam title, subject, a graded/total progress figure, and the full attempt list.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=ResultTest</automated>
  </verify>
  <done>The grading page shows exam details + a graded/needing-grading progress indicator above the full student attempt list; per-attempt grading and totals are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Feature test the grading page details, progress, and full student list</name>
  <files>tests/Feature/Lecturer/GradingPageTest.php</files>
  <read_first>
    - tests/Feature/Lecturer/ResultTest.php (existing index/show conventions, factory setup for attempts with submitted/graded status)
    - database/factories (Attempt factory statuses in_progress/submitted/graded; QuestionFactory essay()/mcq(); AnswerFactory)
  </read_first>
  <action>
    Create `tests/Feature/Lecturer/GradingPageTest.php` (RefreshDatabase) covering GRD-06:
    - Seed an exam with a subject and several attempts across statuses: some `graded`, some `submitted` (needing grading). GET `route('lecturer.results.index', $exam)` as a lecturer → 200.
    - Exam details: response shows the exam title and subject name.
    - Grading progress: response shows the graded count over the gradable total (assert the exact "graded / total" figure via assertViewHas('progress', ...) or assertSee of the computed numbers) — e.g. 1 graded of 3 gradable renders "1 / 3".
    - Full list: every seeded attempt's student name appears, each with a Grade or View action linking to `route('lecturer.results.show', [$exam, $attempt])`.
    - Boundedness: assert the progress numbers are correct without relying on a per-attempt query (assertViewHas the aggregate array).
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=GradingPageTest` passes: 200, exam+subject details, graded/total progress, every student listed, grade/view links present.
    - Full suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=GradingPageTest && php artisan test --filter=ResultTest</automated>
  </verify>
  <done>GradingPageTest proves the grading page shows exam details, grading progress, and lists every student with a grade/view action; ResultTest stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lecturer browser → grading page read | Any lecturer may view any exam's results (Phase 2/3 "any lecturer" precedent, D-06) — no per-lecturer ownership on grading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | `ResultController::index` | accept | `role:lecturer` middleware is the sole gate, matching the shipped grading model (D-06); no student can reach it. This plan adds only aggregate counts already derivable from the same rows. |
| T-12-08 | Tampering | grading writes | accept | No write path added — grade saves remain in the unchanged AnswerGradeController (locked-read + null-guard). |

No package installs in this plan.
</threat_model>

<verification>
- `php artisan test --filter=GradingPageTest` + `--filter=ResultTest` pass.
- Full suite stays green.
- Grading page shows exam details + progress + the full student list; progress is a bounded aggregate.
</verification>

<success_criteria>
GRD-06 satisfied: the grading page shows exam details and grading progress and lists every student with an attempt, reusing the shipped grading backend without changing per-attempt grading.
</success_criteria>

<output>
Create `.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-03-SUMMARY.md` when done.

Artifacts this plan produces:
- `ResultController::index` progress aggregate (reuses AttemptVoider::summarize)
- Grading page header (exam details + progress) on `lecturer.results.index`
- Reached from: 12-04 exams tab (per-exam grading link)
</output>
