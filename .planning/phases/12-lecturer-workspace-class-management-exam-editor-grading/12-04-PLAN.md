---
phase: 12-lecturer-workspace-class-management-exam-editor-grading
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - app/Http/Controllers/Lecturer/SubjectManageController.php
  - resources/views/lecturer/subjects/partials/_exams-tab.blade.php
  - app/Http/Controllers/Lecturer/ExamController.php
  - resources/views/lecturer/exams/create.blade.php
  - tests/Feature/Lecturer/ExamsTabTest.php
  - tests/Feature/Lecturer/ExamControllerTest.php
autonomous: true
requirements: [CLS-04, CLS-08]

must_haves:
  truths:
    - "The Exams tab lists all exams for the subject with create, edit, and delete"
    - "Each exam row shows its grading progress (graded vs. total attempts) as a bounded aggregate"
    - "The Exams tab surfaces the draft/active toggle and reset-submissions action inline per exam, reusing the shipped routes and warning modal"
    - "Creating a new exam is scoped to the subject and returns to the exam editor"
    - "The old unscoped lecturer.exams.index no longer renders a divergent all-exams listing"
  artifacts:
    - path: "resources/views/lecturer/subjects/partials/_exams-tab.blade.php"
      provides: "Subject-scoped exams management: list + CRUD + toggle + reset + grading progress"
      contains: "grading"
    - path: "app/Http/Controllers/Lecturer/SubjectManageController.php"
      provides: "Exams withCount grading-progress aggregate + per-exam reset counts for the tab"
      contains: "withCount"
  key_links:
    - from: "resources/views/lecturer/subjects/partials/_exams-tab.blade.php"
      to: "route lecturer.exams.show"
      via: "per-exam Edit/Open link into the two-tab editor"
      pattern: "exams\\.show"
    - from: "app/Http/Controllers/Lecturer/SubjectManageController.php"
      to: "database (attempts)"
      via: "withCount graded/total per exam for CLS-08 progress"
      pattern: "withCount"
---

<objective>
Fill the Exams tab of the per-subject hub (CLS-04) and show per-exam grading progress (CLS-08). Replace 12-01's stub `_exams-tab` partial with the real management surface: the subject's exams listed with create/edit/delete, the shipped draft/active toggle (CLS-06) and reset-submissions (CLS-07) surfaced inline per row (reusing existing routes + the confirm modal + AttemptVoider — never rebuilt), and a grading-progress indicator per exam computed as a bounded `withCount` aggregate. Close the pre-existing gap where `lecturer.exams.index` listed every exam unscoped by lecturer, by folding the canonical listing into this subject-scoped, ownership-gated tab and redirecting the old index to the lecturer home (mirroring `SubjectController::index`).

Purpose: Make the exams tab the one place a lecturer manages a subject's exams — create, publish, reset, grade-track — with no unscoped listing left behind.
Output: enriched `SubjectManageController` exam data; rewritten `_exams-tab` partial; `ExamController::index` redirect + subject-scoped create; ExamsTabTest; ExamControllerTest index update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-CONTEXT.md

# Wave-1 surfaces this plan extends (read the 12-01 / 12-02 SUMMARYs too)
@app/Http/Controllers/Lecturer/SubjectManageController.php
@resources/views/lecturer/subjects/partials/_exams-tab.blade.php
@app/Http/Controllers/Lecturer/ExamController.php
@resources/views/lecturer/exams/create.blade.php

# Shipped machinery to REUSE (do not rebuild)
@app/Services/AttemptVoider.php
@resources/views/lecturer/exams/show.blade.php
@app/Http/Controllers/Lecturer/SubjectController.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feed exam data (grading progress + reset counts) to the hub and build the exams tab</name>
  <files>app/Http/Controllers/Lecturer/SubjectManageController.php, resources/views/lecturer/subjects/partials/_exams-tab.blade.php</files>
  <read_first>
    - app/Http/Controllers/Lecturer/SubjectManageController.php (the 12-01 show() that already loads $subject + grouped classes + a stub $exams list — replace the stub exams load)
    - app/Services/AttemptVoider.php (summarize() → inProgress/submittedUngraded/graded/notYetGraded/total; reuse for the reset confirm counts, one grouped query per exam)
    - resources/views/lecturer/exams/show.blade.php (the shipped Submissions reset confirm-modal wiring + copy to reuse; the publish/unpublish forms to reuse)
    - resources/views/lecturer/home.blade.php (the <x-confirm-modal> + x-ref delete wiring idiom for the exam delete)
  </read_first>
  <action>
    In `SubjectManageController::show`, replace the stub exam load with a CLS-08 bounded aggregate: `$exams = $subject->exams()->withCount(['attempts', 'attempts as graded_attempts_count' => fn ($q) => $q->where('status', 'graded')])->orderBy('title')->get();` — grading progress per exam is `graded_attempts_count` over `attempts_count`, one query for the whole set, never a per-attempt loop (constraint #6). For the CLS-07 reset confirmation copy, also compute the per-exam attempt breakdown by reusing the shipped service: build `$attemptCountsByExam = $exams->mapWithKeys(fn ($e) => [$e->id => app(\App\Services\AttemptVoider::class)->summarize($e)])` — this is a small, bounded number of grouped COUNT queries (a handful of exams per subject), not a scan of attempt rows, and it reuses the single voiding authority rather than re-deriving counts. Pass `exams` and `attemptCountsByExam` to the view.

    Rewrite `resources/views/lecturer/subjects/partials/_exams-tab.blade.php` (replacing the 12-01 stub) as the exams management surface (CLS-04):
    - A "New exam" button linking to `route('lecturer.exams.create', ['subject' => $subject])` (subject pre-scoped — see Task 2).
    - A table of `$exams`, each row:
      - Title, linking to `route('lecturer.exams.show', $exam)` (the two-tab editor from 12-02).
      - Draft/active `<x-status-pill>` + the inline toggle (CLS-06): the shipped publish/unpublish `<form>` posting to `route('lecturer.exams.publish', $exam)` / `route('lecturer.exams.unpublish', $exam)` (reuse verbatim — do not add new routes or logic).
      - Grading progress (CLS-08): "{{ $exam->graded_attempts_count }} / {{ $exam->attempts_count }} graded" plus a small Tailwind progress bar (guard divide-by-zero; "No attempts yet" when `attempts_count` is 0), and a "Grade" link to `route('lecturer.results.index', $exam)` (the 12-03 grading page).
      - "Edit" → `route('lecturer.exams.show', $exam)`.
      - Reset submissions (CLS-07): reuse `<x-confirm-modal>` opening a per-row modal whose body states the concrete counts from `$attemptCountsByExam[$exam->id]` (INT-02 — state notYetGraded vs graded exactly as the shipped Submissions panel does), posting to `route('lecturer.exams.submissions.reset', $exam)`. When that exam has zero attempts, render the reset control disabled (mirror the show.blade.php `$attemptCounts['total'] === 0` disabled state) rather than an armed modal. Do NOT re-implement AttemptVoider or a second delete path — the route + service are the shipped ones.
      - Delete (draft-only): for `@if (! $exam->is_published)`, a `<x-confirm-modal>` posting to `route('lecturer.exams.destroy', $exam)`.
      - Empty-state row when the subject has no exams.
  </action>
  <acceptance_criteria>
    - `grep -c "withCount" app/Http/Controllers/Lecturer/SubjectManageController.php` >= 1 and `grep -c "graded_attempts_count" app/Http/Controllers/Lecturer/SubjectManageController.php` >= 1 (CLS-08 bounded aggregate).
    - `grep -c "exams.submissions.reset" resources/views/lecturer/subjects/partials/_exams-tab.blade.php` == 1 (CLS-07 reuse).
    - `grep -c "exams.publish\|exams.unpublish" resources/views/lecturer/subjects/partials/_exams-tab.blade.php` >= 1 (CLS-06 toggle reuse).
    - `grep -c "results.index" resources/views/lecturer/subjects/partials/_exams-tab.blade.php` >= 1 (grading link).
    - `grep -c "AttemptVoider" app/Http/Controllers/Lecturer/SubjectManageController.php` >= 1 (reuse, not rebuild).
    - The exams tab renders each exam with grading progress, a working toggle, a reset control gated by attempt count, edit, and draft-only delete.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=SubjectManageTest</automated>
  </verify>
  <done>The Exams tab lists the subject's exams with create/edit/delete, per-exam grading progress (bounded aggregate), and inline draft/active toggle + reset-submissions reusing the shipped routes/service/modal.</done>
</task>

<task type="auto">
  <name>Task 2: Scope exam create to the subject and fold the unscoped exams index</name>
  <files>app/Http/Controllers/Lecturer/ExamController.php, resources/views/lecturer/exams/create.blade.php, tests/Feature/Lecturer/ExamControllerTest.php</files>
  <read_first>
    - app/Http/Controllers/Lecturer/ExamController.php (index() renders an unscoped all-exams table; create() shows a subject dropdown; store() redirects to exams.show)
    - app/Http/Controllers/Lecturer/SubjectController.php (the index()→home redirect precedent to mirror for exams index)
    - resources/views/lecturer/exams/create.blade.php (the subject <select> + back-button to retarget)
  </read_first>
  <action>
    Close the unscoped-listing gap (CONTEXT: `lecturer.exams.index` is unscoped by lecturer). Change `ExamController::index()` to `return redirect()->route('lecturer.home');` — exactly mirroring `SubjectController::index()`. The per-subject hub Exams tab is now the canonical exam listing; the old `exams/index.blade.php` view becomes caller-less (leave it in place, not deleted). A student hitting `exams.index` still gets 403 from `role:lecturer` before the redirect — unchanged.

    Subject-scope exam creation: in `ExamController::create(Request $request)`, read an optional `?subject=` id and pass it as `$selectedSubjectId` alongside `$subjects`. In create.blade.php, pre-select that subject in the `<select>` (`@selected(old('subject_id', $selectedSubjectId) == $subject->id)`) so "New exam" from a subject's Exams tab lands pre-scoped; keep the field editable and keep `store()`'s existing server-side `subject_id` validation. Retarget the create page's "Back to exams" `<x-back-button>` from `route('lecturer.exams.index')` to the hub exams tab when a subject is known (`route('lecturer.subjects.manage', ['subject' => $selectedSubjectId]) . '?tab=exams'`), falling back to `route('lecturer.home')` otherwise. `store()` continues to redirect to `route('lecturer.exams.show', $exam)` (the editor) — no change.

    In ExamControllerTest, update the exam-index assertions: the index now redirects to `lecturer.home` (assertRedirect(route('lecturer.home'))) for a lecturer, and a student still receives 403. Keep the store/destroy assertions consistent with their actual redirects.
  </action>
  <acceptance_criteria>
    - `grep -c "redirect()->route('lecturer.home')" app/Http/Controllers/Lecturer/ExamController.php` >= 1 (index folded).
    - `grep -c "selectedSubjectId\|request('subject')\|\$request->query('subject')" app/Http/Controllers/Lecturer/ExamController.php` >= 1 (create scoping).
    - A lecturer GET `exams.index` → 302 to `lecturer.home`; a student GET `exams.index` → 403.
    - "New exam" from a subject's Exams tab renders the create form with that subject pre-selected; the created exam belongs to that subject and redirects to its editor.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=ExamsTabTest && php artisan test --filter=ExamControllerTest</automated>
  </verify>
  <done>`exams.index` redirects to home (gap closed), exam create is subject-pre-scoped and returns to the editor, and ExamControllerTest reflects the redirect.</done>
</task>

<task type="auto">
  <name>Task 3: Feature tests for the exams tab, grading progress, toggle, reset, and gap closure</name>
  <files>tests/Feature/Lecturer/ExamsTabTest.php</files>
  <read_first>
    - tests/Feature/Lecturer/ResetSubmissionsTest.php (the shipped CLS-07 reset behavior to lean on — do not re-test the delete itself, just that the tab surfaces it)
    - tests/Feature/Lecturer/ExamPublishTest.php (CLS-06 toggle behavior already covered — assert the tab exposes the affordance)
    - database/factories (ExamFactory published()/draft() states if present, Attempt factory statuses, QuestionFactory)
  </read_first>
  <action>
    Create `tests/Feature/Lecturer/ExamsTabTest.php` (RefreshDatabase) covering CLS-04/CLS-08:
    - Seed a subject assigned to a lecturer with 2 exams; GET `route('lecturer.subjects.manage', ['subject' => $subject]) . '?tab=exams'` → 200; both exam titles appear; "New exam" links to `route('lecturer.exams.create', ['subject' => $subject])`.
    - CLS-08: for an exam with A total attempts and G graded, assert the exams tab shows "G / A graded" (assertViewHas exams collection carrying `graded_attempts_count`/`attempts_count`, or assertSee the figure). Assert the counts are correct (e.g. 3 attempts, 1 graded → "1 / 3").
    - CLS-06 surfaced: a published exam row exposes the unpublish form (posts to `exams.unpublish`); a draft row exposes the publish form.
    - CLS-07 surfaced: an exam with attempts exposes a reset control posting to `route('lecturer.exams.submissions.reset', $exam)` whose modal body states the graded/notYetGraded counts; an exam with zero attempts shows the reset control disabled.
    - Grading link: each row links to `route('lecturer.results.index', $exam)`.
    - Gap closure: a lecturer GET `route('lecturer.exams.index')` → 302 to `lecturer.home`.
    - Subject-scoped create: GET `route('lecturer.exams.create', ['subject' => $subject])` pre-selects the subject; POST create with that subject → exam persisted under the subject, redirect to `exams.show`.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=ExamsTabTest` passes: exams listed, grading progress figures, publish/unpublish surfaced, reset surfaced with counts + disabled-when-empty, grading link, index→home redirect, subject-scoped create.
    - Full suite stays green.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=ExamsTabTest</automated>
  </verify>
  <done>ExamsTabTest proves the exams tab lists exams with CRUD, per-exam grading progress, inline toggle + reset (with correct counts), the grading link, and the folded index; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lecturer browser → exams tab (hub) | Rendered by the SEC-03-gated SubjectManageController (12-01) — the exams tab inherits the same per-subject ownership gate. |
| lecturer browser → reset/toggle/create writes | Existing `role:lecturer`-gated exam routes; reset is a permanent hard delete guarded by the shipped INT-02 warning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Data loss | inline reset-submissions | mitigate | Reuse the shipped `<x-confirm-modal>` + `AttemptVoider` — the modal states concrete graded/notYetGraded counts (INT-02) before the permanent delete; reset control disabled when zero attempts. No second delete path. |
| T-12-10 | Elevation of Privilege / IDOR | exams-tab data + create | mitigate | Tab renders only inside SubjectManageController's `abort_unless` ownership gate; create's `subject_id` is server-validated in StoreExamRequest (a forged `?subject=` only pre-selects the dropdown, never bypasses validation). |
| T-12-11 | Repudiation | folded `exams.index` | accept | Index redirects to home; the divergent unscoped listing is removed, reducing (not adding) exposure. |

No package installs in this plan.
</threat_model>

<verification>
- `php artisan test --filter=ExamsTabTest` + `--filter=SubjectManageTest` + `--filter=ExamControllerTest` pass.
- Full suite stays green.
- Exams tab: CRUD + per-exam grading progress (bounded) + inline toggle + reset with counts; `exams.index` → home.
</verification>

<success_criteria>
CLS-04 and CLS-08 satisfied: the Exams tab lists a subject's exams with create/edit/delete, shows per-exam grading progress as a bounded aggregate, and surfaces the shipped draft/active toggle and reset-submissions inline; the unscoped exam listing is folded into the subject hub.
</success_criteria>

<output>
Create `.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-04-SUMMARY.md` when done.

Artifacts this plan produces:
- Full `_exams-tab` partial (CRUD + toggle + reset + CLS-08 progress)
- `SubjectManageController` exam aggregates; `ExamController::index` → home; subject-scoped create
- Depends on: 12-01 (hub + stub partial), 12-02 (editor route), 12-03 (grading link target)
</output>
