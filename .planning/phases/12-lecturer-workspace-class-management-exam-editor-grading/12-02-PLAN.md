---
phase: 12-lecturer-workspace-class-management-exam-editor-grading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/Lecturer/ExamController.php
  - resources/views/lecturer/exams/show.blade.php
  - tests/Feature/Lecturer/ExamControllerTest.php
  - tests/Feature/Lecturer/ExamEditorTest.php
autonomous: true
requirements: [EDT-01, EDT-02]
user_setup: []

must_haves:
  truths:
    - "A lecturer edits one exam's details and its questions on a single page presented as two tabs"
    - "The Details tab carries the exam/test name field and validates it"
    - "Saving details on an already-attempted exam still raises the EDT-04 warn-and-void confirmation first"
    - "The publish/unpublish toggle, View Results, whole-exam delete, and the Submissions (reset) panel remain reachable from the editor"
    - "Questions and their options always render in stored position order"
  artifacts:
    - path: "resources/views/lecturer/exams/show.blade.php"
      provides: "Two-tab exam editor (Details + Questions) replacing the separate edit and question pages"
      contains: "tab"
    - path: "app/Http/Controllers/Lecturer/ExamController.php"
      provides: "show() feeds the editor (subjects + ordered questions + attemptCounts); edit() redirects to the editor"
      contains: "redirect"
  key_links:
    - from: "resources/views/lecturer/exams/show.blade.php"
      to: "route lecturer.exams.update"
      via: "Details-tab form posts exam changes, guarded by the _save-warning-modal"
      pattern: "exams\\.update"
    - from: "resources/views/lecturer/exams/show.blade.php"
      to: "resources/views/lecturer/exams/_save-warning-modal.blade.php"
      via: "@include of the EDT-04 shared warning copy on the Details save"
      pattern: "_save-warning-modal"
---

<objective>
Merge the exam's Details and Questions surfaces into ONE page presented as two Alpine tabs (EDT-02), and surface the exam/test name field on the Details tab (EDT-01). The editor lives at the existing `lecturer.exams.show` route (the exam's canonical page); the old standalone `exams.edit` details form is absorbed into the Details tab and `exams.edit` redirects to it, mirroring Phase 11's `SubjectController::index → home` precedent. This is a reorganization of already-shipped, already-secured surfaces — the EDT-04 warn-and-void modal, publish/unpublish, View Results, whole-exam delete, and the CLS-07 Submissions panel all stay wired and reachable; nothing about attempt-voiding, authorization, or grading changes.

Purpose: Give a lecturer one editor page for an exam instead of three separate pages, without regressing any Phase 10 destructive-warning guarantee or NAV-04 reachability.
Output: `exams.show` rewritten as a two-tab editor; `ExamController::show`/`edit` adjusted; ExamControllerTest updated; a new ExamEditorTest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-CONTEXT.md

# Current surfaces being merged (read all — you are recombining them)
@resources/views/lecturer/exams/show.blade.php
@resources/views/lecturer/exams/edit.blade.php
@resources/views/lecturer/exams/questions/_form.blade.php
@resources/views/lecturer/exams/_save-warning-modal.blade.php
@app/Http/Controllers/Lecturer/ExamController.php
@app/Http/Controllers/Lecturer/ExamQuestionController.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed and render the two-tab exam editor (Details + Questions)</name>
  <files>app/Http/Controllers/Lecturer/ExamController.php, resources/views/lecturer/exams/show.blade.php</files>
  <read_first>
    - resources/views/lecturer/exams/show.blade.php (current management page: details block, questions list, add-question _form, Submissions panel, publish/delete/results actions — you will keep every affordance, just reorganize into tabs)
    - resources/views/lecturer/exams/edit.blade.php (the details form to move onto the Details tab — subject, title, description, duration, availability + _save-warning-modal wiring)
    - resources/views/lecturer/exams/questions/_form.blade.php (already supports add AND edit mode via an optional $question — reuse for inline per-question edit; no change needed here)
    - app/Http/Controllers/Lecturer/ExamController.php (show() already computes $attemptCounts and loads questions.options; edit() loads $subjects + $attemptCounts)
  </read_first>
  <behavior>
    - GET `lecturer.exams.show` renders a single page with a Details tab (default) and a Questions tab.
    - The Details tab shows the exam name/title input (EDT-01), subject, description, duration, and availability, posting to `lecturer.exams.update`.
    - When the exam has attempts, submitting the Details form opens the `save-exam-changes` confirmation (EDT-04) before the write.
    - The Questions tab lists every question in `position` order, each with its options in `position` order, an inline edit form, an inline delete, and the add-question form.
    - GET `lecturer.exams.edit` redirects (302) to `lecturer.exams.show`.
    - Publish/unpublish, View Results, whole-exam Delete (draft-only), and the Submissions reset panel are all present on the editor page.
  </behavior>
  <action>
    In `ExamController::show(Exam $exam)`: keep the existing `$attemptCounts = app(AttemptVoider::class)->summarize($exam)`; additionally load the subject list the Details form needs (`$subjects = Subject::orderBy('name')->get()`) and the ordered questions (`$exam->load(['questions' => fn ($q) => $q->orderBy('position'), 'questions.options' => fn ($q) => $q->orderBy('position'), 'subject'])`). Pass `exam`, `subjects`, `attemptCounts` to the view. In `ExamController::edit(Exam $exam)`: replace the view render with `return redirect()->route('lecturer.exams.show', $exam);` — the standalone details form is gone; its route name stays alive as a redirect (mirrors SubjectController::index → home). Do NOT change `update()`, `destroy()`, `publish()`, `unpublish()`, `resetSubmissions()`, or any Form Request — the EDT-04 warn-and-void machinery is untouched.

    Rewrite `resources/views/lecturer/exams/show.blade.php` as the two-tab editor:
    - Header: exam title + a `<x-back-button :href="route('lecturer.subjects.manage', ['subject' => $exam->subject_id]) . '?tab=exams'">` naming "Back to exams". A header actions row preserving the shipped affordances: publish/unpublish forms, "View Results" link (`lecturer.results.index`), and — for a draft exam only — the whole-exam Delete confirm (keep the exact `<x-confirm-modal name="delete-exam" ...>` + `x-ref="deleteExamForm"` wiring already in the file, including the draft-only condition `@if (! $exam->is_published)` around Delete).
    - Alpine tab scope: `x-data="{ tab: '{{ request('tab', 'details') }}' }"` with two buttons ("Details" default, "Questions").
    - Details tab (`x-show="tab === 'details'"`): the exam-details form moved verbatim from edit.blade.php (subject select, TITLE input labeled clearly as the exam/test name — EDT-01, description, duration, available_from/until), posting to `route('lecturer.exams.update', $exam)`, with the same conditional `x-ref="editExamForm"` + `@submit.prevent="$dispatch('open-modal', 'save-exam-changes')"` guard and the `@include('lecturer.exams._save-warning-modal', ['exam' => $exam, 'attemptCounts' => $attemptCounts, 'formRef' => 'editExamForm'])`. Keep the whole form inside one `x-data` scope so `$refs` resolves. Also keep the CLS-07 Submissions panel (reset submissions) block from the current show.blade.php on this tab or directly beneath the tabs — do not drop it (NAV-04 / CLS-07 reachability).
    - Questions tab (`x-show="tab === 'questions'" x-cloak`): the existing questions list (looping `$exam->questions`, which now arrives position-ordered) with each question's type/points, options list (position-ordered), the existing per-question Delete confirm-modal, and — replacing the separate questions/edit page — an inline Edit for each question that toggles a hidden `@include('lecturer.exams.questions._form', ['exam' => $exam, 'question' => $question, 'attemptCounts' => $attemptCounts])` (edit mode) via a local Alpine boolean; plus the add-question `@include('lecturer.exams.questions._form', ['exam' => $exam, 'attemptCounts' => $attemptCounts])`. All question forms keep their `_save-warning-modal` wiring exactly as `_form` already provides it. Do NOT change any question route.

    The questions/edit.blade.php standalone page and edit.blade.php become caller-less (edit() redirects; question edit is inline). Leave both files in place, caller-less, mirroring Phase 9's retired-but-kept `auth-session-status.blade.php` precedent — do not delete or re-route.
  </action>
  <acceptance_criteria>
    - `grep -c "request('tab', 'details')" resources/views/lecturer/exams/show.blade.php` == 1 (two-tab scope, Details default).
    - `grep -c "_save-warning-modal" resources/views/lecturer/exams/show.blade.php` >= 1 (EDT-04 preserved on Details save).
    - `grep -c "exams.submissions.reset" resources/views/lecturer/exams/show.blade.php` == 1 (CLS-07 panel preserved).
    - `grep -c "results.index" resources/views/lecturer/exams/show.blade.php` == 1 (View Results preserved).
    - `grep -c "redirect()->route('lecturer.exams.show'" app/Http/Controllers/Lecturer/ExamController.php` >= 1 (edit() redirects).
    - GET `exams.edit` returns 302 to `exams.show`; GET `exams.show` renders Details form title input + a Questions tab section.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=ExamUpdateVoidsAttemptsTest</automated>
  </verify>
  <done>The exam page is a single two-tab editor: Details (name + fields + EDT-04 modal) and Questions (position-ordered list + inline edit + add + delete), with publish/results/delete/reset all reachable; `exams.edit` redirects to it; EDT-04 voiding behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Feature tests for the tabbed editor and update ExamControllerTest</name>
  <files>tests/Feature/Lecturer/ExamEditorTest.php, tests/Feature/Lecturer/ExamControllerTest.php</files>
  <read_first>
    - tests/Feature/Lecturer/ExamControllerTest.php (existing assertions around exams.show/exams.edit; the redirect-target assertions that must move to the new behavior)
    - tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php (EDT-04 coverage to keep green — do not duplicate; just confirm it still passes against the merged editor)
    - database/factories (ExamFactory, QuestionFactory mcq()/essay() states, OptionFactory, Attempt factory)
  </read_first>
  <action>
    Create `tests/Feature/Lecturer/ExamEditorTest.php` (RefreshDatabase) covering EDT-01/EDT-02:
    - GET `route('lecturer.exams.show', $exam)` as a lecturer → 200; response contains a Details-tab name/title input and a Questions-tab region; the Alpine seed defaults to the details tab (assertSee the `request('tab', 'details')` output — no `?tab` → details).
    - `?tab=questions` → the seed carries `tab: 'questions'` (deep-link).
    - GET `route('lecturer.exams.edit', $exam)` → assertRedirect to `route('lecturer.exams.show', $exam)`.
    - EDT-01 validation: POST/PUT `lecturer.exams.update` with an empty `title` → 422 / redirected-with-errors on `title`; with a valid title → saved.
    - Position order: seed a question with three MCQ options created out of position order, assert they render in ascending `position` in the response (assert the option bodies appear in position order).
    - Preserved affordances: the editor response contains the publish/unpublish action, `results.index` link, and the Submissions reset form.

    In ExamControllerTest, update any assertion that expected `exams.edit` to render a details form so it now expects the 302 redirect to `exams.show`; keep the store-redirect-to-show and destroy-redirect assertions as they are (store still redirects to `exams.show`).
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=ExamEditorTest` passes: show-200, details-default, questions deep-link, edit-redirects, title-required, option position order, preserved affordances.
    - `php artisan test --filter=ExamControllerTest` passes with the edit()-redirect expectation.
    - `php artisan test --filter=ExamUpdateVoidsAttemptsTest` still passes (EDT-04 intact).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=ExamEditorTest && php artisan test --filter=ExamControllerTest && php artisan test --filter=ExamUpdateVoidsAttemptsTest</automated>
  </verify>
  <done>ExamEditorTest proves the two-tab editor, name-field validation, position ordering, edit-redirect, and preserved affordances; ExamControllerTest reflects the redirect; EDT-04 stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lecturer browser → exam editor writes | Details save and question add/edit/delete cross into AttemptVoider territory — already warned-and-voided atomically (EDT-04/D-7), unchanged here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering / Data loss | Details + question saves on an attempted exam | mitigate | This plan only reorganizes views; the shipped `_save-warning-modal` + atomic `AttemptVoider::void()` inside one transaction is reused verbatim — no destructive path is duplicated or bypassed. Regression-guarded by ExamUpdateVoidsAttemptsTest. |
| T-12-05 | Repudiation / Reachability | publish, results, delete, reset affordances | mitigate | All four affordances re-hosted on the editor and asserted present (NAV-04); no route removed. |
| T-12-06 | Elevation of Privilege | exam/question mutation routes | accept | `role:lecturer` route-group gate unchanged (D-09: exams carry no per-record ownership); no new write path added. |

No package installs in this plan.
</threat_model>

<verification>
- `php artisan test --filter=ExamEditorTest` + `--filter=ExamControllerTest` + `--filter=ExamUpdateVoidsAttemptsTest` pass.
- Full suite stays green.
- `exams.edit` redirects to `exams.show`; the editor is a single two-tab page with name field, position-ordered questions, and all Phase-10 affordances reachable.
</verification>

<success_criteria>
EDT-01 and EDT-02 satisfied: one exam editor page with Details and Questions tabs, an exam/test name field validated on save, the separate exam-edit and question pages folded in, and the EDT-04 warn-and-void plus publish/results/delete/reset affordances preserved.
</success_criteria>

<output>
Create `.planning/phases/12-lecturer-workspace-class-management-exam-editor-grading/12-02-SUMMARY.md` when done.

Artifacts this plan produces:
- Two-tab editor at `lecturer.exams.show`; `lecturer.exams.edit` now redirects to it
- `ExamController::show` feeds subjects + position-ordered questions + attemptCounts
- Consumed by: 12-05 (adds question/option move + shuffle buttons to the Questions tab), 12-04 (exams tab links here)
</output>
