---
phase: 13-student-exam-experience-class-page-take-exam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/student/attempts/show.blade.php
  - tests/Feature/Student/TakeExamPageTest.php
autonomous: true
requirements: [TAK-09, TAK-10, TAK-11, TAK-12]
must_haves:
  truths:
    - "The take-exam page keeps a sticky top bar carrying the subject name, the exam/test name, the live timer, and answered/total question progress (TAK-09)."
    - "A button in the top bar opens the exam instructions in a popup that reuses the app modal (x-modal), never a native alert — exam details and instructions read as separate things (TAK-09)."
    - "Question navigation is a vertical stepper that checkmarks answered questions from the server-persisted answered map, so the checkmarks survive a mid-exam page reload (TAK-10)."
    - "At 10 minutes remaining the timer turns red and a toaster fires exactly once via a fired-once flag, never per timer tick (TAK-11)."
    - "Answer options render in their authored position order and are never randomized at take time (TAK-12)."
    - "The server-authoritative timer, single-attempt constraint, MCQ auto-grade, and per-question autosave stay untouched — no enforcement moves client-side."
  artifacts:
    - path: "resources/views/student/attempts/show.blade.php"
      provides: "sticky top bar (subject+exam name, timer, progress, instructions button), instructions x-modal, vertical stepper, 10-min red + one-shot toaster on the existing attemptTimer component"
      min_lines: 380
  key_links:
    - from: "resources/views/student/attempts/show.blade.php (vertical stepper)"
      to: "attemptTimer.answered (server-seeded reactive map)"
      via: "each stepper item checkmark binds to answered[<questionId>], seeded from $answeredQuestionIds"
      pattern: "answered\\["
    - from: "resources/views/student/attempts/show.blade.php (instructions button)"
      to: "x-modal instructions popup"
      via: "top-bar button dispatches open-modal for the instructions modal"
      pattern: "open-modal"
    - from: "resources/views/student/attempts/show.blade.php (10-min watcher)"
      to: "attemptTimer fired-once flag"
      via: "one-shot guard at remaining <= 600 flips a flag and dispatches the toaster once"
      pattern: "600"
---

<objective>
Enhance the already-shipped take-exam page (TAK-09, TAK-10, TAK-11, TAK-12) — a presentation layer over `AttemptController::show()` data and the existing `attemptTimer` Alpine component. Add: a sticky top bar carrying subject name + exam name + timer + answered/total progress with an instructions-popup button; a vertical stepper whose checkmarks derive from the server-persisted answered map (so they survive a reload); a 10-minute red-timer + fire-exactly-once toaster; and a regression proving options render in authored order and the server timer/single-attempt enforcement is untouched.

Purpose: This phase touches the project's core value — the clock is enforced and the submission is saved. Every addition here is cosmetic/navigational; the server-authoritative deadline (`finalizeIfExpired`), the once-per-class constraint, MCQ auto-grade, and per-question autosave stay exactly as shipped.

Output: An enhanced `resources/views/student/attempts/show.blade.php` and a new `TakeExamPageTest.php`. No controller, model, route, or enforcement change; no new packages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-student-exam-experience-class-page-take-exam/13-CONTEXT.md

# THE file this plan enhances — read the whole thing before editing:
@resources/views/student/attempts/show.blade.php
# The controller that feeds it (DO NOT MODIFY — read to know what data exists):
@app/Http/Controllers/Student/AttemptController.php
# The modal + toast conventions to reuse:
@resources/views/components/modal.blade.php
@resources/views/components/toast.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sticky top bar (subject + exam name, progress, instructions popup) + TAK-12 option-order regression</name>
  <read_first>
    - resources/views/student/attempts/show.blade.php (the whole page is one x-data="attemptTimer(...)" scope; the sticky header at "sticky top-0 z-10" currently holds only exam title + the timer badge bound to display/badgeClasses(); the attemptTimer object already exposes answeredCount and answered)
    - app/Models/Exam.php (subject() belongsTo; title; description is nullable) and app/Models/Attempt.php (exam() belongsTo)
    - resources/views/components/modal.blade.php (open via $dispatch('open-modal', '<name>'); the submit-confirm modal at the bottom of show.blade.php is the working reference for name/x-on wiring)
    - app/Http/Controllers/Student/AttemptController.php show() (options are built with ->orderBy('position') and column-whitelisted to id/question_id/body — is_correct is never selected; the view must keep rendering options in the supplied order)
  </read_first>
  <files>resources/views/student/attempts/show.blade.php</files>
  <action>
    Enhance the existing sticky header block (the div with "sticky top-0 z-10"). Keep the existing timer badge (x-text="display", x-bind:class="badgeClasses()") exactly as-is — it is the shipped countdown. Add, within that same sticky bar:
      - The subject name AND the exam/test name (read $attempt->exam->subject->name for the subject; the exam title is already present). Read subject via $attempt->exam->subject->name in Blade — exam is already eager-loaded by the controller; do NOT modify the controller. Present subject and exam name as the two header identifiers (v3.md §Take Exam: "put subject name and exam/test name in header").
      - Question progress: answered/total, using the existing reactive answeredCount getter — e.g. an element binding x-text off answeredCount alongside the literal total count($questions). Keep it distinct from the submit modal's existing "N of M questions answered." string (which an existing test asserts verbatim — do not change that string).
      - An "Instructions" button in the top bar that dispatches open-modal for a NEW instructions modal (reuse x-modal, never a native alert). "Exam details and instructions read as separate things": render exam DETAILS (subject, duration_minutes, question count) inline in the header/body, and put INSTRUCTIONS behind the popup. The instructions modal body shows standard take-exam guidance copy (localized via __()) — that answers autosave as you go, the timer is server-enforced and auto-submits at zero, you submit once, and options stay in a fixed order — plus the exam's own $attempt->exam->description when present. Do NOT add an exams table column; there is no separate instructions field, so description supplies any exam-specific note.

    TAK-12 (no code change — assertion + guard): the controller already renders options via orderBy('position'); this task must NOT introduce any shuffle/sort/random in the view. Leave the @foreach ($question['options'] ...) loop rendering in the supplied order. (The Feature test in Task 3 proves rendered order == stored position order.)

    Do not touch the per-question autosave scopes, the auto-submit transition banner, the beforeunload wiring, or the submit modal. NO new packages.
  </action>
  <acceptance_criteria>
    - `grep -c "exam->subject->name" resources/views/student/attempts/show.blade.php` is >= 1 (subject name in header)
    - `grep -c "open-modal" resources/views/student/attempts/show.blade.php` is >= 2 (existing confirm-submit + new instructions modal trigger)
    - `grep -Ec "x-modal name=\"instructions\"|'instructions'|\"instructions\"" resources/views/student/attempts/show.blade.php` is >= 1 (instructions popup exists)
    - `grep -c "answeredCount" resources/views/student/attempts/show.blade.php` is >= 2 (top-bar progress reuses the same getter as the modal)
    - `grep -Eic "shuffle|Math.random|->random\(|sortBy\(" resources/views/student/attempts/show.blade.php` returns 0 (no runtime randomization introduced — TAK-12)
    - `php artisan test --filter=AttemptShowTest` still passes (the existing "N of M questions answered." assertion and is-correct guard stay green)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=AttemptShowTest</automated>
  </verify>
  <done>The sticky top bar shows subject + exam name, live answered/total progress, and an Instructions button that opens a modal popup; exam details sit in the header/body while instructions live behind the popup; no randomization is introduced and the existing take-page tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Vertical stepper with reload-surviving, server-seeded checkmarks (TAK-10)</name>
  <read_first>
    - resources/views/student/attempts/show.blade.php (the attemptTimer object: `answered` is a plain reactive object keyed by question id, seeded from @js($answeredQuestionIds) where $answeredQuestionIds = $savedAnswers->keys()->all(); markAnswered() sets answered[id]=true on the question-answered window event; the question cards loop over $questions with $index)
    - app/Http/Controllers/Student/AttemptController.php show() ($savedAnswers is keyed by question_id from persisted Answer rows — this is why the seed survives a reload)
  </read_first>
  <files>resources/views/student/attempts/show.blade.php</files>
  <action>
    Add a vertical stepper for question navigation (v3.md §Take Exam: "use vertical stepper for question navigation, put checkmark if answered", "show question number on the left of the question"). Restructure the questions body into a two-column layout: a LEFT rail holding the stepper and a main column holding the existing question cards. Keep the whole thing inside the outer attemptTimer x-data scope so the stepper can read the reactive answered map. On mobile, the rail may collapse/stack (Claude's discretion) — keep it simple.

    Give each question card a stable anchor id (e.g. id="question-{{ $question['id'] }}"). For each question, render a stepper item showing the question NUMBER ($index + 1) and a checkmark that is shown only when that question is answered: bind the checkmark's visibility to answered[<that question's id>] (the SAME reactive map answeredCount reads). Each stepper item links to its question anchor (href="#question-{{ $question['id'] }}") so clicking navigates to it.

    CRITICAL (TAK-10, criterion 4): the checkmark state MUST come from the answered map, which is seeded server-side from $answeredQuestionIds (persisted answers) — NOT from any new client-only Alpine state. This is what makes a mid-exam reload still show previously-answered questions checkmarked. Do not introduce a parallel per-card "answered" boolean; reuse the page-level answered map and the existing markAnswered()/question-answered event so live saves keep the stepper current.

    Do not alter the countdown, autosave, or submit logic.
  </action>
  <acceptance_criteria>
    - `grep -Ec "id=\"question-" resources/views/student/attempts/show.blade.php` is >= 1 (question anchors for stepper links)
    - `grep -Ec "#question-" resources/views/student/attempts/show.blade.php` is >= 1 (stepper items link to anchors)
    - `grep -c "answered\[" resources/views/student/attempts/show.blade.php` is >= 2 (stepper checkmark binds the shared answered map, not a new flag)
    - `grep -Eic "x-data=\"{[^}]*answered" resources/views/student/attempts/show.blade.php` returns 0 (no new per-card answered client state introduced)
    - `php artisan view:clear >/dev/null 2>&1 && php artisan view:cache` compiles without error
  </acceptance_criteria>
  <verify>
    <automated>php artisan view:clear >/dev/null 2>&1 && php artisan view:cache 2>&1 | grep -qiv error && echo VIEWS_OK</automated>
  </verify>
  <done>A vertical stepper shows each question's number with a checkmark bound to the server-seeded answered map; question cards carry anchors the stepper links to; no client-only answered state is introduced.</done>
</task>

<task type="auto">
  <name>Task 3: 10-min red timer + one-shot toaster (TAK-11) and Feature tests (TAK-09/10/11/12 + regression)</name>
  <read_first>
    - resources/views/student/attempts/show.blade.php (the attemptTimer() factory: `remaining` decrements each tick; setBucket()/badgeClasses() already map buckets to colors — normal/warning(amber)/critical(red, <=60s, animate-pulse); the sr-only announcement is written once per bucket transition, NOT per tick — this is the FIX-01 one-shot precedent to mirror)
    - resources/views/components/toast.blade.php (the app toast is session-driven; the 10-min alert here is a client-side, in-attempt event, so use an inline Alpine-driven toast/banner styled like the app toast — do not try to flash a session from JS)
    - tests/Feature/Student/AttemptShowTest.php (fixture idiom: build attempt with started_at/duration so remainingSeconds is known; assertViewHas('remainingSeconds', ...); the take page renders questions/options)
    - app/Http/Controllers/Student/AttemptController.php show() ($remainingSeconds seeds the countdown; server enforcement via finalizeIfExpired runs BEFORE render)
  </read_first>
  <files>resources/views/student/attempts/show.blade.php, tests/Feature/Student/TakeExamPageTest.php</files>
  <action>
    TAK-11 — 10-minute red + toaster fired exactly once:
    Extend the attemptTimer() object with a single fired-once boolean flag (e.g. tenMinuteWarned, initialised false). Add a one-shot guard — mirroring the existing per-transition announcement pattern, NOT a per-tick action — that fires when remaining first reaches or drops below 600 seconds: guard on (this.remaining <= 600 && ! this.tenMinuteWarned), and inside the guard set this.tenMinuteWarned = true FIRST, then reveal the toaster (e.g. flip a reactive showTenMinuteToast flag the markup binds x-show to, or dispatch a single custom event). Because the flag is set before the toaster is shown and checked in the guard, the toaster can fire at most once regardless of how many ticks pass under 600s. Do NOT put the toaster trigger on an unguarded per-tick path.
    Turn the timer RED at 10 minutes remaining: adjust the badge color logic so remaining <= 600 renders the red treatment (keep the final-minute pulse if desired). Do not remove the existing sr-only accessibility announcements.
    Render an inline toaster/banner (styled to match the app toast — border-l accent, dismissible) that is x-show-bound to the reactive flag, reading a localized "10 minutes remaining" message.

    TAK-11 is partly a runtime-visual behavior; the automated proof below asserts the one-shot flag LOGIC (single flag, guarded, set-once) and the 600s threshold. The actual on-screen red transition + toaster appearance is a manual/Dusk check (Phase 14) — record it as a manual verification note in the SUMMARY, do NOT add a blocking checkpoint.

    Create tests/Feature/Student/TakeExamPageTest.php (RefreshDatabase; enrolled student; build a published exam with an MCQ question whose options have scrambled position values, plus an in-progress attempt owned by the student). Cover:
      - TAK-09: GET route('student.attempts.show', $attempt) renders subject name and exam title in the response, and renders an Instructions modal trigger (assertSee the subject name; assertSee open-modal / the instructions modal name, false).
      - TAK-10 (reload-surviving checkmarks): persist an Answer for one question, then GET the take page and assert that question's id appears in the seeded answered/$answeredQuestionIds array in the rendered attemptTimer(...) call (assertSee the id within the seed) AND that the stepper markup binds answered[<id>] for that question. Then GET the page a SECOND time (simulating a mid-exam reload) and assert the id is STILL seeded — proving reload survival from persisted answers, not client state.
      - TAK-12: assert the MCQ options render in stored position order — assertSeeInOrder the option bodies ordered by their position values (the scrambled insert order must NOT match, so the assertion is meaningful).
      - TAK-11 (logic only): assert the rendered script contains the fired-once flag and the 600-second threshold, and that the flag is set inside the guard (grep-style assertSee for the flag name and "600"). Add a comment noting the visual red/toaster is a manual/Dusk check.
      - REGRESSION (core value): assert server enforcement is untouched — visiting the take page for an already-expired attempt still finalizes it server-side (mirror AttemptShowTest's expired->submitted case) and the answer endpoint still rejects a write past the deadline. Reuse/extend the existing expired-finalize idiom to prove this plan did not move enforcement client-side.
  </action>
  <acceptance_criteria>
    - `grep -Eic "tenMinuteWarned|warned10|tenMinute" resources/views/student/attempts/show.blade.php` is >= 2 (flag declared + guarded + set — the fired-once flag exists and is reused, not per-tick)
    - `grep -c "600" resources/views/student/attempts/show.blade.php` is >= 1 (10-minute threshold in seconds)
    - `grep -Eic "Math.random|shuffle" resources/views/student/attempts/show.blade.php` returns 0 (no randomization — TAK-12)
    - `php artisan test --filter=TakeExamPageTest` passes (TAK-09/10/11-logic/12 + regression cases green)
    - `php artisan test --filter=AttemptShowTest` still passes (shipped enforcement + answered-count assertions unchanged)
    - Full suite: `php artisan test` reports 0 failures
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter="TakeExamPageTest|AttemptShowTest"</automated>
  </verify>
  <done>The timer turns red at 10 minutes and a toaster fires exactly once via a guarded fired-once flag; TakeExamPageTest proves the top bar, reload-surviving checkmarks, authored option order, the one-shot flag logic, and that server-side deadline enforcement is untouched; the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client countdown → server deadline | The Alpine timer runs purely client-side and can be paused or DOM-edited; it must never be the enforcement mechanism. |
| controller view-model → rendered options | Option rows cross into the page; the answer-key flag must never be selected or rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | client attemptTimer countdown | accept (client) / mitigate (server) | The added 10-min red + toaster and the progress/stepper are cosmetic; the authoritative deadline stays server-side in Attempt::finalizeIfExpired(), unchanged. TakeExamPageTest's expired-finalize + past-deadline-write-rejected regression proves enforcement did not move client-side. |
| T-13-04 | Information Disclosure | option rendering / answer-key leak | mitigate | The controller's column-whitelisted view-model (id/question_id/body, orderBy position) is left untouched; the view renders option body only and introduces no is_correct read. Existing AttemptShowTest "never exposes is_correct" plus TAK-12 order assertion guard it. |

No package-manager installs occur in this plan; no supply-chain (T-13-SC) checkpoint applies.
</threat_model>

<verification>
- `php artisan test --filter="TakeExamPageTest|AttemptShowTest"` green.
- `grep -c "exam->subject->name" resources/views/student/attempts/show.blade.php` >= 1 (TAK-09 subject in header).
- `grep -c "answered\[" resources/views/student/attempts/show.blade.php` >= 2 (TAK-10 stepper reads the shared server-seeded map).
- `grep -c "600" resources/views/student/attempts/show.blade.php` >= 1 and the fired-once flag present (TAK-11 one-shot).
- `grep -Eic "Math.random|shuffle|sortBy\(" resources/views/student/attempts/show.blade.php` returns 0 (TAK-12).
- Full suite: `php artisan test` reports 0 failures.

**Manual verification (deferred to Phase 14 / browser, non-blocking):** confirm the timer visibly turns red and a single toaster appears when the countdown crosses 10:00, and that stepper checkmarks render correctly after a real mid-exam browser reload. Record the result in the SUMMARY.
</verification>

<success_criteria>
The take-exam page shows a sticky top bar with subject + exam name, the live timer, and answered/total progress, plus an Instructions popup separate from the exam details; a vertical stepper checkmarks answered questions from the server-persisted map and survives a reload; at 10 minutes the timer goes red and a toaster fires exactly once; options render in authored order; and the shipped server-authoritative timer, single-attempt constraint, auto-grade, and autosave remain untouched.
</success_criteria>

<output>
Create `.planning/phases/13-student-exam-experience-class-page-take-exam/13-02-SUMMARY.md` when done.
</output>
