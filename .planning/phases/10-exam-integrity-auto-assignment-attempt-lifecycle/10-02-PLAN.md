---
phase: 10-exam-integrity-auto-assignment-attempt-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Lecturer/ResetSubmissionsTest.php
  - tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php
  - tests/Feature/AttemptNullGuardTest.php
autonomous: true
requirements: [CLS-07, INT-03, EDT-04, INT-02]
must_haves:
  truths:
    - "Resetting an exam's submissions deletes every attempt and a reset student can start again"
    - "Saving an edit to an attempted exam voids its attempts, and the warning copy differs at graded=0 vs graded>0"
    - "A vanished attempt row during a lecturer's grade-save produces a safe redirect, never a 500"
  artifacts:
    - path: "tests/Feature/Lecturer/ResetSubmissionsTest.php"
      provides: "CLS-07 reset + INT-03 retake acceptance"
      contains: "retake"
    - path: "tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php"
      provides: "EDT-04 warn-and-void acceptance across all four editor mutations"
      contains: "voids"
    - path: "tests/Feature/AttemptNullGuardTest.php"
      provides: "D-5 third-crash-site regression (T-09-01)"
      contains: "AnswerGrade"
  key_links:
    - from: "tests/Feature/Lecturer/ResetSubmissionsTest.php"
      to: "routes/lecturer.php"
      via: "route('lecturer.exams.submissions.reset') — the route name this plan pins"
      pattern: "submissions.reset"
    - from: "tests/Feature/AttemptNullGuardTest.php"
      to: "app/Http/Controllers/Lecturer/AnswerGradeController.php"
      via: "Gate::after seam deletes the row before the locked read"
      pattern: "Gate::after"
---

<objective>
Land the remaining three Wave 0 executable specs RED: CLS-07/INT-03 (reset then retake), EDT-04
(warn-and-void across all four editor mutations), and D-5 (the last unguarded locked read).

Purpose: these pin the destructive paths' contracts — including the **route name** and the
**warning-copy variance** — before any implementation can quietly pick something else.
Output: two new test files + one extended file, committed RED.

**This plan also fixes a latent bug the artifacts missed.** `AttemptVanishedException::render()`'s
non-JSON branch redirects to `route('student.exams.index')`, which sits behind `role:student`
middleware (`routes/student.php:10`). A LECTURER hitting the D-5 guard would be redirected into a
route that 403s them, with a message telling them to "return to your exam list". RESEARCH.md states
"no changes to the exception class itself are needed" — that is incorrect, verified by direct read.
Task 3 pins the correct lecturer-facing behavior; plan 04 implements it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-exam-integrity-auto-assignment-attempt-lifecycle/10-CONTEXT.md
@.planning/phases/10-exam-integrity-auto-assignment-attempt-lifecycle/10-UI-SPEC.md
@.planning/phases/10-exam-integrity-auto-assignment-attempt-lifecycle/10-VALIDATION.md
</context>

<expected_red_state>
All three files are RED when this plan completes. That is the deliverable.

- `ResetSubmissionsTest` errors — `route('lecturer.exams.submissions.reset')` is not defined yet (plan 07).
- `ExamUpdateVoidsAttemptsTest` fails — the published-edit gate still 403s (plan 08 relaxes it).
- `AttemptNullGuardTest`'s new third method fails with a TypeError/500 — the guard does not exist yet (plan 04).

Do NOT add routes, controllers, or the service in this plan. The 340 pre-existing tests, and
`AttemptNullGuardTest`'s two EXISTING methods, MUST stay green — you are adding to that file, not
rewriting it.
</expected_red_state>

<route_name_contract>
This plan is the first to name CLS-07's route. Once written into a test, it is the contract plan 07
must satisfy. Use exactly:

- Route name: `lecturer.exams.submissions.reset`
- Verb + URI: `DELETE lecturer/exams/{exam}/submissions`
- Controller: `ExamController@resetSubmissions`

Rationale: `DELETE` states the semantics honestly (D-2 hard-deletes), and the name slots beside the
existing `lecturer.exams.publish` / `lecturer.exams.unpublish` pair. This mirrors the locked
route-name contract precedent recorded at `routes/lecturer.php:58-60` for the results routes — a test
pins the name, and the implementation conforms to the test, not the reverse.
</route_name_contract>

<tasks>

<task type="auto">
  <name>Task 1: CLS-07 + INT-03 spec — reset destroys everything, and the student can start again</name>
  <files>tests/Feature/Lecturer/ResetSubmissionsTest.php</files>
  <read_first>
    - `tests/Feature/Student/AttemptAvailabilityTest.php` lines 27-35 — `enrolledStudentFor()`, the correct same-subject fixture template (pins `section.subject_id` to `exam.subject_id`).
    - `app/Http/Controllers/Student/AttemptController.php` — read `store()`. INT-03's retake works through its existing `firstOrCreate`; confirm the shape for yourself and note the redirect it returns.
    - `database/migrations/2026_07_15_100009_create_attempts_table.php` line 25 — the `unique(exam_id, user_id)` index. This is what D-2's delete releases, and why INT-03 needs no migration.
    - `database/factories/AttemptFactory.php` — the `submitted()` / `graded()` states.
    - `routes/lecturer.php` lines 42-46 — the existing publish/unpublish route pair this plan's new route sits beside.
  </read_first>
  <action>
Create `Tests\Feature\Lecturer\ResetSubmissionsTest` using `RefreshDatabase`.

Use the route contract fixed in this plan's `<route_name_contract>` block:
`$this->actingAs($lecturer)->delete(route('lecturer.exams.submissions.reset', $exam))`.

**Fixture discipline — the factory trap applies here too.** Any student who must be able to START an
exam needs `section.subject_id` pinned to `exam.subject_id`. Build a private helper
`enrolledStudentFor(Exam $exam): User` copied from `AttemptAvailabilityTest::enrolledStudentFor()`,
but WITHOUT the `$exam->sections()->sync(...)` line — plan 06 drops the pivot, and a sync call here
would break this file at wave 3. Pin the subject and attach the enrollment; that is all that is
needed once visibility is subject-derived.

Write these methods:

**`test_reset_deletes_every_attempt_on_the_exam()`** (CLS-07). Three attempts in the three states
(in_progress / submitted / graded, distinct students). Act: DELETE the reset route as a lecturer.
Assert `assertDatabaseCount('attempts', 0)` and a redirect.

**`test_reset_permanently_deletes_graded_scores()`** (CLS-07 + INT-02's stated consequence). A graded
attempt WITH answer rows carrying scores. After reset, `assertDatabaseMissing('answers', ['attempt_id' => $attempt->id])`.
This is D-2's locked, deliberate behavior — the test documents the destruction as intended, not as a bug.

**`test_a_student_whose_attempt_was_reset_can_start_the_exam_again()`** (INT-03 — the requirement's
actual acceptance). This is the method that proves the `unique(exam_id, user_id)` index no longer
locks the student out:
  - enrolled student starts an attempt (POST `student.attempts.store`), assert it exists
  - lecturer resets
  - the SAME student POSTs `student.attempts.store` again → assert it succeeds (not a 403/409/500)
    and `assertDatabaseHas('attempts', ['exam_id' => $exam->id, 'user_id' => $student->id])`
  - assert the new attempt's id differs from the old one — proving a genuinely fresh attempt, not a
    resurrected row.

**`test_reset_only_affects_the_target_exam()`**. A second exam with its own attempts is untouched.
Without this, a `void()` missing its `where('exam_id', ...)` would wipe the whole table and still
pass every method above.

**`test_reset_reports_the_outcome_to_the_lecturer()`** (success criterion 5). Assert
`assertSessionHas('status')` after the reset — the flash key is `status`, NOT `success`
(`session('success')` has zero call sites in this codebase and `<x-toast>` does not read it). Assert
the message contains the count, per the UI-SPEC copy contract: `Reset 3 submission(s) for "{title}".`

**`test_a_student_is_forbidden_from_resetting_submissions()`**. A student hitting the route →
`assertForbidden()`, and `assertDatabaseCount('attempts', 1)` (unchanged). The `role:lecturer`
middleware on the route group provides this; the test pins it.

Class doc comment: this file is CLS-07 + INT-03's acceptance, and it pins the
`lecturer.exams.submissions.reset` route-name contract for plan 07.
  </action>
  <verify>
    <automated>php artisan test --filter=ResetSubmissionsTest</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/Lecturer/ResetSubmissionsTest.php` exists.
    - `php artisan test --filter=ResetSubmissionsTest` is RED (route-not-defined errors).
    - The route contract is pinned: `grep -c 'lecturer.exams.submissions.reset' tests/Feature/Lecturer/ResetSubmissionsTest.php` returns >= 4.
    - INT-03's retake method exists: `grep -c 'can_start_the_exam_again' tests/Feature/Lecturer/ResetSubmissionsTest.php` returns 1.
    - The exam-scoping method exists: `grep -c 'only_affects_the_target_exam' tests/Feature/Lecturer/ResetSubmissionsTest.php` returns 1.
    - No pivot use (this file must survive plan 06 untouched): `grep -c 'sections()->sync' tests/Feature/Lecturer/ResetSubmissionsTest.php` returns 0.
    - The flash key is `status`, not `success`: `grep -c "assertSessionHas('success'" tests/Feature/Lecturer/ResetSubmissionsTest.php` returns 0.
    - `routes/lecturer.php` is unchanged: `git diff --name-only HEAD -- routes/` is empty.
  </acceptance_criteria>
  <done>CLS-07 and INT-03 have an executable spec that pins the route name, the retake, exam scoping, and the outcome flash.</done>
</task>

<task type="auto">
  <name>Task 2: EDT-04 spec — all four editor mutations warn, then void, atomically</name>
  <files>tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php</files>
  <read_first>
    - `app/Http/Controllers/Lecturer/ExamQuestionController.php` — read `store()`, `update()`, `destroy()` in full. Note `destroy()`'s inline `abort_if($exam->is_published, 403)` at line 149 (D-6's fourth gate site) and the `abort_unless($question->exam_id === $exam->id, 404)` nested-binding guard each method opens with.
    - `app/Http/Controllers/Lecturer/ExamController.php` — read `update()` (line 90) and `destroy()` (line 103). `destroy()`'s `abort_if($exam->is_published, 403)` is EXPLICITLY OUT OF SCOPE and must stay.
    - `app/Http/Requests/Lecturer/UpdateExamRequest.php`, `StoreQuestionRequest.php`, `UpdateQuestionRequest.php` — the three `return ! $this->route('exam')->is_published;` bodies (D-4's three sites).
    - `tests/Feature/Lecturer/ExamPublishedEditGateTest.php` — the same routes, opposite assertion. Read its request shapes (especially the `options`/`correct_option` payload for question store/update) and reuse them.
    - `.planning/phases/10-exam-integrity-auto-assignment-attempt-lifecycle/10-UI-SPEC.md` — the EDT-04 modal copy table and the toast copy table. Copy strings verbatim.
  </read_first>
  <action>
Create `Tests\Feature\Lecturer\ExamUpdateVoidsAttemptsTest` using `RefreshDatabase`.

EDT-04 covers **four** editor mutations, not one (D-4's three Form Requests plus D-6's inline
`abort_if` in `ExamQuestionController::destroy()`). Relaxing only some of them ships a half-EDT-04
that passes a narrow test — a lecturer could edit an attempted exam's text but not remove a question,
for no principled reason. This file must exercise all four.

Build a private helper `attemptedPublishedExam(): array` returning `[$exam, $question, $attempts]`
where `$exam` is published, has at least one question, and carries three attempts (in_progress /
submitted / graded, distinct students). Do NOT use the pivot — no `sections()->sync()` call — so this
file survives plan 06 untouched.

Write one voiding method per mutation, each asserting attempts are gone AND the edit landed:

**`test_updating_an_attempted_exams_details_voids_its_attempts()`** — PUT `lecturer.exams.update`
with a changed title. Assert `assertDatabaseHas('exams', ['id' => .., 'title' => 'Changed'])` AND
`assertDatabaseCount('attempts', 0)`. Both halves matter — D-7 makes them one atomic transaction.

**`test_adding_a_question_to_an_attempted_exam_voids_its_attempts()`** — POST `lecturer.exams.questions.store`.

**`test_updating_a_question_on_an_attempted_exam_voids_its_attempts()`** — PUT `lecturer.exams.questions.update`.

**`test_deleting_a_question_on_an_attempted_exam_voids_its_attempts()`** — DELETE
`lecturer.exams.questions.destroy` (D-6's fourth site). Assert the question row is gone AND attempts are 0.

**`test_editing_an_exam_with_no_attempts_does_not_touch_anything_and_flashes_the_plain_message()`** —
the zero-count branch. A published exam with NO attempts: the edit saves, and the flash is exactly
`Exam updated.` (the existing, unchanged copy). This pins the UI-SPEC's "friction only exists where
risk exists" rule.

**`test_a_save_that_voids_attempts_reports_the_side_effect_in_its_flash()`** (success criterion 5).
After a voiding save, the flash must be `Exam updated. 3 affected attempt(s) were reset.` — NOT the
plain `Exam updated.`. The lecturer must see the side effect happened, not just that the save
succeeded. Key is `status`.

**`test_a_failed_validation_on_an_attempted_exam_destroys_no_attempts()`** — THE critical safety
method. PUT `lecturer.exams.update` with an invalid payload (e.g. `duration_minutes` => 0 or a
missing `title`). Assert `assertSessionHasErrors(...)` AND `assertDatabaseCount('attempts', 3)`. A
void that ran before validation would destroy every student's work for a save that never landed —
the worst possible outcome. This method is what forbids that ordering.

**Warning-copy variance (INT-02 via EDT-04).** Two methods asserting the rendered GET of
`lecturer.exams.edit` differs by population, per the UI-SPEC copy table verbatim:
  - `test_the_edit_page_warning_names_only_the_ungraded_population_when_none_are_graded()` — with
    2 in_progress + 0 graded, the page contains `have started this exam but have not been graded` and
    does NOT contain `have already been graded`.
  - `test_the_edit_page_warning_names_the_graded_population_when_scores_are_at_stake()` — with
    graded > 0, the page contains `have already been graded`.
  These use `assertSee(..., false)` / `assertDontSee(..., false)` — the copy contains no HTML, but the
  curly quotes and interpolated counts make escaping-sensitive assertions fragile; assert on the
  distinctive clause substrings only, not whole sentences.

**Do NOT assert on `ExamController::destroy()`** (whole-exam deletion). Its `abort_if($exam->is_published, 403)`
is explicitly unchanged this phase per UI-SPEC §1.B. Add a short comment in the class doc saying so,
so a future reader does not "fix" the omission.
  </action>
  <verify>
    <automated>php artisan test --filter=ExamUpdateVoidsAttemptsTest</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` exists.
    - `php artisan test --filter=ExamUpdateVoidsAttemptsTest` is RED (403s from the still-closed gates).
    - All four mutations are exercised: `grep -c 'exams.update\|questions.store\|questions.update\|questions.destroy' tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` returns >= 4.
    - The validation-safety method exists: `grep -c 'destroys_no_attempts' tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` returns 1.
    - Both copy variants are pinned: `grep -c 'have already been graded' tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` returns >= 2.
    - Whole-exam delete is untouched: `grep -c 'exams.destroy' tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` returns 0.
    - No pivot use: `grep -c 'sections()->sync' tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` returns 0.
    - No production code changed: `git diff --name-only HEAD -- app/ routes/` is empty.
  </acceptance_criteria>
  <done>EDT-04 has an executable spec covering all four editor mutations, the zero-count branch, the copy variance, and the validation-before-void ordering.</done>
</task>

<task type="auto">
  <name>Task 3: D-5 spec — the third and last unguarded locked read (T-09-01)</name>
  <files>tests/Feature/AttemptNullGuardTest.php</files>
  <read_first>
    - `tests/Feature/AttemptNullGuardTest.php` — the WHOLE file. You are extending it, not rewriting it. Read its class doc comment (which enumerates sites 1 and 2), its `attemptFixture()` helper, its `hardDeleteAttemptRow()` helper, and — critically — the `Gate::after` seam helper that deletes the row after route-model binding but before the locked read. Copy that seam verbatim for the third site.
    - `app/Http/Controllers/Lecturer/AnswerGradeController.php` — the whole file (41 lines). Line 29's `$locked = Attempt::whereKey($attempt->id)->lockForUpdate()->first();` feeds a non-nullable `AttemptGrader::syncStatus(Attempt $attempt)` at line 34 with no null-check. This is the crash site.
    - `app/Exceptions/AttemptVanishedException.php` — the whole file. Note `render()`'s non-JSON branch: `redirect()->route('student.exams.index')`.
    - `routes/student.php` line 10 — the `role:student` middleware on that redirect target. This is why the exception needs a lecturer branch.
    - `app/Http/Requests/Lecturer/GradeAnswerRequest.php` — its `authorize()`/`rules()`, so your request payload passes validation and actually reaches line 29.
    - `routes/lecturer.php` line 65 — `lecturer.attempts.answers.grade`, a PATCH form route (not JSON).
  </read_first>
  <action>
Extend the EXISTING `Tests\Feature\AttemptNullGuardTest` with the D-5 third-site coverage. Do not
modify its two existing test methods — they are green and pin sites 1 and 2.

**Why this matters now.** Today, hitting this race needs a student deleting their own account at the
exact moment a lecturer saves a grade. Once this phase ships exam reset (D-2's hard delete), it
becomes: a lecturer saves a grade while another lecturer resets that exam. That is an ordinary
Tuesday. Phase 9's threat model T-09-01 mandates a null-check after EVERY `lockForUpdate()->first()`
on `attempts`; this is the last unguarded one.

Update the class doc comment to enumerate **Site 3**: `AnswerGradeController::update()`
(`app/Http/Controllers/Lecturer/AnswerGradeController.php:29`) — a lecturer-facing form PATCH, the
first vanished-row call site outside the student attempt flow.

Add these methods:

**`test_a_vanished_attempt_row_during_a_lecturer_grade_save_does_not_crash()`** — the core D-5
regression. Use the same `Gate::after` seam the existing methods use to hard-delete the `attempts`
row after route-model binding but before the controller's locked read. Act: PATCH
`lecturer.attempts.answers.grade` as a lecturer with a valid score payload. Assert the response is
NOT a 500 — assert it is a redirect. Today this is a TypeError (null passed to a non-nullable
`Attempt` parameter), so this method is RED.

**`test_a_vanished_attempt_row_during_a_lecturer_grade_save_redirects_the_lecturer_somewhere_they_can_actually_go()`**
— pins the fix for the latent bug described in this plan's `<objective>`. The exception's existing
non-JSON branch redirects to `route('student.exams.index')`, which is behind `role:student`. A
lecturer sent there gets a 403 and a message telling them to return to "your exam list" — nonsense
for a lecturer, and a dead end. Assert:
  - `assertRedirect(route('lecturer.exams.index'))` — a lecturer-reachable destination
  - `assertSessionHas('error')` — the `<x-toast>` error variant's key (NOT `status`; the error variant
    persists until dismissed, which is correct for something the lecturer must notice)
  - the flashed message is NOT `AttemptVanishedException::MESSAGE` (the student copy). Assert the
    session error string does not contain `return to your exam list`.

**`test_the_students_vanished_row_message_is_unchanged_for_student_routes()`** — a regression guard on
the branch you are about to add. Reuse the existing site-2 (autosave) path and assert the JSON
`vanished`/`expired` contract still holds, so plan 04's lecturer branch cannot regress the student
behavior Phase 9 shipped and tested.

Plan 04 implements the guard and the exception's lecturer branch. Do not implement either here.
  </action>
  <verify>
    <automated>php artisan test --filter=AttemptNullGuardTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=AttemptNullGuardTest` shows the two pre-existing methods still PASSING and the new D-5 methods FAILING (correct mixed state for Wave 0).
    - The third site is covered: `grep -c 'lecturer.attempts.answers.grade' tests/Feature/AttemptNullGuardTest.php` returns >= 2.
    - The lecturer redirect target is pinned: `grep -c "assertRedirect(route('lecturer.exams.index'))" tests/Feature/AttemptNullGuardTest.php` returns >= 1.
    - The class doc comment names Site 3: `grep -c 'Site 3' tests/Feature/AttemptNullGuardTest.php` returns >= 1.
    - The existing methods are untouched: `git diff -U0 tests/Feature/AttemptNullGuardTest.php` shows only additions plus the class doc-comment edit — no deletions inside the two pre-existing test method bodies.
    - `app/Exceptions/AttemptVanishedException.php` and `app/Http/Controllers/Lecturer/AnswerGradeController.php` are unchanged: `git diff --name-only HEAD -- app/` is empty.
  </acceptance_criteria>
  <done>D-5's third crash site has a regression spec, and the lecturer-facing redirect bug in the inherited exception is pinned rather than inherited silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lecturer browser → destructive exam actions | An irreversible delete of graded student work is triggered from a form POST |
| lecturer browser → grade-save | A concurrent reset removes the attempt row mid-request (TOCTOU) |
| student browser → attempt start | A reset student re-enters the exam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering / Destruction | `ExamController@resetSubmissions`, `ExamController@update`, `ExamQuestionController@store/update/destroy` | mitigate | This plan pins the acceptance: the warning copy differs by population (graded=0 vs graded>0), the reset is exam-scoped, and — critically — `test_a_failed_validation_on_an_attempted_exam_destroys_no_attempts` forbids voiding before a successful write. ASVS L1 V11. |
| T-09-01 | Tampering / DoS | `AnswerGradeController::update()` line 29 — the last unguarded `lockForUpdate()->first()` on `attempts` | mitigate | This plan pins the regression via the established `Gate::after` seam, AND pins that the redirect must land somewhere a lecturer can actually reach — closing a latent dead-end the inherited exception would otherwise produce. After plan 04, T-09-01 holds repo-wide. ASVS L1 V11. |
| T-10-01 | Information Disclosure | — | transfer | Not this plan's surface. Covered by plan 01's `CrossSubjectVisibilityTest` and closed structurally in plan 06. Noted so the register stays complete across the phase. |
| T-10-SC | Tampering | package installs | mitigate | Not applicable — zero packages installed. CLAUDE.md forbids new Composer/npm dependencies; RESEARCH.md's audit records none for this phase. |
</threat_model>

<verification>
- `php artisan test --filter=ResetSubmissionsTest` → RED (route undefined).
- `php artisan test --filter=ExamUpdateVoidsAttemptsTest` → RED (403 from the closed gates).
- `php artisan test --filter=AttemptNullGuardTest` → 2 pre-existing methods green, new D-5 methods red.
- `php artisan test` → the pre-existing 340 remain passing.
- `git diff --name-only HEAD -- app/ routes/ database/ resources/` is empty — tests only.
</verification>

<success_criteria>
Three test files pin the phase's destructive-path contracts RED: the reset route name, the retake, the
four EDT-04 mutations, the copy variance, the validation-before-void ordering, and D-5's guard
including a lecturer-reachable redirect. Zero production code touched. The 340 pre-existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/10-exam-integrity-auto-assignment-attempt-lifecycle/10-02-SUMMARY.md` when done.

**Artifacts this plan produces**

Created:
- `tests/Feature/Lecturer/ResetSubmissionsTest.php` (6 methods — CLS-07 + INT-03; pins route `lecturer.exams.submissions.reset`)
- `tests/Feature/Lecturer/ExamUpdateVoidsAttemptsTest.php` (9 methods — EDT-04 across all four mutations)

Modified:
- `tests/Feature/AttemptNullGuardTest.php` (+3 methods for D-5's Site 3; class doc comment extended)

Deleted: none.

**Contract established for downstream plans**
- Route name `lecturer.exams.submissions.reset` → `DELETE lecturer/exams/{exam}/submissions` → `ExamController@resetSubmissions` (plan 07 must conform)
- `AttemptVanishedException` needs a lecturer branch redirecting to `lecturer.exams.index` with `session('error')` (plan 04 must implement)
</output>
</content>
