---
phase: 11-navigation-restructure-landing-hierarchy-dashboard-subjects
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/Http/Controllers/Student/SubjectBrowseController.php
  - resources/views/student/subjects/index.blade.php
  - tests/Feature/Student/EnrollmentPageTest.php
autonomous: true
requirements: [ENR-09, ENR-10, ENR-11]
must_haves:
  truths:
    - "Class enrollment is a single page with a clear select-subject -> select-class -> enroll flow."
    - "A student can enroll in as many subjects as they want — no credit limit blocks a second subject."
    - "Only classes whose enrollment window is currently open offer an enroll action; not-yet-open and closed classes are shown but not enrollable."
  artifacts:
    - path: "resources/views/student/subjects/index.blade.php"
      provides: "ENR-09 single-page class-enrollment flow (subject chooser + classes + enroll)"
      contains: "Class enrollment"
    - path: "app/Http/Controllers/Student/SubjectBrowseController.php"
      provides: "index() extended to render the chosen subject's classes on the same page"
  key_links:
    - from: "resources/views/student/subjects/index.blade.php"
      to: "app/Http/Controllers/Student/EnrollmentController.php"
      via: "enroll action posts to student.sections.enroll for open-window classes only"
      pattern: "route\\('student\\.sections\\.enroll'"
---

<objective>
Reshape the existing SubjectBrowse catalog into the single-page Class enrollment
flow the workflow asks for: pick a subject, see that subject's classes, enroll —
all on one page (ENR-09), with no credit limit (ENR-10) and enroll offered only for
currently-open windows (ENR-11). This is a copy/label + presentation change over the
already-shipped, capacity-safe enrollment backend (Decision #3 discipline: logic
unchanged, route names unchanged).

Purpose: Success criterion 4 of the phase.
Output: SubjectBrowseController@index extended to render a chosen subject's classes
inline, a rewritten student/subjects/index.blade.php three-step page, and an
enrollment-page test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-navigation-restructure-landing-hierarchy-dashboard-subjects-/11-CONTEXT.md

@app/Http/Controllers/Student/SubjectBrowseController.php
@app/Http/Controllers/Student/EnrollmentController.php
@app/Models/Section.php
@app/Models/Subject.php
@app/Enums/EnrollmentStatus.php
@resources/views/student/subjects/index.blade.php
@resources/views/student/subjects/show.blade.php
@tests/Feature/Student/SubjectBrowseControllerTest.php
</context>

<interface_context>
Consumes 11-01: x-back-button, and the nav label "Class enrollment" set in 11-01.
Reused backend (UNCHANGED): EnrollmentController@store (route student.sections.enroll) is
capacity-safe, window-checked, and enforces one-active-enrollment-per-subject/term; it is NOT a
credit limit — a student may hold active enrollments across many different subjects, which is
exactly what ENR-10 requires. Section::windowStatus() returns 'opens' | 'open' | 'closed';
enroll is offered only when 'open' (ENR-11), the same rule show.blade.php already applies.
student.subjects.show stays working and reachable (11-03 links class pages to it) — do not remove it.
Existing SubjectBrowseControllerTest asserts index lists subject names and show offers/withholds the
enroll action by window state — keep both green.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SubjectBrowseController@index to load a chosen subject's classes inline (ENR-09)</name>
  <files>app/Http/Controllers/Student/SubjectBrowseController.php</files>
  <read_first>
    - app/Http/Controllers/Student/SubjectBrowseController.php (index() + show() — reuse show()'s
      exact bounded section query: withCount enrolled_total, own-enrollment eager load, activeElsewhere)
    - app/Models/Section.php (windowStatus), app/Enums/EnrollmentStatus.php
  </read_first>
  <action>
    Change index() to accept the Request and an optional `subject` query parameter. Always load
    $subjects = Subject::has('sections')->orderBy('name')->get() for the chooser (step 1). When a
    valid `subject` id is present, also resolve $selectedSubject and compute its sections exactly as
    show() does today — reuse the identical bounded query (orderBy year/semester/sequence, withCount
    enrolled_total scoped to Enrolled, eager-load only the acting student's own enrollment) plus the
    $ownEnrollments and $activeElsewhere maps — so step 2/3 render on the same page with no extra
    per-row query. To avoid duplicating that logic between index() and show(), extract the
    section-loading into one private helper method on the controller and call it from both. Pass
    $subjects, $selectedSubject (nullable), $sections, $ownEnrollments, $activeElsewhere to
    view('student.subjects.index', ...). Leave show() behaviourally identical (it now delegates to the
    same helper). Do not change any route names.
  </action>
  <acceptance_criteria>
    - index() reads the optional subject parameter and, when present, passes $selectedSubject +
      $sections + $ownEnrollments + $activeElsewhere to the view.
    - The section-loading logic exists once (a private helper) and is called by both index() and show():
      `grep -nE "private function .*[Ss]ection" app/Http/Controllers/Student/SubjectBrowseController.php` matches.
    - No new N+1: the sections query still uses withCount + wherePivot as before.
    - php artisan test --filter=SubjectBrowseControllerTest passes (index + show cases unchanged).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=SubjectBrowseControllerTest</automated>
  </verify>
  <done>index() renders the chosen subject's classes inline while show() stays reachable and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite student/subjects/index.blade.php as the single-page enrollment flow (ENR-09, ENR-10, ENR-11)</name>
  <files>resources/views/student/subjects/index.blade.php</files>
  <read_first>
    - resources/views/student/subjects/show.blade.php (the sections table + per-section enroll/withdraw
      logic and window-status pills to reuse verbatim)
    - resources/views/components/status-pill.blade.php, back-button.blade.php
  </read_first>
  <action>
    Rewrite the page under x-app-layout with the heading "Class enrollment" (ENR-09 relabel of the old
    "Enroll in a Subject"). Render three visible steps on ONE page:
      Step 1 (select subject): a labelled subject chooser — a select element listing $subjects by name,
      inside a GET form submitting back to route('student.subjects.index') with the chosen subject id as
      the `subject` query param (a "Choose" submit button, or an Alpine @change submit). Keep each
      subject's name in the markup so it is discoverable.
      Step 2 (select class): only when $selectedSubject is set, render its $sections table reusing the
      exact section/window/own-enrollment presentation from show.blade.php (capacity "n/max", the FULL
      pill, the window status pill, and the student's own Enrolled/Withdrawn/Rejected state).
      Step 3 (enroll): for each section, offer the enroll action ONLY when the window is open and the
      section is enrollable — reuse show.blade.php's $canApply gate (windowStatus === 'open' && not full
      && not active-elsewhere) posting to route('student.sections.enroll', $section). For 'opens' and
      'closed' windows, show the class but render no enroll action (ENR-11). Add a short helper line of
      copy stating a student may enroll in as many subjects as they like (ENR-10 — no credit limit).
    Add an x-back-button "Back to home" pointing at route('student.home') at the top of the flow.
    Do not add an inline flash banner — the shell's x-toast renders session status/error already.
  </action>
  <acceptance_criteria>
    - index.blade.php heading text contains "Class enrollment".
    - The page contains a subject chooser submitting to route('student.subjects.index') with a `subject`
      param, and (when a subject is selected) the sections table with route('student.sections.enroll').
    - Enroll action is gated by the open-window condition (no enroll markup for 'opens'/'closed'):
      the $canApply gate from show.blade.php is reused.
    - The page contains x-back-button to route('student.home') and a no-credit-limit note.
    - php artisan test --filter=EnrollmentPageTest passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan view:clear && php artisan test --filter=EnrollmentPageTest</automated>
  </verify>
  <done>One page walks subject -> class -> enroll; only open-window classes are enrollable; no credit limit.</done>
</task>

<task type="auto">
  <name>Task 3: Enrollment-page test (ENR-09, ENR-10, ENR-11)</name>
  <files>tests/Feature/Student/EnrollmentPageTest.php</files>
  <read_first>
    - tests/Feature/Student/SubjectBrowseControllerTest.php (section/window seeding idioms, assertSee/assertDontSee on route('student.sections.enroll', ...))
    - tests/Feature/Student/EnrollmentControllerTest.php (enroll POST idioms for the ENR-10 across-subjects proof)
  </read_first>
  <action>
    Create tests/Feature/Student/EnrollmentPageTest.php (RefreshDatabase):
      - single_page_lists_subjects_and_reveals_classes: GET student.subjects.index shows the enrollment
        heading and the subject in the chooser; GET student.subjects.index?subject={id} additionally
        shows that subject's open section and its enroll action (assertSee route('student.sections.enroll', $section), false).
      - only_open_window_classes_are_enrollable (ENR-11): seed one section whose window has not opened
        and one that is closed for the selected subject; assert the page lists them but assertDontSee
        their enroll routes; seed an open section and assert its enroll route IS present.
      - no_credit_limit_across_subjects (ENR-10): enroll the student in an open class of subject A (POST
        student.sections.enroll), then assert the student can still be offered and can POST enroll for an
        open class of a DIFFERENT subject B — the second enrollment succeeds (assertDatabaseHas an
        Enrolled row for subject B's section), proving no across-subject cap.
  </action>
  <acceptance_criteria>
    - Test proves: subject chooser lists subjects; selecting a subject reveals its classes; open windows
      offer enroll while not-yet-open/closed windows do not; and a student can hold active enrollments in
      two different subjects at once.
    - php artisan test --filter=EnrollmentPageTest passes.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=EnrollmentPageTest</automated>
  </verify>
  <done>The single-page flow, the open-window gate, and the no-credit-limit rule are all proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student -> enrollment write | POST enroll crosses into a capacity- and window-guarded transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | enroll POST | mitigate | The write path (EnrollmentController@store) is UNCHANGED — server re-checks windowStatus and capacity under lockForUpdate; the view's open-window gate is UX only, not the enforcement boundary |
| T-11-04-02 | Information disclosure | index/show section query | mitigate | Only the acting student's own enrollment pivot is eager-loaded (wherePivot user_id = auth id); aggregate counts expose no other student identity — unchanged from the shipped SubjectBrowseController |
| T-11-04-03 | Elevation of privilege | student.subjects.* routes | accept | role:student route-group middleware remains the gate; unchanged |

No package-manager installs occur in this plan; no supply-chain (T-*-SC) row applies.
</threat_model>

<artifacts>
New: tests/Feature/Student/EnrollmentPageTest.php.
Changed: SubjectBrowseController (index accepts optional subject + shared private section-loading helper),
student/subjects/index.blade.php (single-page "Class enrollment" flow).
Unchanged and still reachable: student/subjects/show.blade.php, EnrollmentController, all route names.
</artifacts>

<verification>
- php artisan test --filter=EnrollmentPageTest
- php artisan test --filter=SubjectBrowseControllerTest (regression: index + show still green)
- php artisan test --filter=EnrollmentControllerTest (backend unchanged)
- Full suite green.
</verification>

<success_criteria>
Class enrollment is one page: choose a subject, see its classes, enroll — enroll offered only for
open windows, with no across-subject credit limit, over the unchanged capacity-safe backend.
</success_criteria>

<output>
Create `.planning/phases/11-navigation-restructure-landing-hierarchy-dashboard-subjects-/11-04-SUMMARY.md` when done
</output>
