---
phase: 09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p
plan: 08
type: execute
wave: 3
depends_on: ["09-02", "09-06", "09-07"]
files_modified:
  - resources/views/layouts/landing.blade.php
  - resources/views/landing.blade.php
  - routes/web.php
  - config/app.php
  - phpunit.xml
  - resources/views/layouts/navigation.blade.php
autonomous: true
requirements: [NAV-01, UX-01]

must_haves:
  truths:
    - "A guest visiting / sees a branded Online Examination Portal landing page, not Breeze's welcome view"
    - "An authenticated user visiting / is redirected to their dashboard"
    - "The page title and the navbar wordmark both read Online Examination Portal"
    - "The landing top bar carries the dark-mode toggle and a Sign in link, and no authenticated navigation"
  artifacts:
    - path: "resources/views/layouts/landing.blade.php"
      provides: "Full-bleed guest shell with the pre-paint dark-mode bootstrap and a top-bar slot"
      min_lines: 30
      contains: "localStorage.getItem('theme')"
    - path: "resources/views/landing.blade.php"
      provides: "The branded hero (NAV-01, UX-01)"
      min_lines: 30
      contains: "for Yayasan Peneraju Technical Assessment"
    - path: "routes/web.php"
      provides: "Guest-vs-authenticated branch on /"
      contains: "landing"
  key_links:
    - from: "routes/web.php"
      to: "resources/views/landing.blade.php"
      via: "GET / renders the landing view for guests, redirects authenticated users to route('dashboard')"
      pattern: "view\\('landing'\\)"
    - from: "resources/views/landing.blade.php"
      to: "resources/views/layouts/landing.blade.php"
      via: "<x-landing-layout> wrapper"
      pattern: "landing-layout"
    - from: "resources/views/layouts/landing.blade.php"
      to: "resources/views/components/toast.blade.php"
      via: "hosts <x-toast /> — consistent with the other two shells"
      pattern: "<x-toast"
---

<objective>
Replace Breeze's default `welcome` view with a branded landing page at `/` (NAV-01), and make the app
read as "Online Examination Portal" throughout (UX-01), per D-09 (keep a slim top bar).

Purpose: this is the first thing anyone assessing this deliverable sees. It is also the pre-login
half of Phase Success Criterion 1.
Output: a dedicated landing shell, the landing view, the `/` route branch, the app-name config, and
the navbar wordmark.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-PATTERNS.md
@tests/Feature/LandingPageTest.php
</context>

## Artifacts this phase produces

This plan produces:
- `resources/views/layouts/landing.blade.php` — new file, usable as `<x-landing-layout>`
- `resources/views/landing.blade.php` — new file
- `routes/web.php` — `/` becomes a guest/authenticated branch (route stays unnamed, as today)
- `config/app.php` — `'name'` default changes from `Laravel` to `Online Examination Portal`
- `phpunit.xml` — a new `APP_NAME` env entry making the UX-01 title assertion deterministic
- `resources/views/layouts/navigation.blade.php` — wordmark copy only

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated landing shell — resources/views/layouts/landing.blade.php</name>

  <files>resources/views/layouts/landing.blade.php</files>

  <read_first>
    - `resources/views/layouts/guest.blade.php` — the whole file (30 lines). Copy its `<head>` block:
      the charset/viewport meta tags, the Bunny Fonts preconnect + Figtree stylesheet link, and the
      `@vite(['resources/css/app.css', 'resources/js/app.js'])` call.
    - `resources/views/layouts/app.blade.php` lines 14-26 — the pre-paint dark-mode bootstrap script
      shipped in Phase 7. Copy it verbatim, including its comment. It must run BEFORE the Vite tags so
      the `dark` class is set before any stylesheet-dependent paint.
    - `resources/views/layouts/navigation.blade.php` lines 50-68 — the dark-mode toggle button. Copy it
      verbatim into the top bar: same inline `x-data`, same click handler, same localStorage key, same
      two SVG icons with their `x-show`/`x-cloak`.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-PATTERNS.md`
      § "`resources/views/landing.blade.php` + layout question (NAV-01, UX-01)" — the decisive evidence
      for why this file must exist rather than reusing either shipped layout.
  </read_first>

  <action>
Create `resources/views/layouts/landing.blade.php` as a new, small, dedicated shell.

**Why a third shell rather than reusing one of the two that ship** — record this as a comment at the
top of the file, because "why is there a third layout?" is the first question a reviewer will ask:
`layouts/guest.blade.php` hard-codes a centered narrow card (`sm:max-w-md`, `flex flex-col
sm:justify-center items-center`) with no top-bar slot, and the login page still needs that exact
behavior unchanged — so it cannot be branched to also serve a full-bleed hero. It also lacks the
pre-paint dark-mode bootstrap, which the landing page needs because D-09 puts the dark-mode toggle in
its top bar. `layouts/app.blade.php` has the bootstrap but `@include`s the full authenticated navbar,
which must not render for a guest. Neither fits; this shell takes the `<head>` from the former and the
bootstrap from the latter, with a full-bleed `<body>`.

Structure:
- `<!DOCTYPE html>` / `<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">` — matching both
  existing shells.
- `<head>`: charset, viewport, `<title>{{ config('app.name') }}</title>`, the Bunny Fonts preconnect
  and Figtree link, then the pre-paint dark-mode script copied verbatim from
  `layouts/app.blade.php:14-26`, then `@vite([...])`. Script before Vite — that ordering is the whole
  point of the bootstrap and its own comment says so.
- `<body class="font-sans antialiased">` wrapping a `min-h-screen flex flex-col bg-neutral-primary-soft`
  container. Use the ported token for the background, not a `bg-gray-*`/`dark:bg-gray-*` pair — this is
  new markup, so it should consume the UI-03 token vocabulary (09-CONTEXT.md's "leave existing views
  alone" applies to the ~28 legacy files, not to files this phase creates).
- A **top bar** (D-09: slim, and it survives to host the toggle and Phase 11's help button): the
  wordmark text on the left, and on the right the dark-mode toggle button copied verbatim plus a
  "Sign in" link to `route('login')`. Per 09-UI-SPEC.md's spacing scale use 16px horizontal padding.
  Do NOT render the navbar, any role-scoped links, or the user dropdown — this page is unauthenticated.
- A `<main class="flex-1">{{ $slot }}</main>` for the hero.
- `<x-toast />` once, before `</body>`, consistent with the other two shells.

Name it so Blade resolves `<x-landing-layout>` (an anonymous layout component at
`resources/views/layouts/landing.blade.php` is addressed as `<x-landing-layout>`, matching how
`layouts/guest.blade.php` is used as `<x-guest-layout>` in the six existing auth views — check one of
them to confirm the convention before wiring task 2).

Copy the dark-mode toggle **verbatim**. Do not reimplement or "improve" the localStorage/class-toggle
logic — it must stay byte-identical to the navbar's so both toggles drive the same `theme` key and the
same `dark` class. A second, subtly-different implementation is how the two ends of the app drift out
of sync.
  </action>

  <verify>
    <automated>php artisan view:clear && php artisan test --filter=LandingPageTest 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `resources/views/layouts/landing.blade.php` exists.
    - The pre-paint bootstrap is present and precedes the Vite tag. Confirm by reading the file: the
      line matching `localStorage.getItem('theme')` must appear at a lower line number than the line
      matching `@vite`. Both greps must return exactly 1:
      `grep -c "localStorage.getItem('theme')" resources/views/layouts/landing.blade.php` and
      `grep -c "@vite" resources/views/layouts/landing.blade.php`.
    - The toggle is present: `grep -c 'aria-label="{{ __('"'"'Toggle dark mode'"'"') }}"' resources/views/layouts/landing.blade.php` returns `1`.
    - It hosts the toast once: `grep -c "<x-toast" resources/views/layouts/landing.blade.php` returns `1`.
    - No authenticated navigation leaked in:
      `grep -c "layouts.navigation\|route('logout')\|route('dashboard')" resources/views/layouts/landing.blade.php` returns `0`.
    - The two shipped shells are untouched:
      `git diff --name-only resources/views/layouts/guest.blade.php resources/views/layouts/app.blade.php` produces no output.
  </acceptance_criteria>

  <done>
    A third, dedicated shell exists: full-bleed body, slim top bar with the verbatim dark-mode toggle
    and a Sign in link, the pre-paint bootstrap ahead of Vite, one `<x-toast />`, and no authenticated
    navigation. Neither shipped layout was modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the landing hero and branch the / route</name>

  <files>resources/views/landing.blade.php, routes/web.php</files>

  <read_first>
    - `tests/Feature/LandingPageTest.php` — written by plan 09-02. **This is the spec** — 7 tests fix
      the view name, the copy, the link and both redirect cases.
    - `routes/web.php` lines 7-13 — the current `/` handler returning `view('welcome')`, and the
      `dashboard` route registered immediately below behind `['auth', 'verified']`.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md`
      § "Landing page (`/` for guests, NAV-01/UX-01)" — the resolved hero contract, and § Typography
      for the Display (36px/600/1.2) and Heading (20px/600/1.2) roles.
    - `resources/views/welcome.blade.php` — confirm it exists before deciding what to do with it (see
      the action).
  </read_first>

  <behavior>
    See tests/Feature/LandingPageTest.php — 7 methods, all of which must pass:
    - Guest GET / returns 200 and renders view 'landing'
    - The hero shows "Online Examination Portal" and "for Yayasan Peneraju Technical Assessment"
    - A Sign in link points at route('login')
    - route('logout') does not appear
    - Authenticated student and lecturer are both redirected to route('dashboard')
  </behavior>

  <action>
**Create `resources/views/landing.blade.php`**, wrapped in `<x-landing-layout>`.

A single hero section, vertically centered, full-bleed, with no card or box around it. Per
09-UI-SPEC.md and 09-CONTEXT.md: **no features section, no how-it-works section, no role-picker.**
This is an assessment deliverable, not a marketing site. Do not add sections the contract does not name.

Contents, in order:
- Display line: "Online Examination Portal" — `text-4xl font-semibold` with tight leading, `text-heading`.
- Subtitle directly beneath: "for Yayasan Peneraju Technical Assessment" — Heading role,
  `text-xl font-semibold`, in `text-fg-brand` or `text-body` so it reads muted relative to the display line.
- A one-line description, Body role (`text-base`, weight 400), `text-body`. Exact wording is Claude's
  discretion per 09-UI-SPEC.md, but it must stay **one line** — e.g. "Take timed exams, view results,
  and manage assessments — all in one place."
- Primary CTA: a "Sign in" button linking to `route('login')`, filled `bg-brand` with white text,
  `rounded-base`, `shadow-xs`, and a `hover:bg-brand-strong` / `focus:ring-brand-medium` treatment
  matching the login card's button. Per 09-UI-SPEC.md's Color contract this is the phase's **only**
  primary CTA and the accent is reserved for it — do not add a second filled button.

Use the ported tokens throughout; no `dark:` classes (the tokens already resolve in both themes).
Follow the 8-point spacing scale — the login card's `py-2.5`/`mb-2.5` exception is scoped to that
verbatim snippet only and does not apply here.

**Edit `routes/web.php`.** Replace the existing `/` closure body so it returns
`redirect()->route('dashboard')` when `auth()->check()` is true, and `view('landing')` otherwise.
Keep it as the same unnamed closure route in the same position — no new route file, no controller, no
middleware. Add a short comment: guests get the landing page (NAV-01); authenticated users never see
it and go straight to their dashboard.

Note the asymmetry deliberately: `/` itself carries **no** `auth` middleware — it must stay reachable
by guests, which is the entire requirement. The redirect target `route('dashboard')` is the route that
carries `['auth', 'verified']`, so an authenticated-but-unverified user redirected there hits Breeze's
existing verification gate rather than the landing page. That is correct and needs no special-casing.

**Leave `resources/views/welcome.blade.php` on disk.** It now has no route pointing at it.
09-RESEARCH.md's State of the Art table says it is "replaced entirely" — that refers to its role at
`/`, which this task takes over. Deleting the file is a separate, trivial cleanup with a small chance
of tripping a Breeze-scaffold expectation, and it is not named by any requirement in this phase. Note
in the SUMMARY that it is now unreferenced so a later cleanup can remove it deliberately.
  </action>

  <verify>
    <automated>php artisan test --filter=LandingPageTest</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=LandingPageTest` passes tests 1, 2, 4, 5, 6 and 7. Test 3 (the
      `<title>` assertion) may still FAIL here — it depends on `config('app.name')`, which task 3 fixes.
    - The route branches correctly: `grep -c "view('landing')" routes/web.php` returns `1` and
      `grep -c "route('dashboard')" routes/web.php` returns at least `1`.
    - The old view is no longer routed: `grep -c "view('welcome')" routes/web.php` returns `0`.
    - `/` gained no auth middleware. Run `php artisan route:list --path=/ ` and confirm the `/` GET row
      lists no `auth` middleware. A guest must reach it.
    - The contract's exclusions hold — the hero has no extra sections:
      `grep -ci "how it works\|features\|I am a student\|I am a lecturer" resources/views/landing.blade.php` returns `0`.
    - Exactly one primary CTA: `grep -c "bg-brand" resources/views/landing.blade.php` returns `1`.
  </acceptance_criteria>

  <done>
    A guest at `/` sees the branded hero with the resolved copy and a single Sign in CTA; an
    authenticated user of either role is redirected to the dashboard. Six of seven LandingPageTest
    tests pass; the title test awaits task 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the app read as "Online Examination Portal" — config, test env, and navbar wordmark</name>

  <files>config/app.php, phpunit.xml, resources/views/layouts/navigation.blade.php</files>

  <read_first>
    - `config/app.php` line 16 — `'name' => env('APP_NAME', 'Laravel'),`. Every shell's `<title>` reads
      through `config('app.name')`.
    - `phpunit.xml` lines 20-32 — the `<php>` env block. It sets `APP_ENV`, `BCRYPT_ROUNDS`, etc. It
      does NOT set `APP_NAME`, so tests currently inherit whatever the local `.env` says.
    - `resources/views/layouts/navigation.blade.php` lines 6-11 — the wordmark, currently
      `{{ __('Exam Portal') }}`, linking to `route('dashboard')`.
    - `resources/views/layouts/app.blade.php` line 8 and `resources/views/layouts/guest.blade.php` —
      both render `<title>{{ config('app.name', 'Laravel') }}</title>`.
    - `tests/Feature/LandingPageTest.php` — test 3 is the title assertion this task turns green.
  </read_first>

  <behavior>
    - config('app.name') resolves to "Online Examination Portal" on a clean clone with no APP_NAME set
    - The test suite resolves it deterministically regardless of the local .env
    - The authenticated navbar wordmark reads "Online Examination Portal"
  </behavior>

  <action>
**Edit `config/app.php` line 16.** Change the fallback from `'Laravel'` to `'Online Examination Portal'`
so the line reads `'name' => env('APP_NAME', 'Online Examination Portal'),`. This is what makes UX-01
hold on a clean clone where `APP_NAME` is unset — which is exactly how this deliverable gets assessed
(cloned from a public repo, per PROJECT.md's delivery constraint). Do not change anything else in the file.

**Edit `phpunit.xml`.** Add `<env name="APP_NAME" value="Online Examination Portal"/>` to the existing
`<php>` block, alongside the other env entries. Reason, worth a brief comment: the suite otherwise
inherits `APP_NAME` from the developer's local `.env`, which a Breeze install sets to `Laravel` — so
the UX-01 title assertion would pass or fail depending on whose machine it runs on. Pinning it here
makes `LandingPageTest`'s title test deterministic and CI-safe. What it asserts is that the shells wire
`config('app.name')` into `<title>` at all; the *branding value* itself is carried by the `config/app.php`
default above.

**Edit `resources/views/layouts/navigation.blade.php` lines 8-10.** Change the wordmark text from
`{{ __('Exam Portal') }}` to `{{ __('Online Examination Portal') }}`. UX-01 requires the app be titled
"Online Examination Portal", and the navbar wordmark is where an authenticated user reads that name.
Change **only** the text node. Do not touch the `<a href="{{ route('dashboard') }}">`, its classes, or
anything else in this file — Phase 11 (NAV-03/NAV-04) owns the navigation restructure and this file will
be reworked there. A wider edit here creates a needless conflict with that phase.

**On `.env` / `.env.example`:** do not attempt to edit either. `.env` is gitignored and environment
access is restricted in this workspace; `.env.example` may be unreadable for the same reason. The
`config/app.php` default covers the clean-clone case and the `phpunit.xml` entry covers the test case,
so UX-01 is satisfied without them. **However:** if this working copy's `.env` sets `APP_NAME=Laravel`,
the running app will still show "Laravel" in the browser title bar even though every test passes,
because `.env` overrides the config default. Check this — run
`php artisan tinker --execute="echo config('app.name');"` — and if it does not print
"Online Examination Portal", record that fact prominently in the SUMMARY as an operator action
("set `APP_NAME=\"Online Examination Portal\"` in `.env`"). Do not fail the task over it, and do not
work around it by hardcoding the title into the shells — that would break the config-driven naming the
README (Phase 14) documents.
  </action>

  <verify>
    <automated>php artisan config:clear && php artisan test --filter=LandingPageTest && php artisan test --filter=ToastTest</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=LandingPageTest` passes ALL 7 tests, including the `<title>` assertion.
    - The clean-clone default is set: `grep -c "env('APP_NAME', 'Online Examination Portal')" config/app.php` returns `1`.
    - The test env is pinned: `grep -c 'APP_NAME' phpunit.xml` returns `1`.
    - The wordmark is updated: `grep -c "Online Examination Portal" resources/views/layouts/navigation.blade.php`
      returns `1`, and the old copy is gone: `grep -c "Exam Portal'" resources/views/layouts/navigation.blade.php` returns `0`.
    - The navigation edit was surgical: `git diff --stat resources/views/layouts/navigation.blade.php`
      shows 1 insertion and 1 deletion.
    - `php artisan test` — full suite green except ToastTest tests 2 and 4, which remain red pending plan 09-09.
    - The SUMMARY records the observed output of `php artisan tinker --execute="echo config('app.name');"`.
  </acceptance_criteria>

  <done>
    `config('app.name')` defaults to "Online Examination Portal" on a clean clone and is pinned for the
    test suite; the navbar wordmark reads the same. All 7 LandingPageTest tests pass. Any local `.env`
    override is recorded as an operator note rather than silently worked around.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → `GET /` | A fully unauthenticated surface, newly created by this plan. Nothing authenticated may render here, and it must not be gated behind `auth`. |
| authenticated browser → `GET /` | Redirected to `route('dashboard')`, which carries the existing `['auth', 'verified']` gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `resources/views/layouts/landing.blade.php`, `resources/views/landing.blade.php` | mitigate | The landing shell deliberately does not `@include('layouts.navigation')` and renders no user dropdown, no role-scoped links and no logout form. Enforced by two paired checks: `LandingPageTest`'s `test_the_landing_page_does_not_render_the_authenticated_navbar` asserting `route('logout')` is absent, and the acceptance criterion greping the shell for `layouts.navigation`/`route('logout')`/`route('dashboard')` and requiring zero. |
| T-09-11 | Elevation of Privilege | `routes/web.php` `/` | accept | `/` is intentionally unauthenticated — that is NAV-01 itself. It exposes only static marketing copy and a link to the existing login route; it reads no request input, touches no database and takes no parameters. The authenticated branch redirects to `route('dashboard')`, whose `['auth', 'verified']` middleware is unchanged and remains the actual gate — the redirect is a convenience, not a security boundary, and nothing is authorized by it. |
| T-09-10 | Information Disclosure | `<x-toast />` in the landing shell | accept | Same disposition as plan 09-07's guest-shell toast: flash data is per-session and set only by this app's controllers, so a guest can only see a message flashed into that guest's own session. In practice no flow flashes to `/` today. |

T-09-01 does not apply. T-09-02 is mitigated in the `<x-toast>` component itself (plan 09-07) and
inherited here. T-09-03 does not apply — this plan adds no dialog.

**ASVS L1:** V4 (Access Control) is the category to think about, and the finding is that it is
deliberately *not* changed — no route's middleware is added or removed; `/` was already public
(returning `welcome`) and stays public. V2/V3/V5/V6 are untouched: no auth, session, validation or
crypto surface. The `config/app.php` and `phpunit.xml` edits are branding and test determinism only.
</threat_model>

<verification>
- `php artisan test --filter=LandingPageTest` — 7/7 green.
- `php artisan route:list --path=/` — the `/` GET row carries no `auth` middleware.
- `php artisan test` — full suite green except ToastTest tests 2 and 4 (plan 09-09's).
- `bash scripts/ui-03-token-gate.sh` — still exits 0; the landing hero is a new token consumer.
- Dark mode: load `/`, click the toggle, reload — the theme persists and there is no flash of the wrong
  theme on reload (the pre-paint bootstrap doing its job).
</verification>

<success_criteria>
Phase Success Criterion 1's first half is satisfied: a visitor lands on a branded "Online Examination
Portal" page carrying the subtitle "for Yayasan Peneraju Technical Assessment" before logging in, and
reaches the login card from it. (Its second half — the card being actually styled, proven against the
compiled CSS — is plan 09-06.)
</success_criteria>

<output>
Create `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-08-SUMMARY.md` when done.
</output>
