---
phase: 09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Feature/LandingPageTest.php
  - tests/Feature/Auth/AuthenticationTest.php
autonomous: true
requirements: [NAV-01, NAV-02, UX-01]

must_haves:
  truths:
    - "LandingPageTest fails because / still renders Breeze's welcome view and does not redirect authenticated users"
    - "The extended AuthenticationTest fails because login.blade.php still renders Breeze's default markup, not the Flowbite card"
    - "Both files assert against the resolved copy in 09-UI-SPEC.md, not invented wording"
  artifacts:
    - path: "tests/Feature/LandingPageTest.php"
      provides: "NAV-01 + UX-01 executable spec"
      min_lines: 55
      contains: "for Yayasan Peneraju Technical Assessment"
    - path: "tests/Feature/Auth/AuthenticationTest.php"
      provides: "NAV-02 executable spec appended to the existing Breeze test file"
      contains: "Login to your account"
  key_links:
    - from: "tests/Feature/LandingPageTest.php"
      to: "routes/web.php"
      via: "GET / — guest sees landing, authenticated user redirects to route('dashboard')"
      pattern: "assertRedirect|route\\('dashboard'\\)"
    - from: "tests/Feature/Auth/AuthenticationTest.php"
      to: "resources/views/auth/login.blade.php"
      via: "GET /login — asserts the Flowbite card markup and the two Breeze auth links"
      pattern: "route\\('register'\\)|route\\('password.request'\\)"
---

<objective>
Land the Wave 0 failing tests for the two pre-login surfaces: the landing page (NAV-01, UX-01) and
the Flowbite login card (NAV-02).

Purpose: executable spec for plans 09-06 (login card) and 09-08 (landing). They MUST fail now.
Output: `tests/Feature/LandingPageTest.php` (new), plus new assertions appended to the existing
`tests/Feature/Auth/AuthenticationTest.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-VALIDATION.md
</context>

## Artifacts this phase produces

This plan produces:
- `tests/Feature/LandingPageTest.php` — new file
- New test methods appended to `tests/Feature/Auth/AuthenticationTest.php` — existing file, additive only

It **locks** (does not create) the following, which plans 09-06 and 09-08 must satisfy:
- Route `/` (name: none) — guest renders `landing` view, authenticated redirects to `route('dashboard')`
- View `resources/views/landing.blade.php`
- Layout `resources/views/layouts/landing.blade.php`
- `config('app.name')` resolving to `Online Examination Portal`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing NAV-01 + UX-01 spec — tests/Feature/LandingPageTest.php</name>

  <files>tests/Feature/LandingPageTest.php</files>

  <read_first>
    - `tests/Feature/Auth/AuthenticationTest.php` — the style analog: `use RefreshDatabase;` even on
      guest-only GETs (project convention), `$this->get('/')` / `$this->actingAs($user)->get(...)`,
      one behavior per method.
    - `routes/web.php` lines 1-14 — the current `/` handler returns `view('welcome')`; `dashboard` is
      registered at line 11 behind `['auth', 'verified']`.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md`
      § "Landing page (`/` for guests, NAV-01/UX-01)" — the resolved copy and structure. Assert
      against this file's exact strings; do not invent wording.
    - `database/factories/UserFactory.php` — confirm the `student()` / `lecturer()` state method names
      before using them.
  </read_first>

  <behavior>
    - A guest GET / returns 200 and renders the 'landing' view
    - A guest GET / shows the hero display line "Online Examination Portal"
    - A guest GET / shows the subtitle "for Yayasan Peneraju Technical Assessment"
    - A guest GET / offers a Sign in link pointing at route('login')
    - A guest GET / does NOT render the authenticated navbar (no dashboard link, no logout)
    - An authenticated student GET / redirects to route('dashboard')
    - An authenticated lecturer GET / redirects to route('dashboard')
    - The rendered page <title> contains "Online Examination Portal" (UX-01)
  </behavior>

  <action>
Create `tests/Feature/LandingPageTest.php` in namespace `Tests\Feature`, extending `Tests\TestCase`,
with `use RefreshDatabase;` and importing `App\Models\User`.

Write these test methods:

1. `test_the_landing_page_renders_for_a_guest` — `$this->get('/')` then `assertStatus(200)` and
   `assertViewIs('landing')`.
2. `test_the_landing_page_shows_the_app_title_and_subtitle` (UX-01) — `$this->get('/')` then
   `assertSee('Online Examination Portal')` and `assertSee('for Yayasan Peneraju Technical Assessment')`.
   Both strings come verbatim from 09-UI-SPEC.md's landing hero contract.
3. `test_the_landing_page_title_tag_names_the_app` (UX-01) — `$this->get('/')` then
   `assertSee('<title>Online Examination Portal</title>', false)`. Pass `false` as the second argument
   so the assertion is not HTML-escaped. If the eventual shell renders the title with surrounding
   whitespace, relax this to a `assertStringContainsString('Online Examination Portal', $response->getContent())`
   scoped to the `<title>` element — but keep the assertion targeting the title tag specifically, not
   the page body, since the hero already covers the body case in test 2.
4. `test_the_landing_page_links_to_login` — `$this->get('/')` then `assertSee(route('login'), false)`.
5. `test_the_landing_page_does_not_render_the_authenticated_navbar` — `$this->get('/')` then
   `assertDontSee(route('logout'), false)`. The landing page is unauthenticated; per 09-UI-SPEC.md it
   must not render the navbar/user-dropdown.
6. `test_an_authenticated_student_is_redirected_from_the_landing_page_to_the_dashboard` (NAV-01) —
   `$student = User::factory()->student()->create();` then
   `$this->actingAs($student)->get('/')->assertRedirect(route('dashboard'));`.
7. `test_an_authenticated_lecturer_is_redirected_from_the_landing_page_to_the_dashboard` (NAV-01) —
   same with `User::factory()->lecturer()->create()`.

Note for test 3: `config('app.name')` currently resolves from the `APP_NAME` env var and defaults to
`'Laravel'`. Plan 09-09 owns making this deterministic (it sets the `config/app.php` default and adds
an `APP_NAME` entry to `phpunit.xml`). Do not add that entry here — this plan only asserts the
outcome, and the test staying RED until 09-09 is correct.
  </action>

  <verify>
    <automated>php artisan test --filter=LandingPageTest 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Feature/LandingPageTest.php` exists and defines exactly 7 methods matching `^\s*public function test_`.
      Check: `grep -c "public function test_" tests/Feature/LandingPageTest.php` returns `7`.
    - The resolved subtitle copy is asserted verbatim:
      `grep -c "for Yayasan Peneraju Technical Assessment" tests/Feature/LandingPageTest.php` returns at least `1`.
    - Both redirect cases exist: `grep -c "route('dashboard')" tests/Feature/LandingPageTest.php` returns at least `2`.
    - `php artisan test --filter=LandingPageTest` FAILS. All 7 tests are red: `/` still returns
      Breeze's `welcome` view for guests and does not redirect authenticated users. A green run means
      the assertions are not reaching the route.
  </acceptance_criteria>

  <done>
    7 tests encode NAV-01 (guest landing + authenticated redirect, both roles) and UX-01 (title tag,
    hero display line, subtitle), asserting the exact copy resolved in 09-UI-SPEC.md. All fail today.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append the failing NAV-02 spec to tests/Feature/Auth/AuthenticationTest.php</name>

  <files>tests/Feature/Auth/AuthenticationTest.php</files>

  <read_first>
    - `tests/Feature/Auth/AuthenticationTest.php` — read the whole file first. It is a shipped Breeze
      test that currently passes. Every existing method must keep passing; this task is strictly additive.
    - `resources/views/auth/login.blade.php` — the current Breeze markup being replaced:
      `<x-guest-layout>`, `<x-auth-session-status>`, `<x-input-label>`, `<x-text-input>`,
      `<x-input-error>`, `<x-primary-button>`, "Forgot your password?" link at line 37.
    - `.planning/v3.md` lines 3-26 — the verbatim Flowbite 4 card the login page must reproduce.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md`
      § "Login card (`resources/views/auth/login.blade.php`, NAV-02)" — the enumerated required
      deviations from the raw snippet.
  </read_first>

  <behavior>
    - GET /login still returns 200 (existing Breeze test, unchanged)
    - GET /login renders the Flowbite card heading "Sign in to our platform"
    - GET /login renders the submit button text "Login to your account"
    - GET /login renders the footer line "Not registered?" with a link to route('register')
    - GET /login renders a "Lost Password?" link to route('password.request')
    - GET /login emits the ported token classes on the card (bg-neutral-primary-soft, rounded-base)
    - GET /login still posts to route('login') with a CSRF field
    - A failed login repopulates the email via old('email') and shows an inline field error, not a toast
    - POST /login with valid credentials still authenticates (existing Breeze test, unchanged)
  </behavior>

  <action>
Append new test methods to the existing `tests/Feature/Auth/AuthenticationTest.php`. Do not modify,
rename, or delete any existing method — the Breeze tests in this file are part of the 294 that
currently pass and must keep passing.

Add these methods:

1. `test_the_login_screen_renders_the_flowbite_card` (NAV-02) — `$this->get('/login')` then
   `assertSee('Sign in to our platform')` and `assertSee('Login to your account')`. Both strings are
   verbatim from the v3.md snippet (lines 6 and 22) and are named in 09-UI-SPEC.md's Copywriting
   Contract as non-renameable.
2. `test_the_login_card_links_to_the_register_route` (NAV-02) — `$this->get('/login')` then
   `assertSee('Not registered?')` and `assertSee(route('register'), false)`. Per 09-CONTEXT.md these
   wire to the real Breeze routes and are NOT left as the snippet's inert `#` hrefs.
3. `test_the_login_card_links_to_the_password_reset_route` (NAV-02) — `$this->get('/login')` then
   `assertSee('Lost Password?')` and `assertSee(route('password.request'), false)`.
4. `test_the_login_card_uses_the_ported_design_tokens` (NAV-02 ↔ UI-03) — `$this->get('/login')` then
   `assertSee('bg-neutral-primary-soft', false)` and `assertSee('rounded-base', false)`. This asserts
   the token class names reach the markup. It deliberately does NOT prove they emit CSS — that is
   UI-03's separate build gate, owned by plan 09-06. Add a comment saying exactly this, so a future
   reader does not mistake a green test here for UI-03 being satisfied.
5. `test_the_login_form_still_posts_to_the_login_route_with_csrf` (NAV-02) — `$this->get('/login')`
   then `assertSee('action="'.route('login').'"', false)` and `assertSee('name="_token"', false)`.
   This is the regression guard against reproducing the snippet's static `<form action="#">`.
6. `test_a_failed_login_repopulates_the_email_and_shows_an_inline_error` (NAV-02) — create a user via
   `User::factory()->create()`, POST to `route('login')` with that user's email and the wrong
   password, follow the redirect back to `/login`, and assert the response sees the submitted email
   value and `assertSessionHasErrors('email')`. Per 09-UI-SPEC.md, form validation errors stay inline
   per-field via `<x-input-error>` — UX-03's toast governs create/save/delete, not form validation.
   Add that as a comment.

Reuse whatever imports the file already has; add `use App\Models\User;` only if it is not already
present.
  </action>

  <verify>
    <automated>php artisan test --filter=AuthenticationTest 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - Six new methods are present:
      `grep -c "test_the_login_screen_renders_the_flowbite_card\|test_the_login_card_links_to_the_register_route\|test_the_login_card_links_to_the_password_reset_route\|test_the_login_card_uses_the_ported_design_tokens\|test_the_login_form_still_posts_to_the_login_route_with_csrf\|test_a_failed_login_repopulates_the_email_and_shows_an_inline_error" tests/Feature/Auth/AuthenticationTest.php`
      returns `6`.
    - The verbatim button copy is asserted: `grep -c "Login to your account" tests/Feature/Auth/AuthenticationTest.php` returns at least `1`.
    - `php artisan test --filter=AuthenticationTest` FAILS, and the failures are confined to the six
      new methods. Every pre-existing method in the file still passes. Confirm by reading the failure
      list in the output — if any pre-existing Breeze test name appears among the failures, this task
      has regressed the file and is not done.
  </acceptance_criteria>

  <done>
    Six additive test methods encode NAV-02 against the resolved copy and the enumerated deviations
    from the raw v3.md snippet. They fail today because login.blade.php still renders Breeze's default
    markup. All pre-existing tests in the file still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → `GET /` | Unauthenticated surface; must not leak authenticated navigation or user data |
| guest browser → `POST /login` | Breeze's existing credential boundary — unchanged this phase (view restyle only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `resources/views/landing.blade.php` (guest `/`) | mitigate | LandingPageTest test 5 asserts the landing page renders no `route('logout')`, i.e. no authenticated navbar/user-dropdown leaks onto the guest surface. |
| T-09-05 | Spoofing / CSRF | `resources/views/auth/login.blade.php` | mitigate | AuthenticationTest test 5 asserts `name="_token"` and the real `route('login')` action survive the Flowbite restyle — the regression guard against shipping the snippet's inert `<form action="#">`. Laravel's existing `VerifyCsrfToken` middleware is unchanged. |

T-09-01 (attempt TOCTOU) is out of this plan's scope — no attempt code path is touched. T-09-02 (toast
XSS) and T-09-03 (native dialogs) are carried by plans 09-07 and 09-10.

**ASVS L1:** V2 (Authentication) is touched only indirectly — this is a view restyle; Breeze's auth
controllers, routes, rate limiting and session handling are unchanged. V5 (Input Validation)
introduces no new rules; the login form keeps Laravel's existing CSRF and validation pipeline, which
test 5 and test 6 pin.
</threat_model>

<verification>
- `php artisan test --filter=LandingPageTest` fails all 7.
- `php artisan test --filter=AuthenticationTest` fails exactly the 6 new methods and passes all
  pre-existing ones.
- `php artisan test` shows no new failures outside these two files.
</verification>

<success_criteria>
Both Wave 0 pre-login test surfaces exist and are RED for the right reasons, locking the routes, view
names and exact copy that plans 09-06 and 09-08 must deliver.
</success_criteria>

<output>
Create `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-02-SUMMARY.md` when done.
</output>
