---
phase: 09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Feature/ToastTest.php
  - tests/Feature/NoNativeDialogTest.php
autonomous: true
requirements: [UX-02, UX-03]

must_haves:
  truths:
    - "ToastTest fails because <x-toast> does not exist yet"
    - "ToastTest pins the sentinel-exclusion landmine AND the duplicate-render landmine (a flash must render exactly once)"
    - "ToastTest pins T-09-02: flash text is escaped, never raw HTML"
    - "NoNativeDialogTest fails today with exactly 3 violations — the 3 known destructive forms"
  artifacts:
    - path: "tests/Feature/ToastTest.php"
      provides: "UX-03 executable spec incl. sentinel exclusion, single-render, and escaping"
      min_lines: 95
      contains: "substr_count"
    - path: "tests/Feature/NoNativeDialogTest.php"
      provides: "UX-02 static-scan gate over resources/views"
      min_lines: 40
      contains: "allFiles"
  key_links:
    - from: "tests/Feature/ToastTest.php"
      to: "resources/views/components/toast.blade.php"
      via: "renders any app-layout page with a seeded flash and asserts the toast markup"
      pattern: "withSession"
    - from: "tests/Feature/NoNativeDialogTest.php"
      to: "resources/views"
      via: "File::allFiles(resource_path('views')) content scan for native dialog calls"
      pattern: "resource_path\\('views'\\)"
---

<objective>
Land the Wave 0 failing tests for the alert system: the `<x-toast>` behavioral spec (UX-03) and the
native-dialog static-scan gate (UX-02).

Purpose: executable spec for plans 09-07 (toast component), 09-09 (removing duplicate inline
banners) and 09-10 (confirm modal). They MUST fail now.
Output: `tests/Feature/ToastTest.php`, `tests/Feature/NoNativeDialogTest.php`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-VALIDATION.md
@.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-PATTERNS.md
</context>

## Artifacts this phase produces

This plan produces:
- `tests/Feature/ToastTest.php` — new file
- `tests/Feature/NoNativeDialogTest.php` — new file

It **locks** (does not create) the following, which plans 09-07, 09-09 and 09-10 must satisfy:
- Blade component `<x-toast>` → `resources/views/components/toast.blade.php`
- Blade component `<x-confirm-modal>` → `resources/views/components/confirm-modal.blade.php`
- The invariant that a given flash message string appears **exactly once** in any rendered page
- The `aria-label="Dismiss"` contract on the toast close control

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing UX-03 spec — tests/Feature/ToastTest.php</name>

  <files>tests/Feature/ToastTest.php</files>

  <read_first>
    - `resources/views/components/auth-session-status.blade.php` — the existing flash-reading
      component (`@props(['status'])`, `@if ($status)`), used at `auth/login.blade.php:3` and
      `auth/forgot-password.blade.php:7`. It has NO sentinel exclusion.
    - `resources/views/auth/verify-email.blade.php` line 6 — sentinel `verification-link-sent`
      renders "A new verification link has been sent to the email address you provided during
      registration."
    - `resources/views/profile/partials/update-profile-information-form.blade.php` lines 41 and 53 —
      sentinels `verification-link-sent` and `profile-updated`; the latter renders "Saved."
    - `resources/views/profile/partials/update-password-form.blade.php` line 37 — sentinel
      `password-updated`, renders "Saved."
    - `resources/views/lecturer/exams/index.blade.php` lines 8-14 — an example of the inline
      `@if (session('status'))` banner that 11 views currently carry. Plan 09-09 deletes these; test 2
      below is what forces that deletion.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-UI-SPEC.md`
      § "`<x-toast>` component (UX-02, UX-03)" — placement, variants, dismiss behavior, sentinel list.
    - `tests/Feature/Auth/AuthenticationTest.php` — the Feature-test style analog.
  </read_first>

  <behavior>
    - A seeded session('status') renders visibly on an app-layout page
    - That status string appears EXACTLY ONCE in the HTML (no inline-banner duplicate)
    - A seeded session('error') renders visibly and exactly once
    - status 'profile-updated' renders no toast; the profile partial's own "Saved." still renders
    - status 'password-updated' renders no toast; the password partial's own "Saved." still renders
    - status 'verification-link-sent' renders no toast; verify-email's own inline sentence still renders
    - The toast close control carries aria-label="Dismiss"
    - Flash text containing HTML is escaped, never emitted raw (T-09-02)
  </behavior>

  <action>
Create `tests/Feature/ToastTest.php` in namespace `Tests\Feature`, extending `Tests\TestCase`, with
`use RefreshDatabase;` and importing `App\Models\User`.

Seed flashes with `->withSession([...])` rather than driving a real controller action — this isolates
the toast's rendering contract from any particular controller, which matters because 09-CONTEXT.md
locks "this touches zero controllers".

Write these test methods:

1. `test_a_status_flash_renders_as_a_toast` (UX-03) — `$lecturer = User::factory()->lecturer()->create();`
   then `$this->actingAs($lecturer)->withSession(['status' => 'Exam created.'])->get(route('lecturer.exams.index'))`
   and `assertSee('Exam created.')` plus `assertSee('aria-label="Dismiss"', false)`.

2. `test_a_status_flash_renders_exactly_once` (UX-03) — **the duplicate-render guard, and the reason
   plan 09-10 exists.** Same request as test 1, then capture `$response->getContent()` and assert
   `$this->assertSame(1, substr_count($content, 'Exam created.'));`.
   Add this comment above the assertion: `lecturer/exams/index.blade.php` currently renders its own
   inline `@if (session('status'))` banner, and 10 other views do the same. Once `<x-toast>` is in
   `layouts/app.blade.php`, both fire and the message renders twice — two styles on one page, which
   fails UX-03's "one consistent style" bar and re-creates the FIX-03 confusion the toaster exists to
   remove. Plan 09-09 deletes the inline banners; this assertion is what proves it happened.

3. `test_an_error_flash_renders_as_a_toast` (UX-03) — `$this->actingAs($lecturer)->withSession(['error' => 'That section is full.'])->get(route('lecturer.sections.index'))`
   then `assertSee('That section is full.')`.

4. `test_an_error_flash_renders_exactly_once` — same as test 3, `assertSame(1, substr_count($content, 'That section is full.'))`.
   `lecturer/sections/show.blade.php` and three student views currently inline `session('error')` too.

5. `test_the_profile_updated_sentinel_does_not_render_as_a_toast` (the landmine) — `$user = User::factory()->create();`
   then `$this->actingAs($user)->withSession(['status' => 'profile-updated'])->get(route('profile.edit'))`,
   `assertDontSee('profile-updated')` and `assertSee('Saved.')`. The first assertion proves the toast
   skipped the sentinel; the second proves the existing inline confirmation still works untouched.

6. `test_the_password_updated_sentinel_does_not_render_as_a_toast` — same shape with
   `['status' => 'password-updated']` against `route('profile.edit')`; `assertDontSee('password-updated')`
   and `assertSee('Saved.')`.

7. `test_the_verification_link_sent_sentinel_does_not_render_as_a_toast` — create an unverified user
   (`User::factory()->unverified()->create()` if that state exists; otherwise
   `User::factory()->create(['email_verified_at' => null])`), then
   `$this->actingAs($user)->withSession(['status' => 'verification-link-sent'])->get(route('verification.notice'))`,
   `assertDontSee('verification-link-sent')` and
   `assertSee('A new verification link has been sent to the email address you provided during registration.')`.
   This one renders through `layouts/guest.blade.php`, so it also proves the guest shell's toast honours
   the same exclusion list as the app shell.

8. `test_the_toast_escapes_html_in_flash_text` (T-09-02) — `$this->actingAs($lecturer)->withSession(['status' => '<b>bold</b>'])->get(route('lecturer.exams.index'))`,
   then `assertDontSee('<b>bold</b>', false)` and `assertSee('&lt;b&gt;bold&lt;/b&gt;', false)`. This
   pins the requirement that the component renders flash text through Blade's escaping echo and never
   through a raw echo. Comment it as T-09-02.

The three sentinel strings — `verification-link-sent`, `password-updated`, `profile-updated` — are the
complete exclusion list from 09-UI-SPEC.md. Do not add to it and do not pattern-match; these are exact
string comparisons.
  </action>

  <verify>
    <automated>php artisan test --filter=ToastTest 2>&1 | tail -25</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Feature/ToastTest.php` exists and defines exactly 8 methods matching `^\s*public function test_`.
      Check: `grep -c "public function test_" tests/Feature/ToastTest.php` returns `8`.
    - The single-render guard is present: `grep -c "substr_count" tests/Feature/ToastTest.php` returns at least `2`.
    - All three sentinels are pinned: each of
      `grep -c "verification-link-sent" tests/Feature/ToastTest.php`,
      `grep -c "password-updated" tests/Feature/ToastTest.php`,
      `grep -c "profile-updated" tests/Feature/ToastTest.php` returns at least `1`.
    - `php artisan test --filter=ToastTest` FAILS. Tests 1, 2, 3, 4 and 8 are red (no `<x-toast>`
      exists, so no toast markup and no `aria-label="Dismiss"`). Tests 5, 6 and 7 may pass today by
      accident — the sentinel is not toasted because nothing toasts anything yet. That is expected;
      they become meaningful regression guards the moment plan 09-07 lands the component.
  </acceptance_criteria>

  <done>
    8 tests encode UX-03: the status and error variants render, each renders exactly once, all three
    Breeze sentinels are excluded while their inline confirmations survive, the close control is
    labelled, and flash text is escaped (T-09-02).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the failing UX-02 static-scan gate — tests/Feature/NoNativeDialogTest.php</name>

  <files>tests/Feature/NoNativeDialogTest.php</files>

  <read_first>
    - `resources/views/lecturer/exams/show.blade.php` lines 50-85 — two destructive forms, each using
      an inline `onsubmit` handler that calls a native browser dialog and returns its result.
    - `resources/views/lecturer/subjects/index.blade.php` lines 34-42 — the third such form.
    - `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-PATTERNS.md`
      § "`tests/Feature/NoNativeDialogTest.php` — no direct analog; closest shape" — the proposed
      scanner shape. No existing test in this codebase scans view file contents; this is a new shape.
    - `tests/Unit/WindowSemanticsTest.php` — the closest style analog: no `RefreshDatabase`, no HTTP call.
  </read_first>

  <behavior>
    - Scanning resources/views for native browser dialog invocations yields zero matches
    - The scan today yields exactly 3 matches, in the 2 known lecturer view files
    - The failure message names the offending file paths, not just a count
  </behavior>

  <action>
Create `tests/Feature/NoNativeDialogTest.php` in namespace `Tests\Feature`, extending
`Tests\TestCase`. Do NOT use `RefreshDatabase` and do not make an HTTP call — this is a pure
file-content assertion (see 09-PATTERNS.md; `WindowSemanticsTest` is the closest style precedent
despite this file living under `tests/Feature` per 09-VALIDATION.md's Wave 0 list).

<!-- planner-discipline-allow: confirm( -->
<!-- planner-discipline-allow: alert( -->

Write two test methods:

1. `test_no_blade_view_invokes_a_native_browser_dialog` (UX-02) — iterate
   `\Illuminate\Support\Facades\File::allFiles(resource_path('views'))`, `file_get_contents()` each
   file, and collect `$file->getRelativePathname()` into a `$violations` array when the contents
   contain either of the two needles `confirm(` or `alert(` (use `str_contains`, two separate
   checks). Assert with
   `$this->assertSame([], $violations, 'Native browser dialog found in: '.implode(', ', $violations));`
   Use `assertSame([], ...)` rather than `assertEmpty(...)` so the diff names the files.

   Add a doc-comment on the method stating: UX-02 requires one popup/alert style throughout and that
   the browser's native dialogs are never used. The three call sites this scan finds today are
   migrated to `<x-confirm-modal>` by plan 09-10.

   Add a second comment, directly above the needle definitions, carrying this warning for future
   authors: this scan is a blunt substring match, so **no Blade file may define an Alpine or
   JavaScript function whose name ends in the needle text** — e.g. an `x-data` method named for the
   confirmation action would trip this gate even though it is not a native dialog. Plan 09-11's
   component must name its Alpine handler something else. Naming the needles in a `$needles` array
   variable rather than inlining them twice makes this constraint obvious at a glance.

2. `test_the_destructive_lecturer_forms_use_the_confirm_modal_component` (UX-02) — assert that
   `resources/views/lecturer/exams/show.blade.php` and
   `resources/views/lecturer/subjects/index.blade.php` each contain the string `<x-confirm-modal`.
   Read each via `file_get_contents(resource_path('views/lecturer/exams/show.blade.php'))` etc. and
   `assertStringContainsString`. Without this, test 1 could be satisfied by simply deleting the
   confirmation step entirely rather than migrating it — a silent removal of a destructive-action
   guard. Comment it as exactly that.
  </action>

  <verify>
    <automated>php artisan test --filter=NoNativeDialogTest 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `tests/Feature/NoNativeDialogTest.php` exists and defines exactly 2 methods matching `^\s*public function test_`.
      Check: `grep -c "public function test_" tests/Feature/NoNativeDialogTest.php` returns `2`.
    - The scan targets the view tree: `grep -c "resource_path('views')" tests/Feature/NoNativeDialogTest.php` returns at least `1`.
    - The component-usage guard is present: `grep -c "x-confirm-modal" tests/Feature/NoNativeDialogTest.php` returns at least `1`.
    - `php artisan test --filter=NoNativeDialogTest` FAILS both tests. Test 1's failure message must
      name exactly these two files: `lecturer/exams/show.blade.php` and
      `lecturer/subjects/index.blade.php`. If it names any other file, the needle is matching
      something unintended — investigate before proceeding, because plan 09-10 will be unable to turn
      this green.
  </acceptance_criteria>

  <done>
    A 2-method static-scan gate exists. It fails today naming exactly the two known view files, and it
    cannot be satisfied by deleting the confirmation step rather than migrating it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller flash → rendered HTML | `session('status')` / `session('error')` text crosses into the DOM via `<x-toast>`; today all values are `__()`-wrapped literals, but the component must not assume that |
| lecturer browser → destructive POST | Delete exam / delete question / delete subject; the confirmation step is the last guard before an irreversible write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering (XSS) | `resources/views/components/toast.blade.php` | mitigate | ToastTest test 8 seeds `<b>bold</b>` into `session('status')` and asserts the escaped entity is emitted and the raw tag is not — pinning Blade's escaping echo and forbidding a raw echo of flash text. Plan 09-07 implements it. |
| T-09-03 | Tampering (inconsistent UX; hard requirement) | `resources/views/**` | mitigate | NoNativeDialogTest test 1 enforces zero native dialogs across the whole view tree; test 2 additionally forbids satisfying it by deleting the confirmation rather than migrating it. Any reintroduction is a test failure, not a style nit. |

T-09-01 is out of this plan's scope (no attempt code path touched).

**ASVS L1:** V5 (Input Validation / output encoding) is the live category for T-09-02 — the mitigation
is contextual output encoding, which Blade provides by default and test 8 pins. V2/V3/V4/V6 are not
touched: no auth, session, access-control or crypto surface changes here. Note that `withSession()` in
these tests seeds the session directly and therefore does not exercise or weaken any real
authorization path.
</threat_model>

<verification>
- `php artisan test --filter=ToastTest` fails tests 1, 2, 3, 4, 8.
- `php artisan test --filter=NoNativeDialogTest` fails both, naming exactly 2 view files.
- `php artisan test` shows no new failures outside these two files.
</verification>

<success_criteria>
Both Wave 0 alert-system test files exist and are RED for the right reasons, and between them they
pin the two landmines that make this phase risky: the Breeze sentinel overload and the duplicate
inline-banner render.
</success_criteria>

<output>
Create `.planning/phases/09-v3-0-foundations-semester-model-design-tokens-alerts-entry-p/09-03-SUMMARY.md` when done.
</output>
