---
phase: 14-delivery-dark-mode-wiki-manual-demo-data-browser-tests
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - composer.json
  - composer.lock
  - .env.dusk.local
  - tests/DuskTestCase.php
  - tests/Browser/StudentFlowTest.php
  - tests/Browser/LecturerFlowTest.php
  - phpunit.xml
  - README.md
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
user_setup:
  - service: laravel-dusk
    why: "Running the browser tests needs Chrome + ChromeDriver + Herd serving at APP_URL — a real HTTP + browser process the executor may not be able to drive headlessly on this machine."
    dashboard_config:
      - task: "Create the separate Dusk MySQL database (e.g. yp-student-exam-dusk) named in .env.dusk.local"
        location: "Local MySQL / Herd"
      - task: "Run php artisan dusk after composer install and npm run build, with Herd serving APP_URL"
        location: "Terminal on the user's machine"

must_haves:
  truths:
    - "Laravel Dusk is installed as a dev dependency and configured to run against Herd (APP_URL=http://yp-test.test), not artisan serve"
    - "Dusk uses a SEPARATE database via .env.dusk.local + DatabaseTruncation — it never truncates the documented yp-student-exam database"
    - "Dusk drives the primary student and lecturer end-to-end flows by clicking through the navigation"
    - "php artisan test (Unit + Feature) stays green (baseline 441) alongside Dusk"
  artifacts:
    - path: ".env.dusk.local"
      provides: "Dusk environment — separate DB + Herd APP_URL"
      contains: "APP_URL"
    - path: "tests/DuskTestCase.php"
      provides: "Base Dusk case using the DatabaseTruncation trait (never RefreshDatabase across the HTTP boundary)"
      contains: "DatabaseTruncation"
    - path: "tests/Browser/StudentFlowTest.php"
      provides: "Student end-to-end flow driven by clicking through the nav with waitFor()"
      contains: "waitFor"
    - path: "tests/Browser/LecturerFlowTest.php"
      provides: "Lecturer end-to-end flow driven by clicking through the nav with waitFor()"
      contains: "waitFor"
  key_links:
    - from: "tests/DuskTestCase.php"
      to: ".env.dusk.local"
      via: "Dusk loads .env.dusk.local, whose DB_DATABASE is the separate Dusk DB, and DatabaseTruncation resets it between tests"
      pattern: "DatabaseTruncation"
    - from: "tests/Browser/StudentFlowTest.php"
      to: "resources/views/layouts/navigation.blade.php"
      via: "the flow clicks the nav/help/home links rather than visiting routes directly (proves NAV-04 reachability)"
      pattern: "clickLink|->click\\("
---

<objective>
TEST-01..04: install Laravel Dusk as a dev dependency, wire it to a SEPARATE database via `.env.dusk.local` + DatabaseTruncation (Decision #7 — never `yp-student-exam`), and write browser tests that drive the primary student and lecturer flows by clicking through the navigation, all while the existing PHPUnit suite stays green. Dusk is the one sanctioned exception to the "no new packages" rule (the brief mandates it). This is the final plan of the milestone.

Purpose: The finished portal is proven in a real browser end-to-end, with the demo data left untouched.
Output: Dusk installed + configured against a separate DB, two browser-flow tests, a green PHPUnit suite, a README "Running the browser tests" section, and a human-verified `php artisan dusk` run (flagged manual — needs Chrome/ChromeDriver/Herd).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-CONTEXT.md

# Decision #7 (LOCKED): Dusk gets .env.dusk.local + DatabaseTruncation against a SEPARATE
#   database BEFORE the first test runs — RefreshDatabase cannot cross Dusk's real HTTP
#   process boundary, and the default would wipe the documented yp-student-exam demo seed.
# Decision #6 (LOCKED): the browser's native page-leave confirmation (v2.0 AVL-05) stays a
#   MANUAL check — ChromeDriver 126+ auto-dismisses it before Dusk's dialog API sees it.
# Windows/Herd ChromeDriver + APP_URL behaviour is LOW-MEDIUM confidence — if the install or
#   the browser run cannot happen headlessly here, DOCUMENT it and flag it manual; never
#   silently skip TEST-* or mark them passed (14-CONTEXT.md execution boundary).
# php artisan test must stay green: keep tests/Browser OUT of phpunit.xml's Unit/Feature
#   testsuites so the standard suite (baseline 441) never tries to launch a browser.
@phpunit.xml
@composer.json
@routes/student.php
@routes/lecturer.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dusk + separate-DB config via .env.dusk.local + DatabaseTruncation (TEST-01, TEST-02)</name>
  <files>composer.json, composer.lock, .env.dusk.local, tests/DuskTestCase.php, phpunit.xml</files>
  <read_first>
    - composer.json require-dev — laravel/dusk will join the existing first-party laravel/* dev deps (breeze, sail, pail, telescope). Same vendor namespace, brief-mandated tool.
    - phpunit.xml — testsuites are Unit + Feature only today; keep it that way so `php artisan test` never launches a browser.
    - 14-CONTEXT.md execution boundary — the headless-fallback rule if composer/network is unavailable.
  </read_first>
  <action>
    Run `composer require --dev laravel/dusk` then `php artisan dusk:install`. laravel/dusk is a first-party Laravel package (Packagist `laravel/dusk`, same `laravel/` vendor as the existing dev deps) and is sanctioned by the brief + Decision #7 — no legitimacy checkpoint is required. Create `.env.dusk.local` by copying `.env` and overriding: `APP_URL=http://yp-test.test` (Herd — never `artisan serve`), and `DB_DATABASE` set to a SEPARATE database (e.g. `yp-student-exam-dusk`) that is NOT the documented demo DB. Configure tests/DuskTestCase.php so every Dusk test resets state with the DatabaseTruncation trait (which works across Dusk's real HTTP process boundary), NOT the request-lifecycle refresh trait the Feature suite uses. Confirm dusk:install did not add a Browser testsuite to phpunit.xml — the `<testsuite>` list must remain exactly Unit and Feature so the standard suite stays browser-free.

    If `composer require` or `php artisan dusk:install` cannot run in this environment (no network / no Chrome), DO NOT mark TEST-01/02 passed: write the `.env.dusk.local`, tests/DuskTestCase.php, and the manifest of what to run by hand, document the blockage in the SUMMARY, and leave everything staged for the user to `composer install` and finish. This is a flagged manual item, not a skip.
  </action>
  <acceptance_criteria>
    - laravel/dusk is a dev dependency: `grep -c 'laravel/dusk' composer.json` ≥ 1 (or, if the install could not run, the SUMMARY documents the blockage and the config files are staged).
    - `.env.dusk.local` exists with `APP_URL=http://yp-test.test` and a `DB_DATABASE` value that is NOT `yp-student-exam` (the separate Dusk DB) — verify the two lines.
    - tests/DuskTestCase.php uses the DatabaseTruncation trait: `grep -c 'DatabaseTruncation' tests/DuskTestCase.php` ≥ 1, and it does NOT use the Feature suite's refresh trait (verify by reading the trait imports).
    - phpunit.xml still declares only the Unit and Feature testsuites (no Browser suite): `grep -c 'testsuite name' phpunit.xml` returns 2.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test</automated>
  </verify>
  <done>Dusk is installed (or staged with a documented blockage), pointed at Herd + a separate DB via .env.dusk.local, resets with DatabaseTruncation, and the standard PHPUnit suite still runs browser-free.</done>
</task>

<task type="auto">
  <name>Task 2: Browser tests for the primary student + lecturer flows, clicking through the nav (TEST-03)</name>
  <files>tests/Browser/StudentFlowTest.php, tests/Browser/LecturerFlowTest.php</files>
  <read_first>
    - routes/student.php — student flow: login → home (dashboard + subjects) → class page (student.subjects.class) → start attempt (attempts.store) → take-exam (attempts.show) → submit (attempts.submit) → result (attempts.result).
    - routes/lecturer.php — lecturer flow: login → home → per-subject hub (subjects.manage: Classes + Exams tabs) → exam editor (exams.show) → grading (results.index/show, attempts.answers.grade).
    - resources/views/layouts/navigation.blade.php — the nav/help/home links to CLICK (the flow must click through nav to prove NAV-04, not visit routes directly).
    - tests/DuskTestCase.php — the base case + DatabaseTruncation from Task 1.
  </read_first>
  <action>
    Write tests/Browser/StudentFlowTest.php and tests/Browser/LecturerFlowTest.php. Each test seeds its OWN fixtures directly in the Dusk database with factories (never the full demo DatabaseSeeder — Dusk's DB is separate and truncated between tests), logs in via the login screen (`loginAs` or by filling the form), and then drives the flow by CLICKING nav links, buttons, and tabs rather than calling `visit(route(...))` directly — this is what proves NAV-04 reachability through the real UI. For every Alpine-driven assertion (toasts, the timer turning red, the stepper checkmarks, the instructions popup, tab switches), wait on the element/condition with Dusk's waitFor()-family assertions, never a fixed-duration sleep. Student flow: reach an exam from the class page, start it, answer a question, submit, and see the result/submitted page. Lecturer flow: open a subject's management hub, switch between the Classes and Exams tabs, open the exam editor, and reach the grading page. Do NOT script the browser's native page-leave confirmation dialog (Decision #6 — v2.0 AVL-05; ChromeDriver auto-dismisses it, so it stays a manual check). Keep both test files free of any browser-native leave-dialog automation.
  </action>
  <acceptance_criteria>
    - Both browser tests exist and drive flows via nav clicks: `grep -cE 'clickLink|->click\(' tests/Browser/StudentFlowTest.php` ≥ 3 and `grep -cE 'clickLink|->click\(' tests/Browser/LecturerFlowTest.php` ≥ 3.
    - Alpine-driven assertions wait on conditions: `grep -c 'waitFor' tests/Browser/StudentFlowTest.php` ≥ 1 and `grep -c 'waitFor' tests/Browser/LecturerFlowTest.php` ≥ 1; neither file uses a fixed-time sleep for an assertion (verify no `->pause(` gating an assertion).
    - Neither file automates the browser-native leave-confirmation dialog: `grep -ic 'beforeunload' tests/Browser/*.php` returns 0 (Decision #6 — it stays manual).
    - The files parse: `php -l tests/Browser/StudentFlowTest.php` and `php -l tests/Browser/LecturerFlowTest.php` report no syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>php -l tests/Browser/StudentFlowTest.php && php -l tests/Browser/LecturerFlowTest.php</automated>
  </verify>
  <done>Two Dusk flow tests exist, seed their own fixtures, click through the nav to exercise the primary student and lecturer journeys with waitFor()-based assertions, and leave the beforeunload dialog manual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Keep PHPUnit green + README browser-test section + the actual php artisan dusk run (TEST-04, manual-only verification)</name>
  <action>
STOP. Do not proceed past this checkpoint autonomously — `gate="blocking"`.

First, autonomously finish the buildable parts: confirm `php artisan test` still passes at baseline 441 (TEST-04 — tests/Browser stays out of phpunit.xml), and ADD a "Running the browser tests" section to README.md documenting how to create the separate Dusk database, populate `.env.dusk.local`, and run `php artisan dusk` (distinct from the existing `php artisan test` section; keep both). Confirm `grep -c 'php artisan dusk' README.md` ≥ 1.

Then present the verification steps below to the developer, wait for their explicit response, and record their verdict in the SUMMARY. The actual `php artisan dusk` browser run needs Chrome + ChromeDriver + Herd and is LOW-MEDIUM confidence on Windows/Herd — if it cannot run here, record it as a deferred manual-verification item; never mark TEST-01/TEST-03 "passed" without a human confirming a real browser run.
  </action>
  <what-built>Dusk is installed and configured against a separate database (Decision #7), and two browser-flow tests drive the primary student and lecturer journeys by clicking through the nav (TEST-03). The PHPUnit suite stays green because tests/Browser is kept out of phpunit.xml (TEST-04). The README gains a "Running the browser tests" section (create the separate Dusk DB, .env.dusk.local, `php artisan dusk`). The ACTUAL browser run needs Chrome + ChromeDriver + Herd serving APP_URL, and Windows/Herd behaviour is LOW-MEDIUM confidence — it cannot be proven headlessly here, so it is a flagged manual item.</what-built>
  <how-to-verify>
    1. Before this checkpoint, confirm the automated gates are green: `php artisan test` still passes at baseline 441 (Dusk did not disturb the Unit/Feature suite — TEST-04), and both tests/Browser files pass `php -l`.
    2. On the user's machine: ensure Herd serves the app at `http://yp-test.test`, run `composer install` and `npm run build`, create the separate Dusk database named in `.env.dusk.local`, then run `php artisan dusk`.
    3. Confirm both StudentFlowTest and LecturerFlowTest pass in a real Chrome window (TEST-01/TEST-03), and confirm afterwards that the documented `yp-student-exam` demo data is still intact (Dusk truncated only its own separate DB — TEST-02).
    4. Separately (Decision #6): the v2.0 AVL-05 beforeunload leave-confirmation remains a manual browser check — start an exam, try to close the tab, confirm the browser's own prompt appears. Not automated.
  </how-to-verify>
  <resume-signal>Type "approved" once `php artisan dusk` passes both flows against Herd with the demo DB untouched, or paste the ChromeDriver/APP_URL error for follow-up. If the environment cannot run Chrome/ChromeDriver, record it as a deferred manual-verification item (do not mark TEST-01/03 passed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB isolation | Dusk runs a real HTTP process against a SEPARATE database; the boundary that must hold is that Dusk never points at, nor truncates, the documented `yp-student-exam` demo DB (Decision #7). |
| package install | `composer require --dev laravel/dusk` pulls a package + ChromeDriver binary from the network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | Dusk wiping the demo DB | mitigate | `.env.dusk.local` sets `DB_DATABASE` to a separate Dusk DB and DatabaseTruncation resets only that DB; the human-verify step confirms the `yp-student-exam` demo data is intact after a run. |
| T-14-04-SC | Tampering | `composer require --dev laravel/dusk` | mitigate | laravel/dusk is classified [KNOWN] first-party — Packagist `laravel/dusk`, official Laravel package under the same `laravel/` vendor namespace as the existing laravel/breeze, laravel/sail, laravel/pail, and laravel/telescope dev deps, and explicitly sanctioned by the brief + Decision #7. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package is introduced, so no legitimacy checkpoint is required; the install is dev-only. |
</threat_model>

<verification>
- `php artisan test` green — Unit + Feature suite stays at baseline (441 passing), browser-free (TEST-04).
- `php -l tests/Browser/StudentFlowTest.php && php -l tests/Browser/LecturerFlowTest.php` — both parse.
- `.env.dusk.local` + tests/DuskTestCase.php prove the separate-DB + DatabaseTruncation configuration (TEST-02).
- MANUAL (flagged, joins the deferred human-verification list): the actual `php artisan dusk` browser run (needs Chrome + ChromeDriver + Herd; Windows/Herd LOW-MEDIUM confidence) and the Decision #6 beforeunload dialog. If `composer require`/`dusk:install` could not run headlessly, that is documented and staged, never marked passed.
</verification>

<success_criteria>
Success Criteria 4 and 5 of Phase 14 are met: `php artisan dusk` drives the primary student and lecturer flows in a real browser against Herd using its own database (leaving `yp-student-exam` untouched), and the existing PHPUnit suite stays green alongside Dusk — with the actual browser run and the beforeunload dialog flagged as manual-verification items per the execution boundary and Decision #6.
</success_criteria>

<output>
Create `.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-04-SUMMARY.md` when done. Record whether `composer require`/`dusk:install` ran here or was staged for the user, the separate Dusk DB name, and the manual-verification items handed off.
</output>
