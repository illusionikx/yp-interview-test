---
phase: 14-delivery-dark-mode-wiki-manual-demo-data-browser-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/components/status-pill.blade.php
  - resources/views/components/toast.blade.php
  - resources/views/components/confirm-modal.blade.php
  - resources/views/components/dashboard-card.blade.php
  - resources/views/components/welcome-banner.blade.php
  - resources/views/components/back-button.blade.php
  - resources/views/components/danger-button.blade.php
  - resources/views/components/secondary-button.blade.php
  - resources/views/lecturer/subjects/manage.blade.php
  - resources/views/lecturer/subjects/partials/_exams-tab.blade.php
  - resources/views/lecturer/subjects/partials/_classes-tab.blade.php
  - resources/views/lecturer/exams/show.blade.php
  - resources/views/lecturer/exams/questions/_form.blade.php
  - resources/views/lecturer/exams/_save-warning-modal.blade.php
  - resources/views/lecturer/results/index.blade.php
  - resources/views/lecturer/results/show.blade.php
  - resources/views/lecturer/home.blade.php
  - resources/views/student/home.blade.php
  - resources/views/student/subjects/index.blade.php
  - resources/views/student/subjects/class.blade.php
  - resources/views/student/exams/show.blade.php
  - resources/views/student/attempts/show.blade.php
  - resources/views/student/results/show.blade.php
  - tests/Feature/DarkModeContrastTest.php
autonomous: false
requirements: [FIX-02]

must_haves:
  truths:
    - "Every page in the new hierarchy is legible with dark mode on — no text left dark-on-dark"
    - "The exam editor (the page the bug report named) has correct text/surface contrast in dark mode"
    - "Every <x-status-pill> palette keeps both its light and its dark class pairing (a blanket find/replace cannot silently break one arm again)"
  artifacts:
    - path: "tests/Feature/DarkModeContrastTest.php"
      provides: "Regression guard proving each status-pill palette carries a matched light+dark class pair"
      contains: "DarkModeContrastTest"
    - path: "resources/views/components/status-pill.blade.php"
      provides: "Semantic pill with four locked light/dark palettes"
      contains: "dark:"
  key_links:
    - from: "tests/Feature/DarkModeContrastTest.php"
      to: "resources/views/components/status-pill.blade.php"
      via: "Blade::render('<x-status-pill .../>') then assertStringContainsString on both the light and dark class of each palette"
      pattern: "x-status-pill"
---

<objective>
FIX-02: fix dark-mode legibility site-wide. The defect is SEMANTIC (wrong colour pairing — e.g. dark text on a dark surface), not syntactic (a missing `dark:` prefix), so no grep for a missing prefix finds it. Audit shared components first (highest leverage — a past blanket find/replace already broke a non-gray palette once), then sweep page by page through the shipped hierarchy, treating the exam editor as a named required checkpoint.

Purpose: The finished portal must be readable in dark mode everywhere — the last visual-quality gate before the milestone ships.
Output: Corrected colour pairings across components and views, one automated component-contrast regression guard, and a human-verified legibility pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-CONTEXT.md

# Partial semantic token layer already exists (Phase 9, Decision #5): the tokens
# `text-body`, `text-heading`, `bg-neutral-primary-soft`, `border-default`, `bg-brand`,
# `rounded-base` are CSS-custom-property backed and adapt to the `dark` class BY
# CONSTRUCTION — they are NOT the bug surface. The bug surface is RAW colour utilities
# (`text-gray-900`, `bg-white`, `text-black`, non-gray `text-{green|red|amber|blue}-*`)
# whose `dark:` counterpart is wrong, missing on a dark-surfaced element, or inverted.
@tailwind.config.js
@resources/views/components/status-pill.blade.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Component-first dark-mode audit + status-pill contrast regression guard</name>
  <files>resources/views/components/status-pill.blade.php, resources/views/components/toast.blade.php, resources/views/components/confirm-modal.blade.php, resources/views/components/dashboard-card.blade.php, resources/views/components/welcome-banner.blade.php, resources/views/components/back-button.blade.php, resources/views/components/danger-button.blade.php, resources/views/components/secondary-button.blade.php, tests/Feature/DarkModeContrastTest.php</files>
  <read_first>
    - resources/views/components/status-pill.blade.php (four match() palettes: green/red/amber/gray, each already carrying a `dark:` arm — this is the highest-leverage regression target)
    - resources/views/components/toast.blade.php (uses token layer + raw `border-l-green-*`/`dark:border-l-green-*` accents)
    - resources/views/components/dashboard-card.blade.php, resources/views/components/welcome-banner.blade.php (token-layer based — confirm no raw-utility drift)
    - resources/views/components/confirm-modal.blade.php, back-button.blade.php, danger-button.blade.php, secondary-button.blade.php
  </read_first>
  <behavior>
    - Rendering `<x-status-pill status="published">…</x-status-pill>` yields a class attribute containing BOTH `text-green-800` and `dark:text-green-300` (and `bg-green-100` + `dark:bg-green-900`).
    - The same matched-pair invariant holds for the `closed` (red), `full` (amber), and `withdrawn` (gray) palettes.
    - An unrecognised status falls back to the gray palette and still carries both a light and a `dark:` arm.
  </behavior>
  <action>
    Audit each shared component for RAW colour-utility pairings only (leave the token-layer utilities alone — they are dark-correct by construction). For every element that sets a light surface or light text with a raw utility, confirm it carries a correct `dark:` counterpart and that the two are not inverted (light text must not land on a light dark-mode surface, dark text must not land on a dark dark-mode surface). status-pill already carries all four pairings — verify, do not rewrite. Fix any component where a non-gray semantic colour lost its dark arm or was inverted.

    Then create tests/Feature/DarkModeContrastTest.php as a Feature test that uses `Illuminate\Support\Facades\Blade::render()` to render `<x-status-pill>` once per palette keyword and asserts, with assertStringContainsString, that each rendered output contains both the light class and the paired dark class named in the behaviour block above. This is the regression guard that a future blanket find/replace cannot silently strip one arm of a palette. Do NOT assert page-level legibility here — that is inherently visual and belongs to Task 3.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=DarkModeContrastTest` passes (all palette pairs asserted).
    - `grep -c 'dark:' resources/views/components/status-pill.blade.php` returns 4 (one per palette arm) — every palette retains a dark counterpart.
    - Every audited component file still renders (no Blade syntax error): `php artisan view:clear && php artisan test --filter=DarkModeContrastTest`.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DarkModeContrastTest</automated>
  </verify>
  <done>The status-pill contrast guard is green and every shared component's raw colour pairings are correct (light↔dark matched, never inverted).</done>
</task>

<task type="auto">
  <name>Task 2: Page-by-page dark-mode sweep through the shipped hierarchy (exam editor is a named checkpoint)</name>
  <files>resources/views/lecturer/subjects/manage.blade.php, resources/views/lecturer/subjects/partials/_exams-tab.blade.php, resources/views/lecturer/subjects/partials/_classes-tab.blade.php, resources/views/lecturer/exams/show.blade.php, resources/views/lecturer/exams/questions/_form.blade.php, resources/views/lecturer/exams/_save-warning-modal.blade.php, resources/views/lecturer/results/index.blade.php, resources/views/lecturer/results/show.blade.php, resources/views/lecturer/home.blade.php, resources/views/student/home.blade.php, resources/views/student/subjects/index.blade.php, resources/views/student/subjects/class.blade.php, resources/views/student/exams/show.blade.php, resources/views/student/attempts/show.blade.php, resources/views/student/results/show.blade.php</files>
  <read_first>
    - resources/views/lecturer/subjects/manage.blade.php + partials/_exams-tab.blade.php + partials/_classes-tab.blade.php (the two-tab hub — the exam editor lives here and in exams/show + questions/_form)
    - resources/views/lecturer/exams/show.blade.php, resources/views/lecturer/exams/questions/_form.blade.php, resources/views/lecturer/exams/_save-warning-modal.blade.php (the exam-editor surface the bug report named)
    - resources/views/student/attempts/show.blade.php (take-exam page — sticky bar, stepper), student/subjects/class.blade.php (class page)
    - Enumerate the sweep set first: `grep -rl 'dark:' resources/views` — every file EXCEPT resources/views/layouts/navigation.blade.php and the two help pages (owned by plan 14-02).
  </read_first>
  <action>
    Sweep every view carrying `dark:` occurrences, page by page through the shipped hierarchy: landing/auth, both home dashboards, subject lists, class enrollment, the two-tab class-management hub (classes tab + exams tab), the exam editor (exams/show + questions/_form + the save-warning modal — the NAMED bug site: verify no heading, label, table cell, or helper text renders a raw dark text colour on a dark-mode surface), the take-exam page (sticky bar, vertical stepper, question body), grading, and results. For each wrong pairing, fix the raw-utility `dark:` arm so light-mode and dark-mode each pair light-on-dark or dark-on-light correctly. Prefer swapping the offending raw utility to the existing semantic token (`text-body`/`text-heading`/`bg-neutral-primary-soft`/`border-default`) where a nearby element already uses it, rather than inventing a new raw pairing. Do NOT touch resources/views/layouts/navigation.blade.php or either help page — plan 14-02 owns those files.
  </action>
  <acceptance_criteria>
    - The sweep set is fully visited: `grep -rl 'dark:' resources/views | grep -vE 'layouts/navigation|/help.blade.php' | wc -l` files were each reviewed and, where wrong, corrected (record the touched files in the SUMMARY).
    - The exam-editor surface (exams/show, questions/_form, _save-warning-modal) contains no raw dark text utility (`text-gray-800`, `text-gray-900`, `text-black`) that lacks a `dark:` override on the same element: `grep -nE 'text-(gray-800|gray-900|black)' resources/views/lecturer/exams/show.blade.php resources/views/lecturer/exams/questions/_form.blade.php resources/views/lecturer/exams/_save-warning-modal.blade.php` — every hit on that report either sits on a light-only element or carries a paired `dark:text-*` on the same class list (verify each by eye, list them in the SUMMARY).
    - `php artisan view:clear && php artisan test` keeps the full PHPUnit suite green (baseline 441 passing) — no Blade edit broke a view assertion.
  </acceptance_criteria>
  <verify>
    <automated>php artisan view:clear && php artisan test</automated>
  </verify>
  <done>Every swept page's raw colour pairings are corrected, the exam-editor surface is confirmed dark-legible in code, and the PHPUnit suite stays green (441).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual dark-mode legibility walkthrough (FIX-02's manual-only verification)</name>
  <action>
STOP. Do not proceed past this checkpoint autonomously — `gate="blocking"`. FIX-02's acceptance ("no text left dark-on-dark") is inherently VISUAL; no PHPUnit test proves it, so this human pass is the real gate.

Present the verification steps below to the developer, wait for their explicit response, and record their verdict in the SUMMARY. If they report a page still dark-on-dark, treat it as a finding to fix in this plan — not a note for later. If a browser is unavailable, record it as a deferred human-verification item (join the STATE.md deferred list); never mark the visual pass "passed" without a human confirming it.
  </action>
  <what-built>Dark-mode colour pairings corrected across shared components and every page in the shipped hierarchy, with the exam editor treated as the named checkpoint. The automated component-contrast guard proves the status-pill palettes stay paired, but page-level legibility ("no text dark-on-dark") is inherently VISUAL and cannot be proven by PHPUnit (constraint honoured — no test claims to prove legibility).</what-built>
  <how-to-verify>
    1. Serve the app (Herd at APP_URL, or `php artisan serve`) and log in.
    2. Toggle dark mode on (the sun/moon button in the top bar).
    3. Walk each page and confirm ALL text is legible against its surface — no dark-on-dark, no light-on-light:
       - Landing + login, both home dashboards (banner, stat cards, subject tables).
       - Lecturer: subject & class-management hub (Classes tab AND Exams tab, status pills), the EXAM EDITOR (Details + Questions tabs, question form, the save-warning modal) — this is the page the bug report named, inspect it closely.
       - Student: class page (status pills, taken/graded links), take-exam page (sticky top bar, timer, vertical stepper checkmarks, question body), results.
    4. Optionally capture Dusk screenshots in dark mode for the record (the browser run itself is plan 14-04's manual item).
  </how-to-verify>
  <resume-signal>Type "approved" if every page is legible in dark mode, or list the specific page + element that is still dark-on-dark for a follow-up fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan edits server-rendered Blade class attributes and adds one Feature test — no untrusted input crosses any new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | status-pill status string | accept | Pre-existing control unchanged: status-pill already maps arbitrary input through a fixed match() allowlist (T-07-01) and never interpolates the raw status into the class attribute; this plan only verifies the light/dark pairing survives. |
</threat_model>

<verification>
- `php artisan test --filter=DarkModeContrastTest` green (component pairings guarded).
- `php artisan view:clear && php artisan test` green — full PHPUnit suite stays at baseline (441 passing).
- MANUAL (flagged, joins the deferred human-verification list): Task 3 visual dark-mode legibility walkthrough across all pages, exam editor especially. Verification of "no text dark-on-dark" is inherently visual — no automated test asserts it.
</verification>

<success_criteria>
Success Criterion 1 of Phase 14 is met: every page in the new hierarchy is legible with dark mode on — no text left dark-on-dark, including the exam editor the bug report named — confirmed by the human-verify checkpoint, with the component palette pairings guarded automatically against future regression.
</success_criteria>

<output>
Create `.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-01-SUMMARY.md` when done. Record the exact list of view files touched, the exam-editor hits reviewed, and the human-verify outcome (approved / issues).
</output>
