---
phase: 14-delivery-dark-mode-wiki-manual-demo-data-browser-tests
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - resources/views/layouts/navigation.blade.php
  - resources/views/lecturer/help.blade.php
  - resources/views/student/help.blade.php
  - tests/Feature/HelpPageTest.php
autonomous: true
requirements: [UX-05, DEL-06]

must_haves:
  truths:
    - "A help button sits directly beside the light/dark toggle in the top bar (desktop and mobile) and opens the role's user manual"
    - "Each manual is wiki-style: a topic index/navigation plus cross-links between topics, not v2.0's single linear page"
    - "Every UI label quoted in a manual matches the shipped phases-11–13 screen verbatim"
  artifacts:
    - path: "resources/views/layouts/navigation.blade.php"
      provides: "Help button adjacent to the theme toggle in both the desktop and mobile bars"
      contains: "help.show"
    - path: "resources/views/student/help.blade.php"
      provides: "Wiki-style student manual — topic index + cross-linked topics with verbatim labels"
      contains: "href=\"#"
    - path: "resources/views/lecturer/help.blade.php"
      provides: "Wiki-style lecturer manual — topic index + cross-linked topics with verbatim labels"
      contains: "href=\"#"
  key_links:
    - from: "resources/views/layouts/navigation.blade.php"
      to: "routes/student.php + routes/lecturer.php (help.show)"
      via: "the help button's href resolves route('student.help.show') / route('lecturer.help.show')"
      pattern: "route\\('(student|lecturer)\\.help\\.show'\\)"
    - from: "resources/views/student/help.blade.php"
      to: "itself (topic anchors)"
      via: "in-page topic index anchors (href=\"#topic\") and cross-links between topic sections"
      pattern: "href=\"#"
---

<objective>
UX-05 + DEL-06: move the help entry from a standalone nav link to a button sitting beside the light/dark toggle, and rebuild both role manuals as wiki-style documents — a topic index with cross-links between topics — whose copy quotes the shipped UI labels verbatim. Authored now (not earlier) on purpose: phases 11–13 are shipped, so every button, tab, and link label is final and can be quoted exactly (the v2.0 Phase 8 precedent).

Purpose: The finished portal explains itself from a discoverable help button, and the manual is trustworthy because its labels match the real screens.
Output: A help button beside the theme toggle in both bars, two wiki-style manuals with verbatim labels and cross-links, and an updated HelpPageTest that guards reachability, topic headings, cross-links, and label accuracy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-CONTEXT.md

# Routes are unchanged — the manual is still served at lecturer.help.show / student.help.show
# (closures returning the help views). Only the ENTRY POINT (nav link -> button by the toggle)
# and the CONTENT (linear -> wiki, stale v2.0 labels -> verbatim phases-11–13 labels) change.
@routes/student.php
@routes/lecturer.php
@resources/views/layouts/navigation.blade.php
@resources/views/student/help.blade.php
@resources/views/lecturer/help.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Help button beside the theme toggle (UX-05); retire the old Help nav link</name>
  <files>resources/views/layouts/navigation.blade.php, tests/Feature/HelpPageTest.php</files>
  <read_first>
    - resources/views/layouts/navigation.blade.php — the desktop theme toggle sits in the `sm:flex ... gap-2` container (~line 54–73); the mobile toggle sits in the hamburger `sm:hidden gap-1` container (~line 109–127). The interim "Help" links are in the desktop links block (~line 33–36 lecturer, 46–49 student) and the mobile responsive block (~line 147–149, 157–159).
    - tests/Feature/HelpPageTest.php — `test_the_student_navbar_renders_links_to_class_enrollment_my_exams_and_help` and `test_the_lecturer_navbar_renders_links_to_classes_exams_and_help` currently assertSee('Help').
  </read_first>
  <action>
    Add a Help button as an immediate sibling of the light/dark toggle button in BOTH the desktop `gap-2` container and the mobile `gap-1` container. Render it as an anchor styled to match the toggle (icon button — a question-mark/lifebuoy SVG) whose href is `route('lecturer.help.show')` for lecturers and `route('student.help.show')` for students, with an accessible name of "Help" (aria-label="Help" and an sr-only "Help" label) so it stays discoverable and testable. Remove the now-duplicated "Help" entry from the interim role-links block in both the desktop and the mobile responsive menus. Keep the theme toggle, dropdown, and the other interim links (Classes / Class enrollment / My Exams) untouched. Use the existing token/dark utilities already on the toggle so the button is dark-legible without extra sweep work.

    Update HelpPageTest so the two navbar-render tests assert the help ENTRY is the button beside the toggle (assert the response still contains an element linking to the role's help route with the accessible "Help" name) rather than the retired nav link — keep every other assertion in those two tests intact (Classes/Exams, Class enrollment/My Exams still present).
  </action>
  <acceptance_criteria>
    - `grep -n "help.show" resources/views/layouts/navigation.blade.php` shows the help route referenced inside both the desktop toggle container and the mobile toggle container (2+ hits), and NOT in the interim links block.
    - `php artisan test --filter=HelpPageTest` passes — reachability, role-scoping, and the two navbar-render tests are green with the button as the help entry.
    - The theme toggle still renders (its `x-data`/localStorage block is untouched): `grep -c "localStorage.setItem('theme'" resources/views/layouts/navigation.blade.php` returns 2.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=HelpPageTest</automated>
  </verify>
  <done>The Help button sits beside the toggle in both bars, opens the correct role manual, the old Help nav link is gone, and HelpPageTest is green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild both manuals as wiki-style, cross-linked, verbatim-label documents (DEL-06)</name>
  <files>resources/views/student/help.blade.php, resources/views/lecturer/help.blade.php, tests/Feature/HelpPageTest.php</files>
  <read_first>
    - Student screens to quote VERBATIM: resources/views/student/home.blade.php, student/subjects/index.blade.php (the "Class enrollment" catalog: subject → class → Enroll flow, hide/unhide past semesters), student/subjects/class.blade.php (class page: exam list card, status pills "not yet open"/"opened"/"closed", taken/graded result link, the Start button disabled once attempted), student/attempts/show.blade.php (take-exam: sticky bar subject+exam name, timer, question progress, the instructions popup button, vertical stepper, Submit), student/results/show.blade.php.
    - Lecturer screens to quote VERBATIM: resources/views/lecturer/home.blade.php, lecturer/subjects/index.blade.php (subject CRUD), lecturer/subjects/manage.blade.php + partials/_classes-tab.blade.php + partials/_exams-tab.blade.php (Classes/Exams tabs, class code + progress bar, exam draft↔active toggle, Reset submissions, grading progress), lecturer/exams/show.blade.php + exams/questions/_form.blade.php (Details/Questions tabs, move up/move down, shuffle), lecturer/results/index.blade.php + results/show.blade.php (grading).
    - resources/views/student/help.blade.php + lecturer/help.blade.php — the current LINEAR v2.0 manuals (stale labels like "Enroll"/"View Sections"/"Apply"/"My Exams"/"Proceed"). Replace, do not append.
  </read_first>
  <action>
    Rebuild each help view as a wiki-style manual inside the existing x-app-layout. At the top (or in a sticky side column) render a TOPIC INDEX — a list of in-page anchor links (href="#topic-id") to every topic section. Give each topic section a matching id and heading. Between topics, add CROSS-LINKS: where one topic references another concept, link to that topic's anchor (e.g. the student "Taking an exam" topic cross-links to "Viewing your results"; the lecturer "Exams tab" topic cross-links to "Grading" and to "The exam editor"). Author the prose by walking the shipped screens in read_first and quoting their button/tab/link/pill text EXACTLY as rendered — do not paraphrase a label. Keep colour pairings dark-legible (reuse the token layer / matched raw pairs from the shipped views — plan 14-01 already fixed the component palettes).

    Choose a topic taxonomy that covers each role's real workflow (Claude's discretion on exact topic names, but each must map to a shipped screen):
    - Student topics (suggested): Home & dashboard; Enrolling in a class; Your subjects & class page; Taking a timed exam; Viewing your results.
    - Lecturer topics (suggested): Home & dashboard; Managing subjects; Class management (Classes tab); Managing exams (Exams tab); The exam editor; Grading.

    Update HelpPageTest: replace the old linear-heading assertions with the new topic headings; assert each manual renders a topic index of in-page anchors (assertSee an `href="#"` anchor per topic); assert at least one cross-link between topic sections exists; and add a handful of VERBATIM-LABEL assertions per role that would fail if a label were paraphrased (e.g. student: 'Class enrollment', 'Enroll', the class-page pill words, 'Submit'; lecturer: 'Move up', 'Move down', 'Shuffle', 'Reset submissions', the draft/active toggle label) — quote each exactly as it appears in the shipped view you read.
  </action>
  <acceptance_criteria>
    - Both manuals are wiki-style: `grep -c 'href="#' resources/views/student/help.blade.php` ≥ 5 and `grep -c 'href="#' resources/views/lecturer/help.blade.php` ≥ 5 (topic index anchors + cross-links).
    - `php artisan test --filter=HelpPageTest` passes with the new topic-heading, topic-index, cross-link, and verbatim-label assertions.
    - Every verbatim-label assertion added to HelpPageTest quotes a string that literally exists in the corresponding shipped view (spot-check each with `grep -n "<label>" resources/views/...` — record the file:line evidence in the SUMMARY, mirroring the v2.0 08-09 accuracy table).
    - `php artisan test` keeps the full PHPUnit suite green (baseline 441 passing).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=HelpPageTest</automated>
  </verify>
  <done>Both manuals are wiki-style with a topic index, cross-links between topics, and verbatim shipped labels; HelpPageTest guards headings, cross-links, and label accuracy; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest → help route | Unchanged: help routes stay behind auth + verified + role middleware (a guest is redirected to login, a lecturer cannot load the student manual and vice-versa) — HelpPageTest's six reachability/role-scoping tests remain the guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Information disclosure | cross-role manual access | mitigate | Do not change the route middleware; HelpPageTest already asserts a student is 403'd from the lecturer manual and vice-versa, and a guest is redirected to login — keep all six tests green. |
| T-14-02b | Tampering | manual is static Blade | accept | The manuals render only literal, server-authored copy — no user input is interpolated, so there is no injection surface. |
</threat_model>

<verification>
- `php artisan test --filter=HelpPageTest` green (reachability, role-scoping, navbar-button entry, topic headings, topic index, cross-links, verbatim labels).
- `php artisan test` green — full suite stays at baseline (441 passing).
- MANUAL (flagged, non-blocking prose read-through, joins the deferred human-verification list): a human reads each manual end-to-end to confirm the PROSE (not just the asserted labels) accurately describes the shipped workflow — HelpPageTest can prove labels are verbatim and topics exist, but not that every sentence is correct (the v2.0 08-09 precedent).
</verification>

<success_criteria>
Success Criterion 2 of Phase 14 is met: a help button beside the light/dark toggle opens a wiki-style user manual with topic navigation and cross-links between topics, with copy that quotes the shipped UI labels verbatim.
</success_criteria>

<output>
Create `.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-02-SUMMARY.md` when done. Include the verbatim-label accuracy table (asserted string → shipped view file:line) and the chosen topic taxonomy per role.
</output>
