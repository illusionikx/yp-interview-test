---
phase: 14-delivery-dark-mode-wiki-manual-demo-data-browser-tests
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - database/seeders/DatabaseSeeder.php
  - database/factories/UserFactory.php
  - tests/Feature/DatabaseSeederTest.php
  - README.md
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04]

must_haves:
  truths:
    - "php artisan migrate:fresh --seed stands the full demo up from an empty database with no exception"
    - "A second php artisan db:seed run does not throw and does not change any row count (re-runnable)"
    - "Many uniquely-named lecturers carry a Dr/Prof/PhD title; no student carries one"
    - "Past-semester data (dated through App\\Support\\Semester) holds graded exams and filled classes that exercise every available status"
    - "There are at least 3 more subjects, classes, and exams beyond the original demo graph"
  artifacts:
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Idempotent expanded demo graph: bulk titled lecturers + students, past-semester graded data across every status, 3–5 more subjects/classes/exams"
      contains: "Semester"
    - path: "tests/Feature/DatabaseSeederTest.php"
      provides: "Assertions for SEED-01/02/03 plus the preserved idempotency guard"
      contains: "isPast"
    - path: "README.md"
      provides: "Clean-clone setup: create DB, migrate, seed, expanded demo credentials + workflow walkthrough"
      contains: "migrate:fresh --seed"
  key_links:
    - from: "database/seeders/DatabaseSeeder.php"
      to: "app/Support/Semester.php"
      via: "past-semester section/exam windows derived from a Semester instance's startsAt()/endsAt(), never relative-month subtraction"
      pattern: "Semester"
    - from: "tests/Feature/DatabaseSeederTest.php"
      to: "database/seeders/DatabaseSeeder.php"
      via: "$this->seed(DatabaseSeeder::class) then assert counts, title exclusivity, past-semester presence, and every status"
      pattern: "DatabaseSeeder::class"
---

<objective>
SEED-01..04: expand the demo seeder so `php artisan migrate:fresh --seed` stands up, from an empty database, a rich and re-runnable demo — many uniquely-named lecturers (the only accounts carrying a Dr/Prof/PhD title) and many students, past-semester data holding graded exams and filled classes that exercise every available status, and 3–5 more subjects/classes/exams — then prove it stands up and document the clean-clone path in the README. Authored now (last but one) on purpose: statuses are final only after phases 10–13 shipped, so "every status" is now a fixed target.

Purpose: A grader can clone, migrate, seed, and immediately review a realistic, status-complete portal — the graded reproducibility criterion.
Output: An expanded idempotent seeder, extended DatabaseSeederTest, a proven `migrate:fresh --seed`, and an accurate README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-CONTEXT.md

# Status vocabularies to exercise (every available status):
#   EnrollmentStatus (app/Enums/EnrollmentStatus.php): enrolled, withdrawn, rejected
#   Exam::availabilityState() (app/Models/Exam.php): opening, available, closed
#   Attempt.status (app/Models/Attempt.php): in_progress, submitted, graded
#   Section::windowStatus() (app/Models/Section.php): opens, open, closed
# Idempotency contract: DatabaseSeederTest::test_seeder_is_idempotent_on_repeat_runs asserts
# STRICT row-count equality across two seed runs — bulk creation MUST be count-guarded so a
# second run is a no-op (named accounts already use firstOrCreate).
@database/seeders/DatabaseSeeder.php
@database/factories/UserFactory.php
@app/Support/Semester.php
@tests/Feature/DatabaseSeederTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bulk titled lecturers + students + 3–5 more subjects/classes/exams (SEED-01, SEED-03), count-guarded</name>
  <files>database/seeders/DatabaseSeeder.php, database/factories/UserFactory.php, tests/Feature/DatabaseSeederTest.php</files>
  <read_first>
    - database/seeders/DatabaseSeeder.php — the existing firstOrCreate demo graph (named lecturer/students, 2 subjects, 2 sections, 1 exam) that must stay intact and additive.
    - database/factories/UserFactory.php — has `lecturer()`/`student()` states; `definition()` uses `fake()->name()` and `fake()->unique()->safeEmail()`.
    - tests/Feature/DatabaseSeederTest.php — the idempotency test counts EVERY table; new bulk rows must be count-stable across runs.
  </read_first>
  <behavior>
    - After seeding, `User::where('role', Lecturer)->count()` ≥ a documented target of many (e.g. ≥ 8) and `User::where('role', Student)->count()` ≥ a documented target of many (e.g. ≥ 25).
    - The number of lecturers whose name carries a Dr/Prof/PhD title is ≥ 6; the number of STUDENTS whose name carries any such title is exactly 0 (exclusivity — the load-bearing SEED-01 rule).
    - `Subject::count()` ≥ 5 (original 2 + at least 3), and each new subject has at least one Section and at least one Exam.
    - Re-seeding leaves all of the above counts identical (no duplication).
  </behavior>
  <action>
    Add a titled-name path for bulk lecturers — either a new UserFactory state (e.g. a `titled()` state that prefixes `fake()->name()` with a random academic title such as "Dr.", "Prof.", or "Assoc. Prof.", or appends ", PhD") or a small seeder helper that does the same — and use plain `fake()->name()` for students so no student name ever carries a title. Create the bulk lecturers and students with count-guarded creation: read the current role count and only `factory()->count(target - existing)->create()` the shortfall, so a second seed run is a no-op and the strict idempotency count-equality holds. Add 3–5 more subjects (deterministic natural keys via firstOrCreate on `code`), each with at least one Section (an open current-semester window) and at least one Exam, assigning some of the new subjects to the bulk lecturers via the subject_user pivot (syncWithoutDetaching). Enroll a spread of the bulk students into the new sections so the classes are visibly filled.

    Extend DatabaseSeederTest with the SEED-01/SEED-03 assertions from the behaviour block (count targets, title exclusivity via a regex over names, ≥ 5 subjects each with a section and an exam). Keep the existing demo-graph and idempotency assertions untouched and green.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=DatabaseSeederTest` passes, including the preserved `test_seeder_is_idempotent_on_repeat_runs` (strict count equality).
    - Title exclusivity holds: the DatabaseSeederTest assertion that zero students match the title regex and ≥ 6 lecturers do is green.
    - `grep -nE 'count\(' database/seeders/DatabaseSeeder.php` shows bulk factory calls are guarded by an existing-count read (no unconditional `->count(n)->create()` that would duplicate on re-seed) — verify each bulk block is count-guarded.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DatabaseSeederTest</automated>
  </verify>
  <done>The seeder creates many titled lecturers, many untitled students, and 3–5 more subjects/classes/exams idempotently; DatabaseSeederTest guards counts and title exclusivity and stays idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Past-semester graded data exercising every available status, dated through Semester (SEED-02)</name>
  <files>database/seeders/DatabaseSeeder.php, tests/Feature/DatabaseSeederTest.php</files>
  <read_first>
    - app/Support/Semester.php — construct the prior semester as `new Semester(Semester::current()->year - 1, Semester::current()->number)` (or the prior ordinal) and take its `startsAt()`/`endsAt()` for past-window dates. Do NOT compute past dates by relative-month subtraction (SEED-02).
    - app/Models/Exam.php (availabilityState) + Section.php (windowStatus) + Attempt.php (status) + app/Enums/EnrollmentStatus.php — the four status vocabularies listed in the context comment above.
    - database/seeders/DatabaseSeeder.php::seedDemoAttempt — the Eloquent-direct attempt-building pattern (never a simulated HTTP submit) + AttemptGrader usage to reach 'graded'.
  </read_first>
  <behavior>
    - A past-semester Section exists whose (year, semester) resolves to a Semester where `isPast()` is true, with a fully-filled enrollment (capacity reached) and a graded exam.
    - Every EnrollmentStatus (enrolled, withdrawn, rejected) appears at least once across the demo enrollments.
    - Every Attempt status (in_progress, submitted, graded) appears at least once.
    - Both a currently-'available' exam and a past 'closed' exam exist (Exam::availabilityState), and a not-yet-'opening' exam exists.
    - Re-seeding does not duplicate any of the above (count-guarded / firstOrCreate).
  </behavior>
  <action>
    Add a past-semester block to the seeder. Derive the past window from a Semester instance's startsAt()/endsAt() (route ALL past dating through App\Support\Semester — see read_first). Create a past-semester subject/section with its opens_at/closes_at set from the past semester so `windowStatus()` reads 'closed', fill it to capacity with bulk students (enrolled), and give it a past exam whose available_from/available_until fall inside that past window so `availabilityState()` reads 'closed'. Build graded attempts on that past exam directly via Eloquent (mirroring seedDemoAttempt), invoking AttemptGrader so the attempts reach 'graded'. Add the remaining statuses so the full matrix is covered: at least one 'in_progress' attempt (no submitted_at, current exam), the existing 'submitted' demo attempt, a withdrawn and a rejected enrollment (the rejected one carries a RejectionReason), a current 'available' exam, and a future 'opening' exam (available_from in the future via the current/next Semester). Guard every block with count checks or firstOrCreate so re-seeding is a no-op.

    Extend DatabaseSeederTest with the SEED-02 assertions from the behaviour block: a past Semester section exists (assert via `Semester::forDate` / isPast on its year+semester), every EnrollmentStatus present, every Attempt status present, and the 'closed'/'available'/'opening' exam states present. Keep idempotency green.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=DatabaseSeederTest` passes with the new SEED-02 assertions AND the preserved idempotency test.
    - Past dating routes through Semester (positive): `grep -nE 'Semester::|new Semester|startsAt\(|endsAt\(' database/seeders/DatabaseSeeder.php` shows the past-window derivation.
    - No relative-month/-year fudge for semester dating (negative): `grep -nE 'subMonths|subYears' database/seeders/DatabaseSeeder.php` returns no matches.
    - The status matrix is asserted green: enrollment {enrolled, withdrawn, rejected}, attempt {in_progress, submitted, graded}, exam availability {opening, available, closed} each appear in DatabaseSeederTest's assertions.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DatabaseSeederTest</automated>
  </verify>
  <done>The seeder holds past-semester graded, filled data covering every enrollment/attempt/exam-availability status, all dated through Semester, and the test proves each status is present while staying idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Prove migrate:fresh --seed stands up (SEED-04) + update README setup/credentials/walkthrough</name>
  <files>README.md, database/seeders/DatabaseSeeder.php</files>
  <read_first>
    - README.md — the current "Setup From a Clean Clone", "Seeded Demo Credentials", and "Per-Role Walkthrough" sections. NOTE the walkthrough is STALE: it says a lecturer "publishes and assigns an exam to a classroom" and a student "sees only the exams assigned to their classroom" — Phase 10 removed manual assignment (auto-assignment derives visibility from subject enrollment). The credentials table lists only the 4 named accounts.
    - database/seeders/DatabaseSeeder.php — the final expanded graph from Tasks 1–2, for an accurate dataset description.
  </read_first>
  <action>
    Actually run `php artisan migrate:fresh --seed` against the configured dev database and confirm it completes from an empty schema with no exception, then run `php artisan db:seed` a second time and confirm it also completes with no exception (re-runnable). This is autonomously verifiable — it is a database operation, no browser needed.

    Update the README: keep the create-DB / migrate / seed steps accurate for a clean clone; refresh the "Seeded Demo Credentials" section to describe the expanded dataset (the four named fixed-credential accounts remain the documented entry points at the shared password, plus a note that many additional titled lecturers and students are seeded for realism); and correct the stale "Per-Role Walkthrough" so it matches the shipped v3.0 workflow — a lecturer manages a subject's classes and exams from the per-subject hub and an exam becomes visible to a class automatically once it is active (no manual assign step), and a student enrolls in a class and takes exams from their class page. Do not add the Dusk instructions here — plan 14-04 owns the "Running the browser tests" README section.
  </action>
  <acceptance_criteria>
    - `php artisan migrate:fresh --seed` exits 0 with no exception in output (capture the tail in the SUMMARY).
    - `php artisan db:seed` run immediately afterward also exits 0 with no exception (re-runnable proof).
    - README documents create-DB → migrate → seed and the demo credentials; `grep -c 'migrate:fresh --seed' README.md` ≥ 1.
    - The stale "assign … to a classroom" language is gone: `grep -niE 'assign(s|ed)? .*(to a )?(classroom|class)' README.md` no longer describes a manual per-class assignment step (auto-assignment wording present instead) — verify by reading the section.
  </acceptance_criteria>
  <verify>
    <automated>php artisan migrate:fresh --seed && php artisan db:seed</automated>
  </verify>
  <done>migrate:fresh --seed stands the full expanded demo up from empty and re-runs cleanly, and the README's setup, credentials, and walkthrough match the shipped v3.0 workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded demo credentials | The seeder writes fixed-credential demo accounts (shared password) — an evaluation-only posture, already documented in the README as not a production security stance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Spoofing | fixed demo passwords | accept | Documented evaluation-only deliverable; the README already warns not to reuse the demo password scheme in production. No new credential surface is added — bulk accounts share the same demo password by design. |
| T-14-03b | Tampering | re-seed clobbering reviewer edits | mitigate | Named accounts use firstOrCreate and bulk creation is count-guarded, so a re-seed neither throws nor overwrites a reviewer's manual edits (Pattern 1). |
</threat_model>

<verification>
- `php artisan test --filter=DatabaseSeederTest` green (SEED-01/02/03 assertions + preserved strict-idempotency guard).
- `php artisan migrate:fresh --seed && php artisan db:seed` both exit 0 (SEED-04 stands up + re-runnable).
- `php artisan test` green — full suite stays at baseline (441 passing).
- Note: running `php artisan test` afterward wipes the dev DB (RefreshDatabase) — the README already documents re-running `migrate:fresh --seed` to restore the demo data for manual review.
</verification>

<success_criteria>
Success Criterion 3 of Phase 14 is met: `php artisan migrate:fresh --seed` stands up, from an empty database, many uniquely-named lecturers (the only accounts carrying Dr/PhD prefixes) and students, past semesters holding graded exams and filled classes that exercise every available status, and 3–5 further subjects, classes, and exams — re-runnably, and documented in the README.
</success_criteria>

<output>
Create `.planning/phases/14-delivery-dark-mode-wiki-manual-demo-data-browser-tests/14-03-SUMMARY.md` when done. Record the final seeded counts, the status matrix coverage, and the `migrate:fresh --seed` run tail.
</output>
